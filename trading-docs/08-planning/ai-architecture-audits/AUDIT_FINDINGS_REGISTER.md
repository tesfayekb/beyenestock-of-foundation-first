# AI Architecture Audit Findings Register

> **Purpose:** Single source of truth for all findings across Phase 1 audits (P1.3.1 through P1.3.13) and Phase 0/1 governance work.
>
> **Status:** Living document. Updated after each per-item audit merges to main.
>
> **Created:** 2026-04-26 (Phase 1 P1.3.1b of `CONSOLIDATED_PLAN_v1.2_APPROVED.md`)
> **Last updated:** 2026-04-28
> **Last updated by:** P1.3.7 (AI-SPEC-008 / OPRA Flow Alpha audit appended — **CLUSTER B AUDIT 3 of 4**)
> **Repo HEAD at creation time:** `f456922cdafcf0f99b757958dab0309aa9d08248` (P1.3.1 merged).

---

## How To Use This Register

- **Operator:** read this when making batch governance decisions, when assessing Gate 1 readiness, or when deciding which findings to prioritize for Phase 2 corrections. The register is the one-glance view of "what's open, what's resolved, and what crosses multiple specs"; the per-item redlines (`AI-SPEC-001.md`, `AI-SPEC-002.md`, …) remain the per-spec source of truth for the full evidence chain behind each entry.
- **Future audit Cursors (P1.3.2 through P1.3.13):** **APPEND** new findings from your per-item audit to the appropriate sections below. Do **NOT** modify entries from prior audits unless the audit explicitly resolves a prior finding (in which case **move** the row to §4 "Resolved Findings" with date + resolution note + reference to the resolving audit). Increment the `Update Log` with a new row each time you append.
- **Claude (cross-check) and GPT (validator):** consult this register during cross-spec review to identify themes and contradictions. If a per-item audit's §7.2 (cross-spec contradictions) or §8 (carry-forward findings) surfaces a theme that is already in §5 here, link the new audit's spec ID into that theme's "Affected Specs" cell rather than creating a duplicate row.

> **Update mandate:** Each subsequent per-item audit (P1.3.2 → P1.3.13) MUST update this register as part of its workflow. The audit redline file is the per-spec evidence; this register is the cross-spec roll-up. PRs that produce a per-item redline without updating this register will be returned for completion before merge.

> **ID convention:** Open findings use `<class>-<spec-id>-<n>` (e.g., `C-AI-001-1` = Class C, AI-SPEC-001, item 1; `B-AI-002-3` = Class B, AI-SPEC-002, item 3). Class A items roll up by spec rather than per-item to avoid noise (Class A is mechanical and batch-cleared at Gate 1). Pre-audit findings (Phase 0 / Phase 1 infrastructure work) use the `PRE-P{phase}-N` prefix and live in §0 below.

---

## 0. Pre-Audit Findings (Phase 0 / Phase 1 Infrastructure)

These findings emerged during Phase 0 governance setup and Phase 1 audit infrastructure work (P1.1 evidence pack, P1.2 cross-cutting matrix, P1.3.0 audit template, P1.3.0b raw spec archive). They are NOT per-item audit findings — they predate the per-item audits — but they are real findings that affect the audit and integration work and must be tracked alongside per-item findings so that the register's stated purpose ("single source of truth for ALL findings") holds.

Findings are categorized by source phase. Each finding has an ID prefix indicating origin:

- **PRE-P0-N** — Phase 0 governance setup
- **PRE-P11-N** — P1.1 evidence pack
- **PRE-P12-N** — P1.2 cross-cutting matrix
- **PRE-P130-N** — P1.3.0 audit template
- **PRE-P130B-N** — P1.3.0b raw spec archive

### 0.1 Phase 0 Findings (PRs #54, #55)

| ID | Source PR / File | Finding | Status | Resolution Path |
|----|------------------|---------|--------|------------------|
| PRE-P0-1 | PR #54 / `feature-proposals.md` lines 26, 84 | `master-plan.md` literal references should be `MASTER_PLAN.md` (case mismatch on case-sensitive filesystems). Verified at HEAD: line 26 contains "MUST be added to `master-plan.md` before implementation begins"; line 84 contains "Reference to PLAN-XXX-NNN in master-plan.md". | open — deferred | Phase 4 (when `feature-proposals.md` is opened to add FP-001 entry); operator approved deferral 2026-04-26 |
| PRE-P0-2 | PR #55 / `.cursorrules` + `.lovable/rules.md` | Pre-existing structural divergence: `## PHASE GATE VERIFICATION PROTOCOL (MANDATORY)` is at `.cursorrules` line 264 vs `.lovable/rules.md` line 136. Pre-existing, NOT introduced by P0.2. (Cross-listed in evidence pack §9.5.) | open — pre-existing debt | Separate parity audit task (out of scope for current Phase 1 plan) |
| PRE-P0-3 | PR #55 / `.cursorrules` + `.lovable/rules.md` | Section naming drift: `.cursorrules` line 123 says "REFERENCE INDEX MAINTENANCE"; `.lovable/rules.md` line 123 says "REFERENCE INDEX MAINTENANCE AND RECONCILIATION". (Cross-listed in evidence pack §9.5.) | open — pre-existing debt | Same parity audit task as PRE-P0-2 |
| PRE-P0-4 | PR #55 + PR #58 / `.cursorrules` line 36 | `.cursorrules` line 36 references "11 rules" but `constitution.md` defines exactly 10 T-Rules (T-Rule 1 Foundation Isolation through T-Rule 10 Silent Failures Are Forbidden). Verified at HEAD by `grep -nE '^### T-Rule [0-9]+' trading-docs/00-governance/constitution.md`. | open — pre-existing debt | Phase 5 cleanup |

### 0.2 P1.1 Evidence Pack Findings (PR #56)

| ID | Source Section | Finding | Status | Resolution Path |
|----|----------------|---------|--------|------------------|
| PRE-P11-1 | Evidence pack §9.1 | `trading-docs/00-governance/change-control-policy.md` missing — referenced by `.cursorrules`, `.lovable/rules.md`, and `feature-proposals.md` but file does not exist at HEAD. | open — pre-existing governance debt | Separate cleanup task per consolidated plan §1; will affect subsequent audit §11 governance update entries |
| PRE-P11-2 | Evidence pack §9.2 | Reference indexes missing in `trading-docs/07-reference/`: `function-index.md`, `event-index.md`, `config-index.md`, `env-var-index.md` (all four absent at HEAD per evidence pack §9.2 lines 507–512). | open — pre-existing governance debt | Separate cleanup task per consolidated plan §1; SPEC VERIFICATION PROTOCOL operates without these indexes |
| PRE-P11-3 | Evidence pack §9.4 | `_RISK_PCT` ladder non-monotonic at HEAD: Phase 1 (0.010) > Phase 2 (0.0075). Re-introduced by 2026-04-20 width widening (different mechanism than B1-1 originally addressed). Already cross-referenced as cross-spec theme in §5 of this register. | resolved — Action 7b (Phase 2-4 scaled 2× to preserve dollar-equivalent ladder; monotonicity test added at `test_risk_engine.py`) | Separate task before Day 40 of paper trading; explicitly not fixed by AI-SPEC-012 (Item 12 sizing) |
| PRE-P11-4 | Evidence pack §9.6 (newly discovered) | `counterfactual_pnl` is 3 columns on `trading_prediction_outputs`, NOT a separate table. The migration filename `20260421_add_counterfactual_pnl.sql` is correct, but the artifact is `ALTER TABLE ... ADD COLUMN`. P1.2 / P1.3 audits must treat as columns, not as a table. | open — newly discovered during P1.1 | Phase 2 corrections to AI-SPEC-010, AI-SPEC-012; flagged in matrix carry-forward #5 |
| PRE-P11-5 | Evidence pack §10.3.3 line 687 | `gex:atm_iv` consumer-only orphan: sole consumer at `backend_agents/macro_agent.py:214`, no producer in any production code. Already cross-referenced as matrix carry-forward #2 and tracked in §5 of this register. | open — pre-existing operational risk | Resolution path: per-spec audits for AI-SPEC-001, AI-SPEC-005 will document; eventual fix wires producer in `gex_engine.py` after recompute |
| PRE-P11-6 | Evidence pack §10.3.3 line 688 | `gex:updated_at` consumer-only orphan: referenced as `age_key` in `_safe_redis()` docstring example at `prediction_engine.py:123` (inside docstring, not an actual call site). Already cross-referenced as matrix carry-forward #1. **Mechanism note:** evidence pack §10.3.3 originally framed this as "fail-open fallthrough"; matrix v1.1 corrected the mechanism to "the gate function is dead code". The conclusion (no effective freshness gate) is unchanged. | open — pre-existing operational risk | Resolution path: AI-SPEC-005 / AI-SPEC-013 audits will document; eventual fix wires both `_safe_redis()` caller AND producer |
| PRE-P11-7 | Evidence pack §10.3.1 (lines 670–675) | `strategy:bull_debit_spread:enabled` and `strategy:bear_debit_spread:enabled` named in `MASTER_PLAN.md` lines 59–60 but NOT present anywhere in code despite Phase 2B (Bull/Bear Debit Spreads) being declared COMPLETE in `MASTER_PLAN.md`. Phase 2B Rule 5 ("New strategies are gated by Redis flags") appears violated. | open — governance debt with naming inconsistency | Resolution path: cross-spec theme already in §5 (strategy-class taxonomy); affects AI-SPEC-007 / AI-SPEC-011 / AI-SPEC-012 audits |

### 0.3 P1.2 Cross-Cutting Matrix Findings (PR #57)

| ID | Source Section | Finding | Status | Resolution Path |
|----|----------------|---------|--------|------------------|
| PRE-P12-1 | Matrix §1 carry-forward #4 (corrected v1.1) | `_safe_redis()` is defined at `prediction_engine.py:100` but never called from outside its own docstring (line 120 inside docstring lines 107–131). Function is dead code at HEAD. GEX freshness gate is non-functional because the gate function is dead code, NOT because `gex:updated_at` lacks a producer. Already tracked in §5 of this register as cross-spec theme. | open — pre-existing operational risk | Resolution path: AI-SPEC-005 / AI-SPEC-013 audits will plan freshness substrate buildout (caller + producer both needed) |
| PRE-P12-2 | Matrix §4 `risk_engine.py` block | 3-spec collision: AI-SPEC-001 + AI-SPEC-012 + AI-SPEC-013 all modify `backend/risk_engine.py`. Module-level merge order proposed: **1 → 13 → 12**. | open — sequencing constraint | Resolution path: hard sequencing rule for P1.3 / Phase 2; the `_RISK_PCT` ladder fix (PRE-P11-3) must land BEFORE Item 12 begins |
| PRE-P12-3 | Matrix §7 dependency graph | Soft sequencing question between AI-SPEC-012 and AI-SPEC-013: Item 12 depends on Item 13 demotion paths; Item 13 promotion may need Item 12 capital framework. | open — soft sequencing | Resolution path: P1.3 Item 12 + Item 13 audits reviewed together; ship Item 13 advisory-only first, Item 12 next, then promote Item 13 |
| PRE-P12-4 | Matrix §1 metadata + §3 corrections | `AI_ARCH_EVIDENCE_PACK.md` §10.2 line 617 + §10.3.3 line 688 carry the same `_safe_redis` mischaracterization that v1.1 corrected in the matrix. Cursor flagged but did not fix (immutable input from P1.1). | open — minor doc inconsistency | Resolution path: future cleanup task; conclusion in evidence pack §10.3.3 is correct, only mechanism description is wrong; defer to separate amendment task |

### 0.4 P1.3.0 Audit Template Findings (PR #58)

| ID | Source | Finding | Status | Resolution Path |
|----|--------|---------|--------|------------------|
| PRE-P130-1 | Cursor risks/follow-up (PR #58) | Constitution rule-count mismatch (`.cursorrules` line 36 says "11 rules" vs `constitution.md`'s 10 T-Rules) — same as PRE-P0-4. Surfaced again during P1.3.0 cross-check. | open — duplicate of PRE-P0-4 | See PRE-P0-4 resolution path |

### 0.5 P1.3.0b Raw Spec Archive Findings (PR #59)

| ID | Source | Finding | Status | Resolution Path |
|----|--------|---------|--------|------------------|
| PRE-P130B-1 | Cursor risks/follow-up (PR #59) | `trading-docs/04-modules/` already existed at branch base with pre-existing `README.md` and `exit-engine.md`. Phase 4 placement of validated module documentation as siblings of new `archive/` directory still works. | informational only — no action | Phase 4 module placement |
| PRE-P130B-2 | Cursor risks/follow-up (PR #59) | Tarball delivered as `.tar` (uncompressed) instead of `.tar.gz` per prompt's anticipated format. `tar -xf` worked transparently; checksums verified clean. | informational only — no action | Future-prompt accuracy improvement if delivery format changes |

### 0.6 Cross-Reference With Sections 1–6

Several Phase 0 / Phase 1 findings ALSO appear elsewhere in the register. The cross-references below are intentional — findings can appear in §0 (origin record) AND §5 (cross-spec theme view) AND §1–§3 (per-audit Class C/B/A entries) — each section serves a different purpose. The register should be read top-to-bottom for discovery, but operator decisions reference whichever section is most actionable.

| Pre-audit ID | Also tracked in | Per-audit ID (where applicable) | Purpose of the cross-reference |
|--------------|-----------------|--------------------------------|-------------------------------|
| PRE-P11-3 (`_RISK_PCT` non-monotonic) | §5 cross-spec themes; §2 Class B | B-AI-001-7 (AI-SPEC-001 §10.2 B7) | §0 records origin in evidence pack §9.4; §5 tracks the multi-spec impact; §2 records Item 1's specific buildout constraint ("must not worsen") |
| PRE-P11-4 (`counterfactual_pnl` columns vs table) | will appear in §2 Class B when AI-SPEC-010 / AI-SPEC-012 audits complete | (TBD by P1.3.10 / P1.3.12) | §0 records origin in evidence pack §9.6; future per-item audits will surface the correction in §2 |
| PRE-P11-5 (`gex:atm_iv` orphan) | §5 cross-spec themes (theme: `_safe_redis()` dead code family); AI-SPEC-001 §8 yes-impact | (referenced by B-AI-001-6 indirectly) | §0 records the consumer-only finding in evidence pack §10.3.3; §5 tracks it as part of the freshness substrate theme |
| PRE-P11-6 (`gex:updated_at` orphan) | §5 cross-spec themes (theme: `_safe_redis()` dead code family); §2 Class B | B-AI-001-6 (AI-SPEC-001 §10.2 B6) | §0 records the consumer-only finding; §2 records Item 1's V0.1-prerequisite buildout (caller + producer) |
| PRE-P11-7 (debit-spread feature flags) | §5 cross-spec themes (strategy-class taxonomy); §3 Class A | A2 within AI-SPEC-001 row of §3 | §0 records origin in MASTER_PLAN lines 59–60 vs `risk_engine.py:105–106`; §3 records the per-spec mechanical correction; §5 tracks the system-wide cleanup |
| PRE-P12-1 (`_safe_redis()` dead code) | §5 cross-spec themes; §2 Class B | B-AI-001-6 (AI-SPEC-001 §10.2 B6) | §0 records origin in matrix §1 carry-forward #4 (v1.1 corrected); §5 tracks it as a multi-spec theme; §2 records Item 1's specific impact |

---

## 1. Open Class C Escalations (Operator Decisions Required)

These items require explicit operator decisions before redline closure. Per `CONSOLIDATED_PLAN_v1.2_APPROVED.md` §3, Class C corrections involve architectural intent and have operator-only authority. Per template `_template.md` §10.3, "Class C corrections block the redline from closing" until the operator decides.

| ID | From | Issue | Conflicting D-XXX or Rule | Default Position | Status |
|----|------|-------|---------------------------|------------------|--------|
| C-AI-001-1 | AI-SPEC-001 §10.3 C1 | `size_multiplier_cap` channel (Spec §7 — paper-binding V0.2 + small-live post-graduation authority for an LLM-driven sizing reducer that composes `min` with `_RISK_PCT[phase]`). Does it require new D-023 (consolidated plan default), or fall inside D-014's existing "automatic regression" scope? | T-Rule 4 (Locked Decisions Are Final) + D-014 (Position Sizing); consolidated plan §5 reserves D-023 placeholder for "AI authority boundary" | Ratify D-023 with explicit scope: LLM-driven sizing reducer; cannot increase any cap; cannot override D-005 / D-010 / D-011 / D-022; promotion to live-binding requires V0.2 graduation criteria + 90-day A/B per `system-state.md` line 8 (`live_trading: blocked_until_90day_AB_test_passes`). | open |
| C-AI-001-2 | AI-SPEC-001 §10.3 C2 | Spec §12 V0.1 Advisory → V0.2 Paper-Binding gate AND Spec §13 Lean Live 0.25x → 0.5x gate. Both gates use a mix of statistical thresholds (≥200 cards, ≥30 actions, Wilson lower-95 ≥ 0.40) and operational SLOs (parse rate ≥99.5%, prompt frozen ≥10 days). They do not specify whether transitions are automated when criteria pass or require explicit operator approval per event; Spec §17 ("authority degradation requires explicit operator action") only addresses *demotion*, not promotion. | T-Rule 9 (Paper Phase Is Mandatory, 45 days minimum, 12 GLC criteria) + D-013 (Paper Phase) + `system-state.md` line 8 (`live_trading: blocked_until_90day_AB_test_passes`) | Per T-Rule 9 + `system-state.md`: any transition that affects live capital (V0.2 → small-live veto, lean live 0.25x → 0.5x) requires explicit operator approval per event; paper-binding-only promotions can be automated subject to GLC-001..012 cleanliness. Encode this distinction in D-023 alongside C1. | open |
| C-AI-001-3 | AI-SPEC-001 §10.3 C3 | Spec §4 ("Capped opportunity lean") + Spec §5 (Opportunity Lean Constraints). The opportunity-lean channel is the only Governor authority surface that *proposes* a trade rather than gating one (it is admissibility-of-a-new-strategy-class, not just a sizing-cap on an already-proposed trade). Even at ≤0.25x paper-only, this is in tension with Spec §27 ("AI controls admissibility and size ceilings, not trade execution") and with Spec §27's own self-flagged dangerous assumption about event-risk precision. | T-Rule 5 (Capital Preservation Is Absolute) — lean is reductive in dollar terms (small size, defined risk) but additive in *trade count*; T-Rule 9 satisfied by V0.1 paper-only constraint | Per consolidated plan §5 conservative-first principle: defer the lean channel to a separate spec (AI-SPEC-001b or AI-SPEC-014); ship core Item 1 V0.1 as veto-only. Re-introduce lean only after the core Governor passes graduation. | open |
| C-AI-002-1 | AI-SPEC-002 §10.3 C1 | Spec §2 `decision_outcome` enum is binary-coded for "opened OR blocked" (8 values: `opened_traded`, `blocked_governor`, `blocked_meta_labeler`, `blocked_adversarial`, `skipped_rules`, `blocked_constitutional`, `halted_blocked_cycle`, `synthetic_case`). Has no value for **Governor sizing reductions** — the `size_multiplier_cap` channel proposed in AI-SPEC-001 §7 / `C-AI-001-1`. Without coordination, Item 1's size-cap writes will fail Item 2's CHECK constraint. | T-Rule 4 (Locked Decisions Are Final) + cross-spec coordination via D-023 (proposed in AI-SPEC-001 §11 as the AI authority boundary D-XXX; covers `C-AI-001-1` / `C-AI-001-2` / `C-AI-001-3` dispositions) | Operator decision: (option 1) extend Item 2 enum with `cap_governor` / `reduced_governor` BEFORE Item 2 ships; (option 2) D-023 wording requires AI-SPEC-001 to write `opened_traded` for size-cap decisions and Item 1's authority is limited to size-cap-not-block. Either way, encode resolution in D-023 alongside the AI-SPEC-001 dispositions. **Folds into existing D-023 wording, not a new D-XXX.** | open |
| C-AI-002-3 | AI-SPEC-002 §10.3 C3 | Spec §1 architectural commitment ("`trading_positions` remains the execution ledger only — Don't overload it with attribution"). Reality: `trading_positions` already has 4 dead `attribution_*` BOOLEAN columns from baseline migration `20260416172751_*.sql:203–206` (`attribution_direction`, `attribution_structure`, `attribution_timing`, `attribution_vol`). No production code path writes them (verified by `grep -rln "attribution_(direction\|structure\|timing\|vol)" backend/` returning zero matches). Spec does not address. | T-Rule 2 + V0 baseline schema documented in `MARKETMUSE_MASTER.md:351–356` — pre-existing schema commitment | Operator decision on disposition: (option 1) **deprecate-in-place** — add migration comment noting columns abandoned and never written; Item 2's `strategy_attribution` is canonical attribution surface. Lowest-cost. (option 2) **formally remove via DROP COLUMN migration** — sequenced before `strategy_attribution` migration; risk: any historical row with non-NULL values would lose data. (option 3) **repurpose** — define what each column SHOULD mean given four-table architecture; backfill from `strategy_attribution` rows. Highest-cost. **Default position:** option 1 (deprecate-in-place), unless a Phase 2 audit discovers silent consumers. | open |
| C-AI-004-1 | AI-SPEC-004 §10.3 C1 | Spec §4 (`item_promotion_records`) creates a per-item authority-promotion gate that operates alongside the existing `paper_phase_criteria` table (12 GLC criteria per D-013, system-wide go-live gate, migration `20260417000001_paper_phase_criteria.sql`). Spec is silent on coordination: does GLC-005 (Sharpe ≥ 1.5) failing block all per-item promotions, or only the system go-live? Does `item_promotion_records.governor_v1.short_gamma_veto.authority_level = 'production'` require GLC-005 to pass first? Without explicit wording, the two gate authorities create ambiguity. | T-Rule 2 (Governance Documents Are Authoritative); D-013 Paper Phase + T-Rule 9 (Paper Phase Is Mandatory, 12 GLC); T-Rule 4 (Locked Decisions Are Final) for any new D-XXX | Operator decision required between three options: (option 1) **independent gates** — Spec §4 explicitly states `paper_phase_criteria` (system-level) and `item_promotion_records` (per-item-within-system) are independent; both must pass before any item is `production`. Lowest-cost; preserves D-013 unchanged. (option 2) **unified gate** — add `GLC-013: Replay Validation Complete (per Item 4 promotion gate evaluation for at least one item)` as a 13th paper_phase_criteria row; D-013 amended via a new D-XXX (e.g., D-024). Tighter. (option 3) **hierarchical** — `item_promotion_records` is binding ONLY for paper-phase per-item authority; production-binding still requires `paper_phase_criteria` + 90-day A/B per D-013. **Default position (per consolidated plan §5 conservative-first principle):** option 1 (independent gates with explicit non-conflict statement; preserves D-013). | open |
| C-AI-004-2 | AI-SPEC-004 §10.3 C2 | Cross-spec contract ambiguity: AI-SPEC-001 §12 V0.2 paper-binding gate ("≥ 200 replay/advisory decision cards" — `B-AI-001-8`) requires a count of "decision cards" but the canonical card surface is ambiguous between (i) `replay_eval_cases.calibration_eligible = true` rows (Item 4 surface, Spec §4 lines 388–432); (ii) `shadow_predictions` rows (existing forward A/B surface from `backend/shadow_engine.py`); (iii) some "advisory" surface that doesn't yet exist. Without explicit coordination, the Spec §12 gate cannot be evaluated. | T-Rule 4 (Locked Decisions Are Final) + cross-spec coordination via D-023 (proposed in AI-SPEC-001 §11; AI-SPEC-002 §11 addendum already extended D-023 wording for `decision_outcome` enum coordination) | Operator decision required: (option 1) **canonical = `replay_eval_cases.calibration_eligible = true`** — most rigorous; matches Spec §1 line 49 hard rule ("No archived chain = no calibration-grade option pricing replay"). Excludes shadow_engine.py rows. Lower throughput; the 200-card calendar may slip. (option 2) **canonical = (`replay_eval_cases.calibration_eligible = true`) UNION (`shadow_predictions` rows where `regime` is non-degenerate AND timestamp is post-spec-locked HEAD)** — broader; faster to 200 but mixes mechanisms. (option 3) **two separate gates** — D-023 splits into "≥200 replay cards" (Item 4) AND "≥X shadow cards" (shadow_engine.py); both must pass. **Default position:** option 1 (rigorous; matches spec architectural intent). Encode resolution in D-023 wording alongside AI-SPEC-001 / AI-SPEC-002 dispositions. **Folds into existing D-023 wording, not a new D-XXX.** | open |
| C-AI-004-3 | AI-SPEC-004 §10.3 C3 | Spec §4 lines 521–525 mandates a system-wide architectural rule: "Items 1, 5, 6 read item_promotion_records at startup. If no current record exists for an item/scope: default to advisory or disabled, NEVER production." This becomes a binding contract for any item subject to per-item authority promotion, but no D-XXX currently codifies the rule. AI-SPEC-001 P1.3.1 `C-AI-001-1` already proposed D-023 for the AI authority boundary; this audit confirms the same D-023 must additionally encode the startup-read contract. | T-Rule 4 + pending D-023 (per AI-SPEC-001 §11 / AI-SPEC-002 §11 addendum) | Operator decision: extend the pending D-023 wording to include a clause: "any item subject to AI authority promotion (Items 1, 5, 6, and any future per-item-promotion-gated specs) MUST read its current authority level from `item_promotion_records` at startup; absence of a current row defaults to `advisory` (for items already paper-binding-eligible) or `disabled` (for items not yet paper-binding-eligible); items MUST NOT invent their own promotion status." **Folds into existing pending D-023 wording, not a new D-XXX.** | open |
| C-AI-004-4 | AI-SPEC-004 §10.3 C4 | Spec §1 lines 47–66 ("Hard Rule: No Theoretical Pricing — No archived chain = no calibration-grade option pricing replay") establishes a hard architectural prerequisite: archived option-chain data. At HEAD, no chain archive exists — `tradier_feed.py` does not persist chains, no `tradier_chain_archive` migration in 68 migrations, `databento:opra:*` keys are 300s-TTL Redis. Forward archival is months-long; paid-tier historical-chain access is a contract dependency. Spec §10 lines 985–989 acknowledges the failure mode (V0.1 advisory-only) but does not require operator to formally accept it at audit close. **Load-bearing for V0.1 calendar.** | T-Rule 1 (Foundation Isolation) — operator owns the data-substrate decision; T-Rule 9 (Paper Phase Is Mandatory) — D-013 90-day A/B test still runs regardless of Item 4 status | Operator decision required between three options: (option 1) **forward archival (months-long)** — start chain-archival now; Item 4 V0.1 ships with scaffolding + Step 0 validation but produces no calibration-grade results until archive is sufficient (estimated 6+ months for 90 sessions per Spec §6 sample-size tiers). All Items 1, 5, 6 default to advisory until archive matures. (option 2) **paid historical archive** — operator negotiates paid Tradier or Databento historical chain access; Item 4 V0.1 can run calibration-grade replay over recent historical data (e.g., 2024–2025). Cost: TBD; contractual dependency. (option 3) **explicit V0.1 advisory-only acceptance** — operator formally accepts Spec §10 Failure Mode; V0.1 ships per spec scaffolding (4 tables + 8 files + Step 0 validation logic + walk-forward implementation) but produces no calibration-grade results. Items 1, 5, 6 stay advisory; production-binding deferred to V0.2 calendar. **Default position (per consolidated plan §5 conservative-first principle):** option 3 with parallel-track exploration of options 1 and 2; operator revisits the V0.1-vs-V0.2 promotion line at end-of-V0.1. **No new D-XXX strictly required for this option** — explicit operator acceptance logged in `system-state.md` `replay_harness.calibration_grade_capable = false`. | open |
| C-AI-010-1 | AI-SPEC-010 §10.3 C1 | Spec §0 line 14 ("Do not rewrite the current `counterfactual_engine.py`") + Spec §3 lines 174–186 ("Replace Default-to-iron_condor Behavior") — internal contradiction. Spec simultaneously forbids rewriting Layer 1 AND requires changing Layer 1's `_simulate_pnl()` default-strategy fallback. The change is small (return NULL when `strategy_hint` missing) but the architectural commitment is explicit. | T-Rule 2 (Governance Documents Are Authoritative) — spec consistency; T-Rule 4 (Locked Decisions Are Final) — once spec is integrated, the cutover policy must be unambiguous | Operator decision required between two options: (option 1) **Treat the Spec §3 change as a "small surgical bug fix" not a rewrite** — Layer 1 is modified in-place via a tiny PR (replace `counterfactual_engine.py:213–223` hardcoded `iron_condor` fallback with NULL-return path when `strategy_hint` missing); existing tests updated to cover NULL path. Spec §0 "do not rewrite" wording is preserved as architectural commitment against full rewrite, not against bug fixes. Lowest-cost. (option 2) **Freeze Layer 1 entirely; the Spec §3 fix lives ONLY in Layer 2's new engine** — Layer 1 continues hardcoded `iron_condor` fallback indefinitely (with operator-known contamination risk for Layer 1 outputs); Layer 2 ships the corrected logic; downstream consumers MUST consume Layer 2 outputs only. Higher-cost (Layer 2 must ship before any consumer trains on counterfactual data). **Default position (per consolidated plan §5 conservative-first principle):** option 1 (surgical bug fix to Layer 1 with documentation update). Folds into Spec §0 wording amendment in Phase 2. | open |
| C-AI-010-2 | AI-SPEC-010 §10.3 C2 | Spec §0 line 5 (Tier "V0.2 immediate / V0.2 strategy-specific / V0.3 adversarial / V0.4 exit") vs `CROSS_CUTTING_EVIDENCE_MATRIX.md` §1 line 52 (Tier "V0.1") — cluster / tier classification discrepancy. Matrix marks V0.1; spec marks V0.2-V0.4 graduated. Affects Cluster A audit completeness assessment (does P1.3.4 close Cluster A if Item 10 is partially V0.1 already-shipped?). | T-Rule 2 — governance documents must align; matrix is downstream of spec but should be amendable when spec lock formalizes the tier | Operator decision: ratify the layered framing — **V0.1 = Layer 1 (already shipped at commit `2400e98`); V0.2 onward = Layer 2 (proposed in this spec).** Cluster A is correct because Items 5 / 6 / 9 (Cluster B) cannot promote to binding without Item 10 V0.2 outputs; Cluster A audit is therefore complete after P1.3.4 with the understanding that "Cluster A complete" means "all four Cluster A specs audited" — not "all V0.1 work done" (Layer 2 V0.2 ships in Phase 4 alongside Cluster B). Matrix §1 line 52 amended in Phase 2 to show graduated tier. **No new D-XXX required.** | open |
| C-AI-010-3 | AI-SPEC-010 §10.3 C3 | Spec §5 lines 363–376 (slippage formula's `vix_z` / `spread_z` z-score producer authority). Slippage formula references `vix_z` / `spread_z` without defining producer authority. No `vix_z` / `spread_z` Redis key at HEAD; closest existing surfaces: `polygon:vix:current` + `tradier_feed.py` quote logic. Z-score calculation is bilateral with AI-SPEC-001 (Card features include vix_z) and AI-SPEC-005 (Vol Fair-Value Engine). | T-Rule 2 — governance ownership of cross-spec data producers must be unambiguous; cross-spec coordination with D-023 | Operator decision: assign z-score producer authority to one of three options: (option 1) **AI-SPEC-001 owns z-score producers** as part of Card feature engineering; AI-SPEC-005 + AI-SPEC-010 + Item 7 read from Item 1's namespace. Concentrates Card feature ownership. (option 2) **AI-SPEC-005 owns z-score producers** as part of Vol Fair-Value Engine's volatility surface; Items 1, 7, 10 read. Concentrates volatility ownership. (option 3) **Shared producer with explicit Redis namespace** (e.g., `features:vix_z`, `features:spread_z`) owned by the calibration_engine per D-019; consumers read via stable interface. Most decoupled. **Default position (per consolidated plan §5 conservative-first principle):** option 3 (shared producer in stable namespace). Resolves bilateral concerns; encoded in D-023 wording alongside other cross-spec data producer ownership. **Folds into existing pending D-023 wording, not a new D-XXX.** | open |
| C-AI-010-4 | AI-SPEC-010 §10.3 C4 | Spec §4 line 202 (`case_type` enum includes `synthetic`) + Spec §10 lines 502–522 (synthetic case generation) vs AI-SPEC-003 Synthetic Counterfactual (Cluster C, V0.4). Item 10 owns the table `trade_counterfactual_cases` that stores synthetic cases (per Spec §4 line 202: `case_type TEXT -- actual | counterfactual | synthetic`). But Item 3 (AI-SPEC-003) is the canonical owner of synthetic case **generation**. Spec §10 mentions synthetic generation procedure but defers to Item 6 / Item 3 ownership. **Unresolved:** does Item 3 INSERT into Item 10's table, or does Item 3 own a separate `synthetic_counterfactual_cases` table? | T-Rule 2 — cross-spec architectural ownership must be unambiguous; consolidated plan §5 conservative-first principle (re-use existing surfaces) | Operator decision between two options: (option 1) **Item 3 generates synthetic cases that INSERT into Item 10's `trade_counterfactual_cases` table with `case_type = 'synthetic'`, `case_weight = 0.2`** — Item 3 is downstream consumer-of-storage, not separate-table-owner. Item 3 retains ownership of the generation algorithm + provenance flags. Single canonical table for all case types; Items 5-9 read uniform data. Lowest-cost. (option 2) **Item 3 owns separate `synthetic_counterfactual_cases` table** mirroring `trade_counterfactual_cases` schema; Items 5-9 read via UNION across both. Cleaner separation of provenance but duplicated infrastructure. **Default position (per consolidated plan §5 conservative-first principle):** option 1 (Item 3 INSERTs into Item 10's table). Encoded in Item 3 P1.3.X audit (Cluster C — future). **No new D-XXX required.** | open |
| C-AI-010-5 | AI-SPEC-010 §10.3 C5 | Spec §6 lines 423–432 (Old Rows Lockdown — `<Commit_4_deploy_date>` cutover anchor). Lockdown migration uses `<Commit_4_deploy_date>` as cutover for legacy-observability lockdown of pre-Layer-2 rows. "Commit 4" refers to the IC/IB target_credit fix sequence's 4th commit (re-mark + NULL slippage cleanup) — **not yet shipped at HEAD** (planned for Tuesday 2026-04-28 per pre-AI track addendum A1). Anchor is a moving target until Commit 4 ships. | T-Rule 4 (Locked Decisions Are Final) — once spec is integrated, the cutover anchor must be unambiguous; T-Rule 5 (Capital Preservation) — contaminated calibration data must be reliably excluded | Operator decision required: lock the cutover anchor to one of three options: (option 1) **Concrete commit hash** — once Commit 4 of the IC/IB sequence ships, replace `<Commit_4_deploy_date>` with the actual deploy timestamp. Layer 1 backfill runs on that date. Most explicit. (option 2) **Date-of-Layer-2-deployment anchor** — cutover = Layer 2 first-deploy date; Layer 1 rows before that timestamp marked legacy_observability_only regardless of Commit 4 status. Decouples Item 10 V0.2 from IC/IB sequence. (option 3) **Per-row classifier** — instead of a date cutover, evaluate each Layer 1 row at backfill time: if `strategy_hint` missing OR `simulation_status` missing OR Commit-4-touched-the-trade-period, mark legacy_observability_only. Most rigorous; highest-cost. **Default position (per consolidated plan §5 conservative-first principle):** option 1 (concrete commit hash post-Commit-4 ship). Lowest-cost; preserves operator-explicit governance. **Folds into existing D-023 wording (Layer 1/Layer 2 cutover policy) — no new D-XXX strictly required.** | open |
| C-AI-005-1 | AI-SPEC-005 §10.3 C1 | Spec entirely silent on D-016 (`grep -n "D-016\|0\.70\|max.*realized.*implied" ITEM_5_*.md` returns zero matches). Spec proposes HAR-RV remainder model that produces `rv_forecast_final` (Spec §1 line 97) — the natural successor to the `realized` term in D-016's `sigma = max(realized, 0.70 × implied)` formula. **Spec must take a position on D-016 before integration.** | T-Rule 4 (Locked Decisions Are Final) — D-016 cannot be modified, deferred, or reinterpreted without explicit owner approval and a new decision record. D-016's constraint says it "Must be implemented in `prediction_engine.py`" but no `sigma_effective` formula exists in code at HEAD (`what-is-actually-built.md` line 14 records D-016 as "Partially built"). | Operator decision required between three options: (option 1) **HAR-RV REPLACES `realized` in D-016** — `sigma = max(rv_forecast_final, 0.70 × implied)`. Requires D-016 amendment with new D-XXX (e.g., D-024) wording or D-016 line 91 revision. **Highest-impact**: HAR-RV becomes the primary realized-vol input system-wide; existing `polygon:spx:realized_vol_20d` consumers (`prediction_engine.py:528, 706`, `main.py:2061`) are switched. (option 2) **HAR-RV is a NEW signal feeding EV table only; D-016 unchanged.** HAR-RV `rv_forecast_final` flows into Item 5's strategy_ev_table for short-vol/long-vol ranking; D-016's `realized` term continues to read `polygon:spx:realized_vol_20d` from the legacy 12A writer. **Lowest-impact**: D-016 stays as-is; HAR-RV runs alongside as advisory-only. (option 3) **HAR-RV outputs `rv_forecast_final` AND D-016 amends `realized` to read from HAR-RV after V0.2 graduation.** Two-phase: V0.2 ships HAR-RV alongside (option 2 phase); after promotion gate (Spec §7 lines 569–583 Baseline E beats Baseline F) passes, D-016 amendment switches `realized` to HAR-RV. **Tightest sequencing**: requires D-016 amendment migration tied to Item 5 V0.2 promotion event. **Default position (per consolidated plan §5 conservative-first principle):** option 2 (HAR-RV as new signal; D-016 unchanged). Lowest-cost; preserves D-016 explicit governance; defers any D-016 amendment to a future spec or upgrade decision. **Folds into D-023 wording (D-023 enrichment item (l))** — Item 5's authority is over `vol:*` namespace + `vol_fair_value_snapshots` table; Item 5 does NOT have authority to amend D-016. **No new D-XXX strictly required for default option 2.** | open |
| C-AI-005-2 | AI-SPEC-005 §10.3 C2 | Spec §1 + §11 — Layer 1 (existing 12A `polygon:spx:realized_vol_20d` writer at `polygon_feed.py:486`) cutover policy unspecified. If Item 5 follows Cluster A theme #7 Layer 1 / Layer 2 retrofit pattern (introduced by AI-SPEC-010), the spec must specify what happens to `polygon:spx:realized_vol_20d` after Layer 2 ships: does it stay as legacy observability with `label_quality = legacy_observability_only`? Does Layer 2 produce a NEW Redis key (e.g., `vol:rv_forecast_remaining`) and Layer 1 stays put? Does Layer 1 get deprecated? **Spec is silent.** | T-Rule 2 (Governance Documents Are Authoritative) — cutover policies must be unambiguous. T-Rule 4 — once spec is integrated, the cutover policy must be locked. | Operator decision required between three options: (option 1) **Layer 1 stays put as backward-compat surface; Layer 2 produces NEW `vol:*` keys; existing consumers (`prediction_engine.py:528, 706`, `main.py:2061`) continue reading `polygon:spx:realized_vol_20d` indefinitely.** Lowest-cost; preserves backward compat; introduces dual-source surface. **Default position.** (option 2) **Layer 1 stays as `legacy_observability_only`; Layer 2 outputs become canonical; existing consumers migrate to Layer 2 outputs in V0.2 ship scope.** Migrates `prediction_engine.py:528, 706` (IV/RV filter) + `main.py:2061` (learning-stats endpoint) to read from `vol:rv_forecast_remaining` instead of `polygon:spx:realized_vol_20d`. (option 3) **Layer 1 deprecated entirely; Layer 2 producer replaces 12A writer in `polygon_feed.py`.** Most aggressive; risks breaking the IV/RV filter at `prediction_engine.py:706` if Layer 2 doesn't produce the same key shape. **Default position (per consolidated plan §5 conservative-first principle):** option 1 (Layer 1 stays put; Layer 2 ships parallel keys). **Folds into D-023 wording (Layer 1/Layer 2 cutover policy enrichment per AI-SPEC-010 P1.3.4 item (g) — extended to Item 5 as item (m)).** No new D-XXX strictly required. | open |
| C-AI-005-3 | AI-SPEC-005 §10.3 C3 | Spec §7 + §11 — V0.x advisory-only acceptance bilateral with `C-AI-004-4` (chain archive) and `B-AI-005-15` (multi-year RV history). Item 5's V0.2 promotion gate "Baseline E must beat Baseline F" (Spec §7 lines 569–583) requires archived chains for IV extraction during replay calibration. **Bilateral with `C-AI-004-4`:** if operator chooses option 3 (V0.1 advisory-only ship for Item 4), Item 5 V0.2 promotion is calendar-blocked. Spec §7 lines 633–639 Fallback Level 3 explicitly anticipates "logging only, no production authority" — but operator must formally accept this state for Item 5 V0.2 ship. | T-Rule 1 (Foundation Isolation) — operator owns the data-substrate decision; T-Rule 9 (Paper Phase Is Mandatory) — D-013 90-day A/B test still runs regardless of Item 5 status | Operator decision required between three options (mirrors `C-AI-004-4` resolution): (option 1) **forward archival (months-long)** — start chain + 5-min bar archival now; Item 5 V0.2 ships scaffolding-only until archive sufficient (estimated 6+ months for 90 sessions per Spec §11 lines 720–728). All Items 1, 6, 9 default to advisory-only on Item 5 outputs until archive matures. (option 2) **paid historical archive** — Polygon historical-bars + Tradier (or Databento) historical chains paid tier; Item 5 V0.2 can run calibration-grade replay over recent historical data. Cost: TBD (operator); contractual dependency. (option 3) **explicit V0.x advisory-only acceptance** — operator formally accepts Spec §7 Fallback Level 3; Item 5 V0.2 ships per spec scaffolding (engine module + EV table + reliability score state machine + freshness substrate + Redis namespace + Supabase table) but produces no calibration-grade results. Items 1, 6, 9 stay advisory on Item 5 outputs; production-binding deferred to V0.3+ calendar. **Default position (per consolidated plan §5 conservative-first principle):** option 3 with parallel-track exploration of options 1 and 2; operator revisits the advisory-only-vs-calibration-grade promotion line at end-of-V0.2. **Bilateral with `C-AI-004-4`** — same operator decision resolves both. **No new D-XXX strictly required for option 3** — explicit operator acceptance logged in `system-state.md` `vol_fair_value_engine.calibration_grade_capable = false`. | open |
| C-AI-006-1 | AI-SPEC-006 §10.3 C1 | Spec §9.4 line 622 specifies AUTOMATIC authority recovery (advisory → reduced → normal) via `30-session cooldown + 20 qualified signals` — verbatim "Mirror Item 5: minimum 30-session cooldown, then one-step recovery (advisory → reduced → normal) after demonstrated recalibration over 20 qualified signals." Per architectural principle 1 ("AI controls admissibility, not authority promotion") + T-Rule 4 (Locked Decisions Are Final), authority RECOVERY (increasing AI authority) should require explicit operator action. **Bilateral with AI-SPEC-005 §5 reliability state machine** which has the SAME automatic-recovery pattern (Item 6 spec line 622 explicitly references "Mirror Item 5"). **CRITICAL Class C escalation per Guard 1 — first authority-boundary-violation Class C in Cluster B.** | T-Rule 4 (Locked Decisions Are Final); architectural principle 1 ("AI controls admissibility and size ceilings, not trade execution"); paper-binding-ladder constitutional discipline | **Operator decision required** between three options: (option 1) **automatic recovery accepted** — operator approves Spec §9.4 line 622 wording as-is for both Item 5 and Item 6; D-023 enrichment encodes "automatic per-channel authority recovery is permitted within bounds set by `item_promotion_records` row-level operator-locked phase enum"; degradation triggers + cooldown + qualified-signals threshold are sufficient guardrails. (option 2) **operator-mediated recovery only** — Spec §9.4 line 622 amended to require explicit operator approval for any one-step authority recovery; cooldown + qualified-signals threshold becomes ELIGIBILITY gate (operator may approve recovery after 30+20) but not a TRIGGER; bilateral amendment to AI-SPEC-005 §5 reliability state machine. (option 3) **hybrid** — degradation is automatic (authority reduction always allowed); recovery requires operator-mediated approval through `item_promotion_records` change-control with 30+20 as ELIGIBILITY gate. **Default position (per consolidated plan §5 conservative-first principle):** option 2 (operator-mediated recovery only); D-023 enrichment item (p). **Bilateral resolution mandatory** with AI-SPEC-005 §5 to avoid cross-spec inconsistency. | open |
| C-AI-006-2 | AI-SPEC-006 §10.3 C2 | Spec §13 V0.2 Build Scope (lines 744–764) silent on `bf41175` Layer 1 cutover policy. Locked Item 6 spec V0.2 Build Scope does NOT mention the existing Layer 1 LightGBM meta-label scaffold (verified 2026-04-27 — `grep -nE "bf41175\|Loop 2\|meta-label scaffold\|meta_label_v1" ITEM_6_META_LABELER_LOCKED.md` returns 0 matches). Layer 1 is dormant pass-through (`backend/models/meta_label_v1.pkl` absent — `ls -la backend/models/` returns only `.gitkeep`) but `train_meta_label_model()` at `backend/model_retraining.py:722` and inference block at `backend/execution_engine.py:328–399` are LIVE code at HEAD — including the `model:meta_label:enabled` Redis flag and 9 meta-label tests at `backend/tests/test_meta_label_model.py`. **Cutover question:** does Layer 1 stay as legacy observability, get deprecated entirely, or run as champion-challenger to Layer 2 (`run_meta_label_champion_challenger` at `backend/model_retraining.py:984+` already exists)? | T-Rule 2 (Governance Documents Are Authoritative) — cutover policies must be unambiguous; T-Rule 4 — once integrated, cutover policy must be operator-locked | **Operator decision required** between three options: (option 1) **Layer 1 stays put as legacy observability** (per AI-SPEC-005 `C-AI-005-2` + AI-SPEC-010 `C-AI-010-1` precedent) — Layer 1 inference block remains pass-through; if Layer 1 is ever activated (operator places pkl), `label_quality = 'legacy_observability_only'` is flagged on any persisted Layer 1 outputs; Layer 2 ships fresh with canonical Bayesian three-part hybrid; `system-state.md` `meta_labeler.layer_1_phase = 'dormant_legacy_observability'`. (option 2) **Layer 1 deprecated entirely** — `bf41175` scoring block at `backend/execution_engine.py:328–399` removed in Item 6 V0.2 cutover commit; `train_meta_label_model()` at `backend/model_retraining.py:722` retired; `run_meta_label_champion_challenger` at `backend/model_retraining.py:984+` deleted; only Layer 2 ships. (option 3) **Layer 1 + Layer 2 run as champion-challenger** — `run_meta_label_champion_challenger` infrastructure repurposed; Layer 1 is challenger, Layer 2 is champion; comparison metrics (Brier delta, ECE delta, decision-agreement rate) flow into drift-detection per Spec §9. **Default position (per Cluster A precedent of Items 5 + 10):** option 1 (Layer 1 stays put as legacy observability). **Folds into D-023 wording** (Layer 1/Layer 2 cutover policy enrichment per AI-SPEC-010 P1.3.4 item (g) extended by AI-SPEC-005 P1.3.5 item (m) extended by Item 6 P1.3.6 item (q)). | open |
| C-AI-006-3 | AI-SPEC-006 §10.3 C3 | Spec §2 — Bayesian prior parameter governance. Spec §2 hardcodes prior parameters (verified 2026-04-27 — `grep -nE "Bayesian\|prior\|hyperparameter\|alpha=\|beta=\|hardcoded" ITEM_6_META_LABELER_LOCKED.md`): `alpha=1, beta=1` (line 188), `k=20` (line 198), `w_prior` values 0.55 / 0.40 / 0.25 (lines 168/173/178). **Spec contains zero references to `system-state.md`, `approved-decisions.md`, or operator-tunable language.** Prior parameters cannot be retuned without a code change — they are not exposed as governance-tracked constants. | T-Rule 2 (Governance Documents Are Authoritative) — operator-tunable parameters should be governance-tracked | **Operator decision required** between three options: (option 1) **prior parameters are operator-tunable via `system-state.md`** — new `system-state.md` `meta_labeler.prior_parameters` field (`{ alpha, beta, k, w_prior_low, w_prior_mid, w_prior_high }`); operator amendment triggers re-initialization at next promotion-gate review; default values match spec. (option 2) **prior parameters are operator-tunable via `approved-decisions.md`** — new D-XXX (e.g., D-024) ratifies the prior parameter values as constitutional constants; modification requires D-XXX amendment with operator review. (option 3) **prior parameters are code-bound constants** until first promotion-gate review (`n_train >= 300`); modifiable only via PR review with operator sign-off. **Default position (per consolidated plan §5 conservative-first principle):** option 1 (operator-tunable via `system-state.md`); first promotion-gate review revisits the question. **No new D-XXX strictly required if default option 1 chosen** — folds into D-023 enrichment item (s). | open |
| C-AI-008-1 | AI-SPEC-008 §10.3 C1 | Cross-spec producer→consumer contract gap. AI-SPEC-008 §5 lines 270–281 unilaterally claims an 8-feature contract with Item 5 (Vol Fair-Value Engine — `vol_demand_pressure_5m`, `zero_dte_vs_1_3dte_ratio`, `sweep_premium_share_1m`, `aggressive_put_call_imbalance_5m`, `short_delta_pressure_5m`, `atm_delta_pressure_5m`, `dealer_hedge_pressure_z_5m`, `flow_price_divergence_5m`) AND a 20-feature contract with Item 6 (Meta-Labeler — all 20 production features + quality flags per Spec §5 line 290). Verified 2026-04-28 — `grep -n "OPRA\|opra:flow_alpha\|opra_flow" ITEM_5_VOLATILITY_FAIR_VALUE_ENGINE_LOCKED.md` returns 2 matches (line 390 "OPRA flow confirms expansion" qualitative gate condition; line 549 "Databento OPRA-derived features" listed as one of 5 input categories) — **abstract reference only; no field-name enumeration**. `grep -n "OPRA\|opra:flow_alpha\|opra_flow\|Item 8" ITEM_6_META_LABELER_LOCKED.md` returns ZERO matches — Item 6 spec is fully silent. AI-SPEC-005 + AI-SPEC-006 audit register both omit Item 8 from cross-spec themes (V9). Item 6's 47 named feature fields per `B-AI-006-2` through `B-AI-006-6` come from 5 producer surfaces (Items 5 + Rules + LightGBM + Governor + Memory) — **Item 8 NOT named as 6th producer surface**. Mirrors `C-AI-002-1` writer/consumer-coordination pattern. | T-Rule 2 (Governance Documents Are Authoritative) — cross-spec producer→consumer contracts must be authoritative on both sides; T-Rule 4 — once specs are integrated, contracts must be locked | Operator decision required between three options: (option 1) **ratify** Item 8 §5 contracts via amendment to Item 5 §3/§4 + Item 6 §3 specs in next P1.3 amendment cycle — Item 5 §3 adds new producer-surface row enumerating the 8 OPRA features; Item 6 §3 adds 6th producer-surface section enumerating all 20 OPRA features + 1 quality flag (per `A-AI-008-2` corrected count); cleanest; explicit producer-surface enumeration; preserves cross-spec coherence. (option 2) **revoke** Item 8 §5 contract claims; treat OPRA features as research-only inputs that downstream consumers may opt into post-V0.3 maturation via future spec amendment. Most conservative; defers integration. (option 3 — **DEFAULT**) accept Item 8 §5 as forward-looking unilateral claim with **bilateral confirmation deferred** to next P1.3.6 / P1.3.5 amendment cycle (post-Cluster B closure); preserves audit-trail clarity at cost of delayed cross-spec coherence. **Folds into D-023 enrichment item (w)** — cross-spec contract ratification process governance. | open |
| C-AI-008-2 | AI-SPEC-008 §10.3 C2 | flow_agent migration cutover policy contradiction between two locked sources. Locked spec §8 lines 444–462 explicitly states **coexistence with role-narrowing**: "flow_alpha_engine = deterministic feature producer (this Item 8); flow_agent = optional LLM summarizer (existing, post-fix). … flow_agent's role becomes 'translate the structured flow_alpha_engine output into a narrative summary'". Locked AI_BUILD_ROADMAP.md §3.9 line 372 explicitly states **successor-replacement**: "Successor-replace today's `flow_agent.py` (currently brief generator; Item 8 is alpha extractor)". Build Roadmap §3.9 line 373 acknowledges this is operator-pending pending audit. Mirrors `C-AI-005-2` Layer 1/2 cutover and `C-AI-006-2` `dormant_legacy_observability` patterns. | T-Rule 2 (Governance Documents Are Authoritative) — cutover policies must be unambiguous across locked sources; T-Rule 4 — once spec is integrated, cutover policy must be locked | Operator decision required between three options: (option 1 — **DEFAULT RECOMMENDED**) coexistence with role-narrowing per locked spec §8 explicit text; flow_agent narrows to LLM summarizer reading from `flow_alpha_engine` output; existing `ai:flow:brief` Redis key continues but content shifts from Unusual Whales / put-call ratio brief to narrative summary over Item 8's 5-field Governor compact summary; preserves backward-compat with `synthesis_agent.py:93` + `main.py:1917` `ai:flow:brief` consumer wiring. (option 2) full replacement per Build Roadmap §3.9 line 372: `flow_alpha_engine` subsumes flow_agent; `agents:flow_agent:enabled` feature flag deprecated; `ai:flow:brief` Redis key replaced by `opra:flow_alpha:*` namespace; `synthesis_agent.py:93` + `main.py:1917` consumer wiring migrated; most aggressive; risks breaking synthesis_agent input contract during cutover. (option 3) hybrid: new file `backend/flow_alpha_engine.py` (mirrors AI-SPEC-005 `vol_fair_value_engine.py` per `B-AI-005-1` and AI-SPEC-006 `meta_labeler_engine.py` per `B-AI-006-1` canonical-name pattern) ships alongside narrowed `flow_agent.py` (per option 1 narrowing); preserves separation of concerns. **Folds into D-023 enrichment item (v)** — flow_agent cutover policy for Item 8; extends Layer 1/Layer 2 cutover framework from items g + l + q to cover Layer 0 raw-feed substrate + Layer 2 alpha extractor + flow_agent narrowing. No new D-XXX strictly required for option 1 or 3. | open |
| C-AI-008-3 | AI-SPEC-008 §10.3 C3 | Databento subscription scope. Spec §1 lines 33–46 Lee-Ready aggressor inference assumes NBBO ticks at sub-second cadence (Spec line 48: `NBBO_age > 1 second → quote_stale = true`); Spec §1 lines 114–126 sweep cluster detection assumes same-millisecond cluster reconstruction across exchanges; Spec §1 lines 128–133 block trade detection assumes 95th-percentile rolling distributions. **If current Databento subscription tier lacks any of these, that's a procurement decision distinct from D-008 budget governance.** Verified at HEAD via `wc -l backend/databento_feed.py = 452`; subscription detail not visible via grep alone — operator pre-implementation verification required. | T-Rule 1 (Foundation Isolation) — operator owns substrate procurement decisions; D-008 governs budget envelope (~$150–200/month) but not specific subscription tier scope; new D-XXX (D-025 candidate) may be required if subscription scope expansion fits within D-008 budget OR if budget needs revision | Operator decision required between four options: (option 1) Databento subscription tier upgraded within D-008 budget — confirm sub-second NBBO + sweep tape availability at current pricing; no new D-XXX needed. (option 2) Databento subscription tier upgrade requires D-008 budget revision (new D-XXX D-025 candidate raising the cap from $150–200/mo to whatever the new tier costs); highest-impact: changes governance D-008. (option 3) Item 8 V0.3 spec amendment to relax Lee-Ready inference + sweep detection requirements to fit current Databento subscription tier; spec amendment to §1 — fall back to mid-rule aggressor sign without prior-tick lookup AND coarser sweep cluster definition. (option 4 — **DEFAULT**) operator verifies Databento subscription tier scope against Spec §1 requirements **before V0.3 implementation begins**; if subscription tier sufficient, proceed with no governance change; if subscription tier insufficient, operator decides between options 2 and 3 at V0.3 implementation start. **Pre-implementation operator gate** — V0.3 implementation cannot begin until this verification completes. **Folds into D-023 enrichment item (u)** — Item 8 authority boundary (spec authority over Databento subscription scope is operator-only; Item 8 specifies what it needs but does not procure). | open |
| C-AI-008-4 | AI-SPEC-008 §10.3 C4 | V0.x advisory-only acceptance bilateral with chain-archive. Item 8's Spec §6 lines 322–407 production-acceptance gate (`Item 6 with OPRA features must beat Item 6 without OPRA features`) requires Item 6 V0.2 ship + chain-archive substrate sufficiency for replay-time `Item 5 EV row, Item 6 label` join (Spec §6 line 328). **Bilateral with `C-AI-004-4`** chain-archive operator decision and `B-AI-006-1` Item 6 V0.2 Layer 2 engine module. Spec §6 lines 395–407 implicitly anticipate "logging only" fallback but operator must formally accept this state for Item 8 V0.3 ship. Mirrors `C-AI-005-3` advisory-only acceptance for Item 5. | T-Rule 1 (Foundation Isolation) — operator owns the data-substrate decision; T-Rule 9 (Paper Phase Is Mandatory) — D-013 90-day A/B test still runs regardless of Item 8 status | Operator decision required between three options (mirrors `C-AI-004-4` + `C-AI-005-3` resolution): (option 1) forward archival (months-long) — start OPRA flow archive now via `opra_flow_feature_bars` table population; Item 8 V0.3 ships scaffolding-only until 90+ replay sessions accumulate (estimated 6+ months per Spec §6 line 397). (option 2) paid historical archive — Databento OPRA historical-bars subscription paid tier; Item 8 V0.3 can run calibration-grade replay over recent historical data; bilateral with `C-AI-008-3` subscription scope. (option 3 — **DEFAULT**) explicit V0.3 advisory-only acceptance — operator formally accepts Spec §6 implicit "logging only" fallback; Item 8 V0.3 ships per spec scaffolding (engine module + 20 features + storage architecture + freshness substrate + Redis namespace + Supabase tables) but produces no calibration-grade results; Items 5, 6, Governor stay advisory on Item 8 outputs; production-binding deferred to V0.4+ calendar contingent on Item 6 V0.2 ship + chain-archive maturation per `C-AI-004-4`. **Bilateral with `C-AI-004-4`** — same operator decision resolves both. **No new D-XXX strictly required for option 3** — explicit operator acceptance logged in `system-state.md` `opra_flow_alpha.calibration_grade_capable = false`. | open |
| C-AI-006-4 | AI-SPEC-006 §10.3 C4 | Spec §3.4 lines 293–305 — LightGBM Direction Model spec ownership. Spec §3.4 reads 5 fields (`p_bull`, `p_bear`, `direction_confidence`, `direction_edge`, `strategy_direction_alignment`) from a "LightGBM Direction Model" producer. **The producer has NO spec in the AI architecture catalog** — verified by reviewing the consolidated plan §4 (13 AI architecture specs); no standalone LightGBM Direction Model spec exists. The closest existing logic at HEAD: `backend/polygon_feed.py` LightGBM features computed from SPX 5-min bars + `backend/model_retraining.py` LightGBM training infrastructure for meta-label scaffold — but **no LightGBM direction model exists**. Bilateral with `B-AI-006-4` (LightGBM Direction Model 5 fields absent at HEAD). | T-Rule 2 (Governance Documents Are Authoritative); MASTER_PLAN architecture catalog completeness | **Operator decision required** between three options: (option 1) **add LightGBM Direction Model as Item 6 V0.2 ship sub-scope** — Item 6 V0.2 builds the direction model in the same engine module (`backend/meta_labeler_engine.py`) or as a new module (`backend/lightgbm_direction_model.py`); spec is amended to clarify direction model is owned by Item 6. (option 2) **add new spec AI-SPEC-014 LightGBM Direction Model** to the AI architecture catalog — operator approves catalog extension; new audit P1.3.14 added to consolidated plan; Item 6 V0.2 ship calendar-blocked on AI-SPEC-014 V0.x ship. (option 3) **remove the 5 LightGBM Direction Model fields from Item 6 §3.4** — Item 6 V0.2 ships without direction-tilt features; spec amendment required; Item 6 falls back to using only the 18 + 5 + 9 + 10 = 42 fields. **Default position (per consolidated plan §5 conservative-first principle):** option 2 (new spec AI-SPEC-014); minimizes Item 6 V0.2 scope creep but adds Cluster B audit cycle. **Possible new D-XXX (e.g., D-024) for AI architecture catalog extension** — operator decision pending; bilateral with `C-AI-006-3` D-XXX numbering choice. | open |

---

## 2. Open Class B Corrections (Consolidated Operator Approval)

Class B corrections change implementation meaning but not architectural goal. Per `CONSOLIDATED_PLAN_v1.2_APPROVED.md` §3 + template `_template.md` §10.2, GPT or Claude proposes; Cursor verifies; **operator approves a consolidated list, not per-correction**. B1–B8 below are part of one consolidated approval set scoped to AI-SPEC-001.

| ID | From | Spec Section | Spec Says | Reality | Proposed Correction | Status |
|----|------|--------------|-----------|---------|---------------------|--------|
| B-AI-001-1 | AI-SPEC-001 §10.2 B1 | Spec §1 (Final Decision Stack — "Supabase pgvector case retrieval") + Spec §16 (Retrieval Quality Score) | Implies `pgvector` extension is enabled and a populated case-store with embeddings exists. | Zero `pgvector` references across all 68 migrations at HEAD; no embedding infrastructure in `backend/`. | Add Class B note to Spec §1 + §16: pgvector enablement + case-store schema with embedding columns is a **pre-V0.1 implementation prerequisite**, not an existing capability. Spec §28 V0.1 Ship Scope item 1 is the build target — clarify pgvector is part of "Item 1 V0.1 schema" specifically, not assumed-present. | open |
| B-AI-001-2 | AI-SPEC-001 §10.2 B2 | Spec §1 + Spec §24 ("refactored synthesis_agent") | Implies an in-place refactor of an existing module with similar surface area. | `backend_agents/synthesis_agent.py` (628 lines) produces a 5-field JSON `{direction, confidence, strategy, rationale, risk_level}`; Governor schema in Spec §3 has 18+ fields including event taxonomy, novelty/uncertainty/conflict scores, allowed/blocked strategy-class enums, opportunity-lean fields, freshness flags, retrieval case IDs. The schema delta is too large for "refactor". | Spec wording is **observability of architectural intent only** — implementation will be a full rewrite using `synthesis_agent.py`'s structured-output / provider-pluggable scaffolding as starting point. Set correct implementer expectation. | open |
| B-AI-001-3 | AI-SPEC-001 §10.2 B3 | Spec §14 (Versioning and Rollback — `ai_governor_versions` table DDL) | Provides full DDL as if the table is part of the spec deliverable. | Migration not present at HEAD; zero matches for `ai_governor_versions` in `supabase/`. | No Class B *correction* to spec wording; tag as "scheduled buildout" so the audit trail shows the migration is part of P-Phase 4+ implementation, not assumed-present. | open |
| B-AI-001-4 | AI-SPEC-001 §10.2 B4 | Spec §3.B (`GovernorDecisionRecord`) | Implies a `governor_decisions` (or equivalent) persistence target with the documented row shape exists. | No table at HEAD. | Spec defines the row shape; persistence target name should be set in TASK_REGISTER §14A.2. **Recommended canonical name:** `ai_governor_decisions` (matches the `ai_governor_versions` namespace). | open |
| B-AI-001-5 | AI-SPEC-001 §10.2 B5 | Spec §16 (`schema_quality_factor` formula referencing `calibration_eligible = true`) | Implies `calibration_eligible` is an attribute on retrievable cases at query time. | Zero matches for `calibration_eligible` in `backend/` and `supabase/migrations/` at HEAD; the label only lives in archived locked spec + audit infra docs. | Cross-spec sequencing: Item 1 cannot enforce `schema_quality_factor = 0.00 for contaminated rows` until Item 10 (Counterfactual P&L) extends `trading_prediction_outputs` (or its successor case store) with `calibration_eligible`. Add Class B note to Spec §16: depends on AI-SPEC-010 deliverable. | open |
| B-AI-001-6 | AI-SPEC-001 §10.2 B6 | Spec §3 LLM Output (`data_freshness_warning`, `data_freshness_flags`) + Spec §11 ("Data freshness fail") + Spec §16 ("data freshness penalty") | Treats data freshness as a working signal the Governor reads. | Carry-forward #4: `_safe_redis()` at `prediction_engine.py:100` is dead code (no callers). Carry-forward #1: `gex:updated_at` has no producer. Freshness substrate is **non-functional** at HEAD. | Spec §28 V0.1 Ship Scope must include "freshness substrate buildout" as a sub-item — wire a real caller of `_safe_redis()` (or replacement) AND stand up producers for `gex:updated_at` + any other staleness keys the Card reads. Alternatively, Spec §24 must explicitly mark `data_freshness_warning` as MVP-deferred. | open |
| B-AI-001-7 | AI-SPEC-001 §10.2 B7 | Spec §10 (Constitutional Gates — "Streak halt", "Phase sizing tiers") | Lists the gates as one-line bullet items. | "Streak halt" is split: 3-loss reduction at `risk_engine.py:234–235`, 5-loss session halt at `prediction_engine.py:688–691`. "Phase sizing tiers" `_RISK_PCT` lives at `risk_engine.py:79–84` and is **non-monotonic** at HEAD (Phase 1 0.010 > Phase 2 0.0075 — evidence pack §9.4) by paired-design with the 2026-04-20 SPX width widening. | Add implementer-facing Class B note: (a) D-022 enforcement is split between `risk_engine.py` and `prediction_engine.py`; the Governor's "Final Arbiter" composition rule must read from BOTH paths. (b) The Governor's `size_multiplier_cap` must NOT widen the non-monotonic regression; this is a hard constraint pending Item 12 / separate ladder fix. | open |
| B-AI-001-8 | AI-SPEC-001 §10.2 B8 | Spec §12 V0.2 Paper-Binding promotion gate ("≥ 200 replay/advisory decision cards") | Implies Item 4 (Replay Harness) is operational by the time the gate is evaluated. | Item 4 is itself V0.1 Cluster A future design (evidence pack §8 Item 4 — NOT YET IMPLEMENTED). | Add Class B note: gate eligibility requires Item 4 to ship first AND accumulate ≥200 cards. Operator should verify Item 4's expected card production rate before committing to Spec §12 thresholds, otherwise V0.2 paper-binding promotion is calendar-blocked on Item 4 throughput. | open |
| B-AI-002-1 | AI-SPEC-002 §10.2 B1 | Spec §6 (EOD Attribution Engine) + §15 V0.1 Ship Scope item 5 (`backend/eod_attribution_engine.py`) | Names a backend file with full job specification (16:35 ET schedule, idempotent, process_date param, three-phase processing). | File does not exist at HEAD. Verified by `test -f backend/eod_attribution_engine.py` returning absent. No `attribution` files anywhere in `backend/`, `backend_agents/`, `backend_earnings/` (`grep -rln "attribution" backend/` returns zero production matches). | No correction to spec wording — tag as "scheduled buildout"; clarify in Spec §15 that Item 2 includes both the engine file AND integration into `backend/main.py` EOD orchestration chain (proposed slot: between `run_matrix_update_job` at 16:30 ET and `run_eod_reconciliation_job` at 16:40 ET, per existing pattern). | open |
| B-AI-002-2 | AI-SPEC-002 §10.2 B2 | Spec §15 V0.1 Ship Scope items 1, 2 (`CREATE TABLE strategy_attribution` + `CREATE TABLE strategy_utility_labels`) + §6 Phase 1 step 2.a ("Fetch path metrics from `closed_trade_path_metrics`") | Lists 3 new-table migrations and assumes `closed_trade_path_metrics` source-of-truth substrate exists. | All 3 listed Item 2 tables absent from 68 migrations at HEAD. **`closed_trade_path_metrics` is also absent from the codebase entirely** (verified by `grep -rln "closed_trade_path_metrics"` returning matches only in archived spec files). Path-metric column names (`mae_R`, `mfe_R`, `peak_unrealized_pnl`, etc.) are not present on any current table. | Add Class B note: V0.1 ship scope must additionally include the `closed_trade_path_metrics` source-of-truth substrate (table + intraday writer integration into `position_monitor.py`) OR explicitly defer all path-metric copy-on-insert columns of `strategy_attribution` to V0.2 with NULL placeholders for V0.1. | open |
| B-AI-002-3 | AI-SPEC-002 §10.2 B3 | Spec §2 lines 44–46 (FK fields `decision_id`, `meta_labeler_decision_id`, `adversarial_decision_id`) | References "future" tables for Items 1, 6, 7 (e.g., `decision_id UUID NULL REFERENCES ai_governor_decisions(decision_id)` plus inline comments "references future Item 6 table" / "references future Item 7 table" without canonical names). | `ai_governor_decisions` not yet shipped (Item 1 V0.1 deliverable, canonical name proposed in `B-AI-001-4`). `meta_labeler_decisions` and `adversarial_decisions` have NO canonical name in spec. | Add Class B note: cross-spec FK dependency cascade — Item 2's `strategy_attribution` migration must follow Items 1, 6, 7 schema landings OR be split into two migrations (initial without these FK constraints, follow-up adding constraints once dependent specs ship). Assign canonical names to Item 6 / Item 7 decision tables in their respective P1.3.6 / P1.3.7 audits. | open |
| B-AI-002-4 | AI-SPEC-002 §10.2 B4 | Spec §4 (`outcome_mode` Per Strategy taxonomy — 8 strategies × ~10 outcome values each + universal blocked enums) + §6 Phase 1 step 2.e ("Classify outcome_mode") | Defines outcome enums (e.g., `forced_close_1430`, `forced_close_halt`, `pricing_slippage_loss`, `theta_decay_capture`) without a mapping function from real `exit_reason` values. | Actual `exit_reason` allowlist at `supabase/migrations/20260421_exit_reason_comprehensive.sql:48–78` has 22 values (e.g., `time_stop_230pm`, `time_stop_230pm_d010`, `time_stop_345pm_d011`, `take_profit_40pct`, `stop_loss_150pct_credit`, `cv_stress_exit_d017`, `emergency_backstop`, `circuit_breaker`, `eod_reconciliation_stale_open`). Spec's `outcome_mode` enum cannot be derived without an explicit mapping. | Add Class B note: Spec §4 must include (or §6 step 2.e must reference) an explicit `(exit_reason, strategy_type, strategy_metrics) → outcome_mode` mapping function. Without it, Item 2's promise of "calibration-grade strategy-specific outcome labels" cannot be operationalized. | open |
| B-AI-002-5 | AI-SPEC-002 §10.2 B5 | Spec §12 Integration With Other Items (lists Item 5 / 6 / 7 / 9 / 10 only) + cross-cutting matrix §4 line 482 ("Item 2 provides per-trade labels that aggregate up to this matrix") | Spec is silent on `backend/strategy_performance_matrix.py` integration disposition. | Existing 229-line file aggregates `(regime, strategy_type)` cells and feeds sizing reducer (`strategy_selector.py:154`). Per matrix §4: "Item 2 provides inputs" — but Item 2 spec does not address whether the matrix should be refactored (consume `strategy_attribution`), coexist (current behavior), or be deprecated. | Add Class B note: Spec §12 must include integration disposition for `strategy_performance_matrix.py`. Recommended default (least disruptive): refactor the per-cell aggregator to read from `strategy_attribution WHERE calibration_eligible = true` once Item 2 ships and ≥30 days of attributed rows exist; keep `trading_positions` direct read as fallback when attribution coverage < 30 days. Cross-spec coordination with AI-SPEC-011 / AI-SPEC-012 (matrix §4 lines 483–484 list both as readers). | open |
| B-AI-002-6 | AI-SPEC-002 §10.2 B6 | Spec §10 Backfill Rules ("Pre-Commit-4 contaminated rows" only) | Specifies cutover policy ONLY for Pre-Commit-4 contaminated rows. | Broader population of legacy non-contaminated `trading_positions` rows (closed positions before Item 2 ships, lacking `strategy_hint` captured at decision time) is unaddressed. Without explicit cutover, Item 6 / Item 9 training queries could either (a) silently skip these rows (coverage gap) or (b) include them with NULL strategy fields (training contamination — high risk per dimension 9). | Add Class B note: Spec §10 must add a "Legacy Cutover Policy" sub-section specifying that all closed `trading_positions` rows with `entry_at < attribution_engine_first_run_date` either (a) get a backfill INSERT into `strategy_attribution` with `simulation_status='legacy_observability_only'` + `calibration_eligible=false` + `strategy_hint=NULL`, OR (b) are explicitly excluded by Item 6 / Item 9 training accessors via a cutover-date filter. | open |
| B-AI-002-7 | AI-SPEC-002 §10.2 B7 | Spec §1 + §2 line 42 (`case_id UUID REFERENCES trade_counterfactual_cases(id)`) + §6 Phase 2 ("counterfactual cases for process_date") | Treats `trade_counterfactual_cases` as Item 10's deliverable. | Item 10's existing scaffold is column-shaped (`counterfactual_pnl`, `counterfactual_strategy`, `counterfactual_simulated_at` on `trading_prediction_outputs` per `20260421_add_counterfactual_pnl.sql:8–14`). Migration comment line 21–24 explicitly states "trading_prediction_outputs does not persist a per-prediction strategy hint" — the existing column-shaped design cannot satisfy Spec §6 Phase 2 step 2.a ("Require strategy_hint and strategy_structure"). PRE-P11-4 carry-forward applies HIGH-IMPACT. | No correction to Item 2's spec wording — escalate to Class C (`C-AI-002-2`). Operator decision required on Item 10's case-ledger architecture. | open |
| B-AI-002-8 | AI-SPEC-002 §10.2 B8 | Spec §2 (Core Identity Fields — `candidate_id UUID NULL`) | Defines a `candidate_id` field with no FK target. | `trading_positions` already has `prediction_id UUID REFERENCES trading_prediction_outputs(id)` (added by `20260421_add_decision_context.sql:9–10`). Spec does not address whether `strategy_attribution.candidate_id` should equal `trading_positions.prediction_id` (canonical pre-decision candidate) or a separate Item 1 / Item 6 candidate ID. | Add Class B note: Spec §2 must clarify the source-of-truth FK target for `candidate_id`. Recommended default: `candidate_id UUID NULL REFERENCES trading_prediction_outputs(id)` — re-uses existing infrastructure and matches the `prediction_id` channel already wired through `decision_context`. | open |
| B-AI-004-1 | AI-SPEC-004 §10.2 B1 | Spec §4 lines 348–498 + §13 V0.1 Ship Scope items 1–4 | 4 new tables (`replay_eval_runs`, `replay_eval_cases`, `replay_eval_results`, `item_promotion_records`) with full DDL | All 4 absent from 68 migrations at HEAD. Verified by `grep -rln "<table>" supabase/migrations/`. | No correction to spec wording — tag as "scheduled buildout"; clarify in Spec §13 that Item 4's V0.1 scope must include the 4 migrations and that `item_promotion_records` should be seeded with default `'advisory' or 'disabled'` rows for Items 1, 5, 6 at first-time deploy (per Spec §4 line 521–525). | open |
| B-AI-004-2 | AI-SPEC-004 §10.2 B2 | Spec §4 lines 532–554 + §13 V0.1 Ship Scope items 5–12 | 8 `backend/replay_*.py` files (`replay_harness.py` orchestrator, `replay_data_reconstructor.py`, `replay_feature_builder.py`, `replay_decision_stack.py`, `replay_threshold_optimizer.py`, `replay_leakage_guard.py`, `replay_validator.py`, `replay_reporter.py`) | None exist. `ls backend/ \| grep -iE 'replay'` returns zero matches. Closest existing surface (`backend/scripts/backtest_gex_zg.py`, 470 lines) uses theoretical pricing approximations and has empty input data dir — explicitly forbidden by Spec §1 line 62 ("Hard Rule: No Theoretical Pricing"). | Add Class B note: V0.1 ship scope includes deprecation-and-replacement of `backend/scripts/backtest_gex_zg.py` (the existing script's approximation approach is incompatible with Spec §1's hard rule). The 8 new files form the canonical replay infrastructure; the legacy script either deletes or moves to `backend/scripts/_legacy/` with a deprecation note. Tests in `test_b2_backtest.py` / `test_backtest_gex_zg.py` similarly retire or restructure. | open |
| B-AI-004-3 | AI-SPEC-004 §10.2 B3 | Spec §1 line 47–50 (Tradier archived option chains) + Spec §1 line 62–66 ("Hard Rule: No Theoretical Pricing") | Hard prerequisite: archived option chains | **No archive infrastructure at HEAD.** `tradier_feed.py` (327 lines) does not persist chains. No `tradier_chain_archive` migration. `databento:opra:*` keys are 300s-TTL Redis (matrix §6 line 657–658). Spec correctly states "if no historical chain archive exists, chain-dependent replay is NOT calibration-grade" (Spec §1 line 49–50). | Add Class B note: V0.1 ship scope must EITHER (a) include a `tradier_chain_archive` (or `databento_opra_archive`) migration + writer wired into `tradier_feed.py` / `databento_feed.py` with operator-approved retention policy + months-long forward archival window before calibration-grade replay can be claimed, OR (b) explicitly accept Spec §10 Pre-V0.1 Failure Mode (lines 985–989) — V0.1 ships advisory only, no items promote to binding authority. **Load-bearing decision** — escalates to Class C as `C-AI-004-4`. | open |
| B-AI-004-4 | AI-SPEC-004 §10.2 B4 | Spec §1 line 43 (Polygon SPX / I:SPX historical 1-min bars) | Polygon historical 1-min fetch | `polygon_feed.py` (956 lines) only uses `/range/1/day` for VIX (line 706) and `/aggs/ticker/I:SPX/prev` (lines 525, 831). Polygon API supports `/range/1/minute/{from}/{to}` but no current code path uses it. May require Polygon paid tier (operator question; out of scope to verify). | Add Class B note: V0.1 ship scope must add a historical-fetch helper to `polygon_feed.py` (or in `replay_data_reconstructor.py`) wrapping `/v2/aggs/ticker/I:SPX/range/1/minute/{from}/{to}`. Operator must verify the Polygon subscription tier supports historical 1-min bars; if it requires upgrade, that's a contract dependency to be documented. | open |
| B-AI-004-5 | AI-SPEC-004 §10.2 B5 | Spec §13 V0.1 Ship Scope item 6 (`backend/replay_data_reconstructor.py with Commit 1-4 corrections`) — implies Commit-4-corrected re-marking is the source-of-truth | Spec §1 lines 70–138 define Commit 1 (IC/IB target_credit), Commit 2 (Databento LTRIM/GEX), Commit 3 (PCS/CCS), Commit 4 (re-mark + slippage) corrections, but **does not specify which production commit hashes those Commits map to.** | Add Class B note: Spec §1 should reference the production commit hashes that introduced each Commit-N correction (e.g., "Commit 1 = `b2b65c9` IC/IB target_credit fix" — verifiable from git log). At HEAD, the recent commit `77af9af` ("fix(strikes): extract target_credit from Tradier chain for iron_condor and iron_butterfly") may be the IC/IB Commit 1 anchor. Without explicit commit-hash anchoring, future replay runs cannot be deterministically tied to "the corrected logic" version. | open |
| B-AI-004-6 | AI-SPEC-004 §10.2 B6 | Spec §1 line 45 + §5 Timestamp Boundary line 609 ("news where published_at <= T") | News/event ingestion has minute-level `published_at` field | **No `published_at` field exists.** Sentiment agent stores `generated_at` (when WE generated brief, line 82, 135, 303 of `sentiment_agent.py`); economic_calendar uses `event_date` (date-level only); `earnings_calendar` has `earnings_date DATE` and `announce_time TEXT` ('pre' / 'post' / 'unknown') only. `trading_ai_briefs.generated_at` is NOT publish time. | Add Class B note: V0.1 ship scope must extend news/event ingestion to capture `published_at TIMESTAMPTZ` (when the event was originally announced/published) distinct from `ingested_at`/`generated_at` (when we received/generated). Affects: `sentiment_agent.py` raw news fetch, `economic_calendar.py` calendar rows, `earnings_calendar` table. Without this, Spec §5 Timestamp Boundary line 609 cannot be enforced at sub-day granularity — a leakage risk. | open |
| B-AI-004-7 | AI-SPEC-004 §10.2 B7 | Spec §1 line 36 + line 38 (reads from `strategy_attribution` and `ai_governor_decisions`) | Reads from Items 2 + 1 production tables | `strategy_attribution` — AI-SPEC-002 P1.3.2 `B-AI-002-2` documents absent at HEAD. `ai_governor_decisions` — AI-SPEC-001 P1.3.1 `B-AI-001-4` documents absent at HEAD. | Add Class B note: cross-spec FK dependency cascade — Item 4's V0.1 ship cannot run calibration-grade until at least the schema-shipping milestones of Items 1 + 2 land. Coordinate with the master sequencing plan in MASTER_PLAN.md — Item 4's "scaffolding ship" (4 tables + 8 files + Step 0 validation logic) can land independently, but its first calibration-grade run is blocked on Items 1 + 2. Confirms cross-spec theme **FK dependency cascade** (now confirmed in 2 audits: AI-SPEC-002 + AI-SPEC-004). | open |
| B-AI-004-8 | AI-SPEC-004 §10.2 B8 | Spec §1 line 37 (reads from `trade_counterfactual_cases`) | Reads from Item 10 production table | PRE-P11-4 / AI-SPEC-002 `C-AI-002-2`: existing scaffold is column-shaped on `trading_prediction_outputs`, not table-shaped. Operator decision pending. | Add Class B note: cross-spec — Item 4's spec wording at §1 line 37 should be updated PENDING the resolution of `C-AI-002-2`. If option 1 (Item 10 ships table), spec stays as-is. If option 2 (Item 2 + Item 4 reference existing column-shaped scaffold), Item 4 spec wording becomes "reads `counterfactual_pnl` / `counterfactual_strategy` / `counterfactual_simulated_at` columns from `trading_prediction_outputs`." If option 3 (defer to V0.2), Item 4 V0.1 ships without counterfactual-case integration. **Bilateral with AI-SPEC-002 / AI-SPEC-010 audits.** | open |
| B-AI-004-9 | AI-SPEC-004 §10.2 B9 | Spec §4 lines 388–432 (`replay_eval_cases.governor_output JSONB / item5_snapshot JSONB / item6_output JSONB / market_state_card JSONB / baseline_action JSONB / candidate_action JSONB / realized_outcome JSONB`) | Decision card schema | Spec gives column shapes (all JSONB) but does not formalize the JSON sub-schemas. The cross-spec implication for AI-SPEC-001 §12 V0.2 promotion gate ("≥200 replay/advisory decision cards") is a counted-thing whose definition is ambiguous at the spec boundary. | Add Class B note: V0.1 ship scope should include a JSON Schema specification (or at minimum a documented field list) for each of the 7 JSONB columns above — landed as a sub-doc at `trading-docs/04-modules/ai-architecture/replay-decision-card-schema.md` or similar. Bilateral with AI-SPEC-001 §12 wording: "decision card" = one row in `replay_eval_cases` where `calibration_eligible = true`. **shadow_engine.py's `shadow_predictions` rows do NOT count toward the 200-card threshold** (different table, different mechanism, different calibration-grade). Escalates as `C-AI-004-2`. | open |
| B-AI-004-10 | AI-SPEC-004 §10.2 B10 | Spec §3 lines 306–336 (LightGBM Model State Policy) + Spec §5 line 686 ("Going forward: log model artifacts daily") | Daily artifact + prediction-output logging substrate | At HEAD, `model_retraining.py` (12K, dormant; `bf41175` commit) trains models in-process. No artifact-log table; `trading_calibration_log` exists (matrix §5 line 542) but is event-log not artifact-log. No prediction-correlation > 0.95 substrate to evaluate Spec §3 case 3 ("reconstruct model"). | Add Class B note: V0.1 ship scope should include either (a) daily artifact serialization to S3-equivalent + a `model_artifact_log` table to track artifact paths + hashes, OR (b) explicit V0.1 deferral with `model_state_source = 'unavailable'` for all Item 6 / Item 1 calibration paths until artifact-log is ready (which means Spec §3 case 3 does not fire and Spec §3 case 4 is the V0.1 default). | open |
| B-AI-004-11 | AI-SPEC-004 §10.2 B11 | Spec §6 lines 698–703 (rolling 60/20/20 window, step 10 sessions) | Walk-forward implementation | No walk-forward implementation at HEAD. `scripts/backtest_gex_zg.py` does one-shot 2022–2023 sweep (not walk-forward; explicitly forbidden by spec — Spec §6 line 692 "Use rolling window, NOT expanding window"). | Bundled into B-AI-004-2 (`replay_threshold_optimizer.py` / `replay_validator.py`). Walk-forward fold logic must be explicit in `replay_validator.py` per Spec §6 lines 738–744. | open |
| B-AI-004-12 | AI-SPEC-004 §10.2 B12 | Spec §2 lines 168–229 (Step 0 validation tests — 4 binary checks: Pricing $0.10 / SPX 99% / VIX correlation 0.99 / Slippage MAE $0.10) | 4 binary tests | Absent at HEAD. | Bundled into B-AI-004-2 (`replay_validator.py` + Step 0 validation tests in `backend/tests/test_replay_validator.py` per Spec §13 V0.1 Ship Scope item 13). | open |
| B-AI-004-13 | AI-SPEC-004 §10.2 B13 | Spec §1 implicit (model/version tables) — bridging `trading_calibration_log` and a future `model_artifact_log` | Versioning substrate | `trading_calibration_log` is append-only event log; no `model_artifact_log` or `prompt_version_log` table at HEAD. Spec §10 Prompt Versioning lines 1018–1026 ("challenger runs on captured historical cards / promotion only if challenger beats champion on replay + paper shadow") — "captured historical cards" requires a substrate that doesn't yet exist. | Bundled into B-AI-004-10 + B-AI-004-9. Future audit P1.3.X for prompt-versioning specs (if any) should cross-reference. | open |
| B-AI-010-1 | AI-SPEC-010 §10.2 B1 | Spec §0 line 14 + Spec §11 line 558 (Layer 2 calibration-grade engine) | "Layer 2: Calibration-Grade Attribution Engine (new)" — proposed but file/module not named. | No `backend/counterfactual_engine_v2.py` or equivalent at HEAD. Spec §11 V0.2 ship items 9–15 reference Layer 2 functions but do not specify the module file. | Add Class B note to Spec §0 + §11: Layer 2 engine file location is **`backend/counterfactual_engine_v2.py`** (recommended canonical name; matches Item 1's `ai_governor_*` namespace pattern from `B-AI-001-4`). Spec §11 V0.2 ship scope explicitly names this module. | open |
| B-AI-010-2 | AI-SPEC-010 §10.2 B2 | Spec §3 lines 148–153 + Spec §4 lines 198–225, 232–269, 276–294, 299–308 (4 new tables + 3 new columns) | DDL provided as if part of spec deliverable; migration filename unspecified. | All 7 schema artifacts absent at HEAD (verified by `grep -rln "trade_counterfactual_cases\|strategy_utility_labels\|closed_trade_path_metrics\|position_path_samples\|strategy_hint\|strategy_structure\|candidate_source" supabase/migrations/` returning matches only in archived spec files). | No correction to spec wording — tag as "scheduled buildout"; clarify in Spec §11 V0.2 ship scope that Item 10 V0.2 Layer 2 includes 4 new migrations (one per table) + 1 ALTER TABLE migration on `trading_prediction_outputs` for the 3 new columns. Recommended filename pattern: `2026MMDD_add_counterfactual_layer2_*.sql`. | open |
| B-AI-010-3 | AI-SPEC-010 §10.2 B3 | Spec §3 lines 174–186 + `counterfactual_engine.py:213–223` (replace default-to-iron_condor fallback) | Spec proposes Layer 1 + Layer 2 behavior change: when `strategy_hint` missing, return `counterfactual_pnl = NULL` with `simulation_status = "insufficient_strategy_context"`. | Layer 1 at HEAD hardcodes `strategy = "iron_condor"` at `counterfactual_engine.py:219` with explicit comment lines 213–218 acknowledging the limitation. **Bilateral edit to existing Layer 1 `_simulate_pnl()` AND new Layer 2 simulator.** | Add Class B note: V0.2 ship scope includes a small surgical Layer 1 edit to `counterfactual_engine.py:213–223` — replace `strategy = "iron_condor"` fallback with NULL-return path when `strategy_hint` is missing on the source row. **This is consistent with Spec §0 "do not rewrite" architectural commitment** because it is a bug fix to a known dangerous behavior (per Spec §3 lines 174–186), not a rewrite of the engine. Update tests to cover NULL-return path. **Resolution conditional on `C-AI-010-1` operator decision.** | open |
| B-AI-010-4 | AI-SPEC-010 §10.2 B4 | Spec §13 lines 651–660 (`useActivationStatus.ts` Counterfactual Tracking entry) | "`builtStatus: 'live'`, `shippedDate: '2026-04-20'`, `shippedCommit: '2400e98'`" — three fields proposed. | At HEAD `src/hooks/trading/useActivationStatus.ts:241–251`: `builtStatus: 'live'` (line 250) shipped via `4970095`; **`shippedDate` and `shippedCommit` fields are NOT in the entry** and the `ActivationFlag` interface does not declare these fields anywhere in the file. | Add Class B note: Spec §13 Gap 5 is **partially resolved** at HEAD. Remaining work for V0.2 ship: extend `ActivationFlag` interface to include `shippedDate?: string` and `shippedCommit?: string`; add the values to the Counterfactual Tracking entry; consider applying same fields to other `builtStatus: 'live'` entries for consistency. Low-risk frontend-only change. | open |
| B-AI-010-5 | AI-SPEC-010 §10.2 B5 | Spec §6 lines 391–410 (API enforcement of `calibration_eligible`) | "`get_meta_labeler_training_data()`, `get_vol_engine_replay_cases()`, `get_adversarial_block_outcomes()`, `get_exit_optimizer_training_paths()` — function signatures with structural filtering" | None exist at HEAD; `grep -rln "get_meta_labeler_training_data\|get_vol_engine_replay_cases\|get_adversarial_block_outcomes\|get_exit_optimizer_training_paths" backend/` returns zero matches. | No correction to spec wording — tag as "scheduled buildout"; clarify in Spec §11 V0.2 ship scope that Layer 2 module exposes these 4 accessor functions with `calibration_eligible = true` filtering enforced at function-signature layer (not application convention). `diagnostic_opt_in` flag explicit + audit-logged per Spec §6 lines 414–419. | open |
| B-AI-010-6 | AI-SPEC-010 §10.2 B6 | Spec §5 lines 332–376 (slippage formula) | `estimated_slippage = base_strategy_slippage * time_multiplier * vol_multiplier * spread_multiplier * size_multiplier`; `vol_multiplier = 1.0 + 0.25 * max(0, vix_z)`; `spread_multiplier = 1.0 + 0.30 * max(0, spread_z)` | No implementation at HEAD; `vix_z` and `spread_z` producer-undefined (escalates to `C-AI-010-3`). `half_spread_estimate` for `base_strategy_slippage` — bilateral with chain-archive prerequisite (`C-AI-004-4`). | Add Class B note: V0.2 ship scope includes the slippage formula implementation as a Layer 2 utility module (likely `backend/replay_slippage_model.py` or method on Layer 2 engine), with explicit dependency declaration on `vix_z` / `spread_z` producers (resolved by `C-AI-010-3`) and `half_spread_estimate` source (bilateral with `C-AI-004-4`). | open |
| B-AI-010-7 | AI-SPEC-010 §10.2 B7 | Spec §7 lines 442–454 (halt-day blocked-cycle row creation) | "On halt activation: Continue rules-engine candidate computation (without execution). Create row in `trade_counterfactual_cases`: `case_type = 'counterfactual'`, `counterfactual_reason = 'halt_block'`, ..." | No halt-cycle row creation at HEAD. `risk_engine.py:234–235` (3-loss reduction) and `prediction_engine.py:688–691` (5-loss session halt) are the halt-trigger sites — neither writes counterfactual rows. **Cross-spec coordination with AI-SPEC-001 `B-AI-001-7` D-022 split.** | Add Class B note: V0.2 ship scope includes wiring at both halt-trigger sites (`risk_engine.py` AND `prediction_engine.py`) to insert `trade_counterfactual_cases` rows with `simulation_status = 'pending_eod_label'`. EOD job picks them up next run. Bilateral coordination with Item 1 V0.1 ship (`B-AI-001-7` D-022 enforcement). | open |
| B-AI-010-8 | AI-SPEC-010 §10.2 B8 | Spec §2 lines 128–138 (Versioned Width Table) | "`width_table_version`, `vix_bucket`, `strategy_type`, `time_bucket`, `default_short_delta`, `default_long_delta`, `default_width`" — versioned table format proposed. | Hardcoded `_DEFAULT_SPREAD_WIDTH = 5.0` at `counterfactual_engine.py:55`. No externalized width table at HEAD. | Add Class B note: V0.2 ship scope externalizes the width table as either (a) a new `vix_spread_width_table` Supabase table with operator-controlled writes; OR (b) a YAML config file checked into `backend/config/`. Default position: option (a) with versioned rows, enabling reproduction of historical Tier 2 simulations after table calibration. | open |
| B-AI-010-9 | AI-SPEC-010 §10.2 B9 | Spec §0 + §11 (Layer 2 vs Layer 1 routing) | Spec is silent on whether Layer 2 needs its own feature flag for canary-style rollout. | At HEAD, `feedback:counterfactual:enabled` (single flag) gates the entire Layer 1 EOD job. | Add Class B note: V0.2 ship scope adds **`feedback:counterfactual:layer2:enabled`** (Redis key, fail-closed DISABLED initially per Phase 0 conservative-first principle) to gate Layer 2 EOD job independently of Layer 1. Allows operator to run Layer 1 + Layer 2 in parallel during validation, then disable Layer 1 once Layer 2 graduates. Reuses `set-feature-flag` Edge Function pattern. | open |
| B-AI-010-10 | AI-SPEC-010 §10.2 B10 | Spec §4 line 209–210 vs AI-SPEC-002 Spec §2 (counterfactual_reason vs decision_outcome enum) | Item 10's `counterfactual_reason` enum (6 values): `no_trade | governor_block | meta_skip | adversarial_block | halt_block | alternative_strategy`. Item 2's `decision_outcome` enum (8 values, see AI-SPEC-002 audit): different vocabulary. | The two enums share concepts (`blocked_governor` ↔ `governor_block`; `halted_blocked_cycle` ↔ `halt_block`) but are not byte-identical. | Add Class B note: Spec §4 line 209–210 must clarify whether `counterfactual_reason` and `decision_outcome` are **independent vocabularies** (Item 2 = decision-side; Item 10 = counterfactual-reason-side) or **mapped 1:1**. Default position: independent vocabularies with documented mapping table — Item 10 V0.2 ship includes a `counterfactual_reason ↔ decision_outcome` mapping function in the Layer 2 engine, exposed for cross-spec consumers. Folds into D-023 wording (decision-outcome enum coordination). | open |
| B-AI-010-11 | AI-SPEC-010 §10.2 B11 | Migration `20260421_add_counterfactual_pnl.sql:4–5` ('Pure observability — never read by any trading-decision path.') vs Spec §0 line 8 ("Calibration-grade attribution data feeding Items 5-9") | Layer 1 migration header asserts observability-only contract; Layer 2 promotes data flow into training. Governance contract change. | At HEAD, the migration header is the canonical statement of Layer 1's contract; Spec §0 implicitly changes the contract for Layer 2. | Add Class B note: V0.2 ship scope includes governance update — `system-state.md` adds `counterfactual_engine` field tracking `{ layer_1_phase: 'shipped', layer_2_phase: 'not_started | v0.2_scaffolding | v0.2_active | demoted', calibration_eligible_classifier_active: bool, chain_archive_capable: bool }`. The Layer 1 migration header stays as-is (preserves observability contract); Layer 2's new `trade_counterfactual_cases` migration header explicitly declares "Calibration-grade — read by training accessors per Spec §6; advisory authority only — never gates trade execution." | open |
| B-AI-005-1 | AI-SPEC-005 §10.2 B1 | Spec §1 lines 21–100 + §12 line 741 (HAR-RV-Remainder model + EWMA fallback) | HAR-RV regression model on 5-minute SPX bars with 7-coefficient lag structure + EWMA fallback at λ ∈ [0.35, 0.50]. | No `backend/vol_fair_value_engine.py`, `backend/har_rv.py`, or equivalent at HEAD (`ls backend/ \| grep -iE 'har_rv\|har-rv\|vol_engine\|vol_fair_value'` returns zero matches). `backend/polygon_feed.py:486` 12A writer produces ONE input only (20-day annualized realized-vol scalar). | Add Class B note to Spec §1 + §12: HAR-RV engine file location is **`backend/vol_fair_value_engine.py`** (recommended canonical name; matches the AI-SPEC-010 `counterfactual_engine_v2.py` per-spec naming pattern). Spec §12 V0.2 ship scope explicitly names this module. Tests in `backend/tests/test_vol_fair_value_engine.py`. | open |
| B-AI-005-2 | AI-SPEC-005 §10.2 B2 | Spec §1 lines 73–77 (Training Window — rolling 252 sessions; minimum 90 clean post-Commit-4 sessions) | 252-session 5-minute SPX bar archive plus 252-session daily realized-vol history exist. | At HEAD, `polygon_feed.py:158` caps the in-memory `spx_history` at 60 5-minute samples (process-RAM only); `polygon_feed.py:466` caps `spx_daily_returns` at 20 daily entries; Redis `polygon:spx:realized_vol_20d` has 24-hour TTL. **No multi-year persistence anywhere** at HEAD. No `spx_5min_bars` migration. | No correction to spec wording — tag as "scheduled buildout"; clarify in Spec §12 V0.2 ship scope that Item 5 V0.2 includes multi-year SPX 5-minute bar archive substrate (table or external file) AND multi-year daily realized-vol substrate. **Bilateral with `B-AI-004-4`** (Polygon historical-bars fetch helper) — likely shared infrastructure. **Cross-spec FK dependency cascade with Items 2 + 10** (`strategy_attribution` per `B-AI-002-2` + `calibration_eligible` per `B-AI-001-5`). | open |
| B-AI-005-3 | AI-SPEC-005 §10.2 B3 | Spec §1 lines 73–77 (Training Window) — 12A `polygon:spx:realized_vol_20d` Layer 1 producer feeds HAR-RV inputs alongside the new HAR-RV regression. | 12A writer at `polygon_feed.py:486` is a daily-frequency scalar with 24-hour TTL. HAR-RV's `RV_D` (prior session) input expects 252-session daily history, not a single 20-day rolling scalar. **The 12A writer's output shape is incompatible with HAR-RV's input shape.** | Add Class B note: V0.2 ship scope must either (a) extend the 12A writer to also persist daily-RV history to a Supabase table (e.g., `daily_realized_vol_history`), enabling 252-session reconstruction; OR (b) replace the 12A writer with a new daily-RV producer that maintains historical depth via Supabase persistence; OR (c) keep 12A as legacy observability + ship a NEW persistent daily-RV writer for HAR-RV consumption. **Default position (per consolidated plan §5 conservative-first principle):** option (c) — preserve 12A for backward-compatibility with existing consumers (`prediction_engine.py:528, 706`, `main.py:2061`); ship NEW writer. Cutover policy folds into `C-AI-005-2`. | open |
| B-AI-005-4 | AI-SPEC-005 §10.2 B4 | Spec §1 lines 41–57 (Lag Structure — 7 features: `RV_D` / `RV_W` / `RV_M` / `RV_so_far_scaled` / `overnight_gap` / `vix_change_from_prior_close` / `event_day_flag` / `time_bucket`) | HAR-RV lag-structure inputs consumed at decision time. | None of the 7 features exist as Redis keys, Supabase columns, or backend functions at HEAD. `polygon:spx:overnight_gap` is consumed at `prediction_engine.py:544` but its **producer is undefined** at HEAD (verified by `grep -n "polygon:spx:overnight_gap" backend/polygon_feed.py` returning zero matches; the consumer reads the key with a `0.0` default). `polygon:vix:5d_change` exists (consumed at `prediction_engine.py:554`) — adjacent but not equivalent to `vix_change_from_prior_close`. `event_day_flag` would derive from economic_calendar + earnings_calendar but no field of this name exists. | Add Class B note: V0.2 ship scope includes lag-structure feature engineering as a separate buildout unit — likely `backend/vol_fair_value_features.py` or method on `vol_fair_value_engine.py`. Required producers: (a) `polygon:spx:overnight_gap` producer (currently consumer-only orphan); (b) `vix_change_from_prior_close` calculation (similar to existing 5d_change pattern); (c) `event_day_flag` from economic_calendar + earnings_calendar joined on session date; (d) `RV_so_far` running variance from intraday 5-minute bar archive. | open |
| B-AI-005-5 | AI-SPEC-005 §10.2 B5 | Spec §1 lines 92–100 (Output Fields — `rv_forecast_har`, `rv_forecast_ewma`, `rv_forecast_final`, `rv_model_confidence`, `forecast_error_recent`) + Spec §9 line 664 (`vol_fair_value_snapshots` table) | Output schema + persistence target named but DDL absent. | None exist at HEAD; no migration for `vol_fair_value_snapshots` (verified by `grep -rln "vol_fair_value_snapshots" supabase/migrations/` returning zero matches across 68 migration files). | No correction to spec wording — tag as "scheduled buildout"; clarify in Spec §12 V0.2 ship scope that Item 5 V0.2 includes 1 new migration for `vol_fair_value_snapshots` with full DDL covering: `decision_time TIMESTAMPTZ NOT NULL`, `time_bucket TEXT NOT NULL`, `rv_forecast_har NUMERIC`, `rv_forecast_ewma NUMERIC`, `rv_forecast_final NUMERIC`, `rv_model_confidence NUMERIC`, `iv_rv_ratio NUMERIC`, `skew_z NUMERIC`, surface scores (3 columns), `term_slope NUMERIC`, `forecast_error_recent NUMERIC`, `engine_reliability_score JSONB` (per-channel state), `strategy_ev_table_top3 JSONB`, `divergence_check_pass BOOLEAN`, `degradation_flags JSONB`, `forecast_horizon_minutes INTEGER`, plus `calibration_eligible BOOLEAN NOT NULL DEFAULT false` (per cross-spec `calibration_eligible` flag pattern). Recommended migration filename: `2026MMDD_add_vol_fair_value_snapshots.sql`. | open |
| B-AI-005-6 | AI-SPEC-005 §10.2 B6 | Spec §2 lines 104–155 (Implied Volatility Extraction — chain cleaning, forward price, model-free implied variance, ATM straddle proxy, 16-delta skew, surface features, term structure) | 7 distinct sub-modules of IV extraction proposed. | No model-free option-strip implementation at HEAD (verified by `grep -rln "model_free\|truncated_strip\|put_call_parity_forward" backend/` returning zero matches). Tradier chain access exists (`backend/tradier_feed.py`, 327 lines) but does not produce `IVar_Q` outputs. **Bilateral with `C-AI-004-4` chain archive prerequisite** for replay-time IV reconstruction. | Add Class B note: V0.2 ship scope includes IV extraction as either (a) live-only at decision time (no replay support) — works during paper / live trading; OR (b) live + replay (requires archived chains per `C-AI-004-4`). **Default: (a) for V0.1 advisory-only ship; (b) for V0.2 calibration-grade ship.** Module location: `backend/iv_extraction.py` or methods on `vol_fair_value_engine.py`. | open |
| B-AI-005-7 | AI-SPEC-005 §10.2 B7 | Spec §3 lines 237–273 (Terminal Distribution Simulation — N=10,000 scenarios + calibrated Student-t df via grid MLE + regime-conditional df) | Distribution simulator with 10,000-scenario sampling + grid MLE over `df ∈ [3, 12]` + regime-conditional df. | No simulator at HEAD (verified by `grep -rln "StudentT\|terminal_distribution\|df_calibrated\|grid MLE" backend/` returning zero matches); no `df_calibrated` storage; significant compute requirement absent today. | Add Class B note: V0.2 ship scope includes terminal distribution simulator as `backend/vol_terminal_distribution.py` or methods on `vol_fair_value_engine.py`. Performance budget: 10,000 scenarios × 7 strategy candidates × 7 time buckets × 252 sessions ≈ 1.2M Monte Carlo evaluations per replay run. Operator should verify the compute budget fits within hardware envelope before committing to per-decision live evaluation cadence. **Possible operator-decision: cache scenario draws by `(time_bucket, regime_bucket, RV_decile)` to amortize.** | open |
| B-AI-005-8 | AI-SPEC-005 §10.2 B8 | Spec §3 lines 277–303 (LightGBM Directional Tilt with cap and suppression rules) | Tilt requires `governor.uncertainty_score`, `governor.event_class`, `governor.data_freshness_warning`, `ml_confidence`, `ml_calibration_ok`, `ai_event_conflict`. | All `governor.*` fields absent at HEAD per AI-SPEC-001 `B-AI-001-4` (`ai_governor_decisions` table absent); all `ml_*` fields absent at HEAD per AI-SPEC-006 matrix (Item 6 dormant); `ai_event_conflict` is bilateral cross-spec. **Hard sequencing constraint.** | Add Class B note: V0.2 ship scope LightGBM tilt is **calendar-blocked** on Item 1 V0.1 schema-landing milestone + Item 6 V0.x ship. V0.2 ship scope should explicitly stage the tilt module to ship **AFTER** Items 1 + 6 — initial Item 5 V0.1 advisory-only ship runs without LightGBM tilt (`mu_tilt = 0` always), then V0.2 enables tilt once the dependent fields exist. **Bilateral with cross-spec FK dependency cascade theme** (now confirmed in 4 audits: AI-SPEC-002 + AI-SPEC-004 + AI-SPEC-005 + AI-SPEC-010). | open |
| B-AI-005-9 | AI-SPEC-005 §10.2 B9 | Spec §4 lines 309–438 (Strategy-Conditional EV Table + per-strategy decision rules for IC/IB/PCS/Long Straddle/DCS) | Full EV table generator with quality scores per strategy + threshold gates per strategy. | No EV table generator at HEAD (verified by `grep -rln "strategy_ev_table\|EV_per_margin\|cvar_5\|breach_probability" backend/` returning zero matches). Closest existing: `backend/strategy_selector.py` selects strategy by gates not by simulated EV. **Strategy-class taxonomy** — Spec §4 names IC, IB, PCS, Long Straddle, DCS (5 of 7 canonical strategies); matches `risk_engine.py:105–106` enum naming `iron_condor / iron_butterfly / put_credit_spread / call_credit_spread / debit_call_spread / debit_put_spread / long_straddle`. Item 5 must use same strategy IDs as Item 2's `outcome_mode` enum (per `B-AI-002-4`) and Item 6's strategy class definitions (forward-reference — P1.3.6 audit pending). | Add Class B note: V0.2 ship scope includes EV table generator as `backend/vol_strategy_ev_table.py` or methods on `vol_fair_value_engine.py`. Module produces per-decision-time top-3 EV-ranked strategies for Card consumption (Item 1 line 17) AND meta-labeler input (Item 6). **Anti-double-counting rule (Spec §1 line 187–190):** EV table outputs use `skew_z` (not `smile_asymmetry_score`) for decision penalties; outputs both schemas with explicit producer-surface labels. **Confirms cross-spec theme `decision_outcome enum coordination` extension to strategy-taxonomy alignment** (now confirmed in 4th audit: AI-SPEC-002 + AI-SPEC-004 + AI-SPEC-010 + AI-SPEC-005). | open |
| B-AI-005-10 | AI-SPEC-005 §10.2 B10 | Spec §5 lines 441–498 (Engine Reliability Score with per-channel authority + recovery state machine) | Per-channel authority levels (`short_gamma_filter_authority`, `long_vol_enable_authority`, `strategy_ev_ranking_authority`) with degradation triggers + 30-session cooldown + 20-qualified-signals + 4-criteria recovery. | No state machine at HEAD (verified by `grep -rln "short_gamma_filter_authority\|long_vol_enable_authority\|strategy_ev_ranking_authority\|engine_reliability_score" backend/` returning zero matches). **Per-channel granularity is finer than `item_promotion_records` per-item-per-scope granularity** (per AI-SPEC-004 `B-AI-004-1`). Schema extension required: either Item 4's `item_promotion_records` adds a `channel TEXT` column for per-channel-within-item granularity, OR Item 5 maintains per-channel state in `vol_fair_value_snapshots` row with cross-reference. | Add Class B note: V0.2 ship scope includes reliability score state machine as `backend/vol_reliability_state.py` or methods on `vol_fair_value_engine.py`. **Bilateral with AI-SPEC-004 `item_promotion_records` schema** — operator decision required between (a) extend `item_promotion_records` with `channel TEXT NULL` column (preferred — single source of truth for authority); OR (b) Item 5 maintains its own per-channel state internally and reports to `item_promotion_records` as a single rolled-up entry (preserves Item 4 schema as-is, but creates two state surfaces). **Default: option (a)**. Cross-spec coordination: Item 4 V0.1 ship should pre-emptively include the `channel` column with NULL default to avoid downstream migrations. | open |
| B-AI-005-11 | AI-SPEC-005 §10.2 B11 | Spec §6 lines 502–540 (Bid-Ask Spread Tolerance per leg) | Per-leg filter requiring `bid` + `ask` + `mid` + `spread_pct` per option leg + time-based percent tolerance + absolute spread sanity + strategy-level slippage budget. | No spread filter implementation at HEAD (verified by `grep -rln "spread_pct\|max_abs_spread_points\|cheap_option_abs_limit" backend/` returning zero matches). **Bilateral with `C-AI-004-4` chain-archive substrate** — accurate replay-time spread filtering requires archived bid/ask quotes per strike per minute. | Add Class B note: V0.2 ship scope includes spread filter as a method on `vol_fair_value_engine.py` (admission-time filter for entry; advisory for exits). **Bilateral with chain-archive prerequisite** — V0.1 advisory-only ship can run the filter on **live** chains only (no replay support). V0.2 calibration-grade ship requires archived chains per `C-AI-004-4`. **Bilateral with AI-SPEC-010 `B-AI-010-6` slippage formula** — both consume `vix_z` / `spread_z` / `time_of_day` features per `C-AI-010-3` shared `features:*` Redis namespace. | open |
| B-AI-005-12 | AI-SPEC-005 §10.2 B12 | Spec §7 lines 543–639 (Replay Calibration Harness with Baselines A–F) | Baselines A through F orchestrator + QLIKE / log-RV RMSE + forecast metrics + trading metrics + production authority thresholds + per-channel fallback levels (1–3). | No replay calibration harness at HEAD. **Bilateral with AI-SPEC-004 V0.1** (Item 4 owns the harness; Item 5 plugs in as a model under test). Item 4 V0.1 has chain-archive prerequisite (`C-AI-004-4`). **Calendar-blocked bilateral with `C-AI-004-4` and Items 2 + 10 schema-landing milestones** — Item 5 V0.2 calibration cannot run until Items 2 + 10 ship calibration_eligible substrate. | Add Class B note: V0.2 ship scope replay calibration is **calendar-blocked** on Item 4 V0.1 ship + chain-archive substrate sufficiency + Items 2 + 10 calibration_eligible substrate. Item 5 V0.2 ships in **Tier 3 advisory-only mode** (Spec §7 lines 633–639 Fallback Level 3) if chain-archive insufficient at V0.2 ship time. Operator decision required at V0.2 ship time — bilateral with `C-AI-004-4`. Escalates to Class C as `C-AI-005-3` (bilateral resolution). **Confirms cross-spec theme `FK dependency cascade` (now confirmed in 4 audits: AI-SPEC-002 + AI-SPEC-004 + AI-SPEC-005 + AI-SPEC-010).** | open |
| B-AI-005-13 | AI-SPEC-005 §10.2 B13 | Spec §8 lines 643–652 (Retraining Cadence — daily HAR retrain, weekly Student-t df + EWMA, monthly decision-score weights) | Multi-cadence retraining orchestration. | No retraining infrastructure at HEAD (verified by `grep -rln "retrain.*har\|recalibrate.*ewma\|recalibrate.*df_student_t" backend/` returning zero matches). `backend/model_retraining.py` (12K dormant per `bf41175` commit) covers meta-labeler retraining only. | Add Class B note: V0.2 ship scope includes retraining cadence orchestration as either (a) extension of existing `backend/model_retraining.py` (single-file pattern); OR (b) new `backend/vol_retraining.py` (per-engine pattern). **Default: (a)** — re-use existing scheduler infrastructure (cron job pattern in `backend/main.py`); add new entry points for HAR / EWMA / df / decision-weight retraining. **Bilateral with `B-AI-004-4`** Polygon historical-bars fetch helper for daily HAR retrain over 252-session rolling window. **Bilateral with `B-AI-004-10` model artifact-log substrate** — HAR-RV retraining outputs feed model artifact log when ready. | open |
| B-AI-005-14 | AI-SPEC-005 §10.2 B14 | Spec §9 line 664 (`vol_fair_value_snapshots` table) — explicit register row for traceability | Persistence target named in spec data flow but no DDL provided. | Table absent; DDL undefined. | **Bundled with `B-AI-005-5`** (output fields + table together). Register row preserved for §3 verification cross-reference traceability — see `B-AI-005-5` for canonical correction language. | open |
| B-AI-005-15 | AI-SPEC-005 §10.2 B15 | Spec §11 lines 720–728 (Pre-V0.2 Data Requirements — multi-row table) | Multiple data-substrate minimums: 30–60 sessions for EWMA fallback, 90 sessions for HAR-RV pooled, 60 bucket observations for time-bucket df, 100 per regime bucket for regime-conditional df, 100+ signals for strategy EV thresholds, 30+ clean signals for long-vol production authority. | All substrates absent at HEAD. No `spx_realized_vol_history` Supabase table (`grep -rln "spx_realized_vol_history\|realized_vol_history" supabase/migrations/` returns zero matches). EOD writer at `polygon_feed.py:486` overwrites the same key daily — no historical persistence. **Bilateral with `B-AI-004-4`** Polygon historical-bars fetch — likely shared infrastructure. **Bilateral with `C-AI-004-4`** chain archive prerequisite. | Add Class B note: V0.2 ship scope includes data-substrate buildout as a sub-item — either via (a) months-long forward archival starting at V0.2 schema-landing milestone; OR (b) Polygon historical-bars subscription + ingest pipeline (paid contract dependency); OR (c) hybrid (forward archival starts immediately, paid historical fills the gap for the first 6 months). **Default: (c)** — minimizes calendar slip. Operator decision required on Polygon subscription tier upgrade. **Confirms cross-spec theme `calibration_eligible flag` (now confirmed in 4th audit: AI-SPEC-001 + AI-SPEC-002 + AI-SPEC-010 + AI-SPEC-005).** | open |
| B-AI-005-16 | AI-SPEC-005 §10.2 B16 | Spec §0 + §9 (output Redis namespace) — implies freshness gate via `_safe_redis()` for HAR-RV input keys (`polygon:spx:realized_vol_20d`, `polygon:vix:z_score`) + output namespace `vol:*` | Spec is silent on whether engine outputs to a `vol:*` Redis namespace OR only to `vol_fair_value_snapshots` Supabase table; spec implies freshness gating on inputs. | At HEAD, no `vol:*` Redis keys exist. Adjacent `polygon:spx:*` keys are inputs not outputs. **Carry-forward #4:** `_safe_redis()` at `backend/prediction_engine.py:100–131` is dead code (zero callers; `grep -n "_safe_redis(" backend/*.py` returns only the definition). **Carry-forward #1:** `gex:updated_at` has no producer (`grep -rn 'gex:updated_at' backend/` returns reads only, no writes). Freshness substrate is **non-functional at HEAD.** Confirms cross-spec theme `_safe_redis()` dead code in 2nd audit (AI-SPEC-001 was 1st via `B-AI-001-6`). | Add Class B note: V0.2 ship scope adds Redis output namespace `vol:*` (recommended canonical) for live consumers — Items 1 (Card features), 6 (meta-labeler features), 9 (exit optimizer). Specifically: `vol:rv_forecast_remaining` (TTL = forecast horizon), `vol:iv_rv_ratio` (TTL = 60s), `vol:skew_z` (TTL = 60s), `vol:engine_reliability_score` (TTL = 300s), `vol:strategy_ev_table_top3` (TTL = forecast horizon), `vol:authority_level:short_gamma_filter` / `vol:authority_level:long_vol_enable` / `vol:authority_level:strategy_ev_ranking` (TTL = 3600s), `vol:degradation_flags` (TTL = 3600s), `vol:engine_updated_at` (TTL = 3600s, mirrors `gex:updated_at` pattern). **Bilateral with `B-AI-001-6` freshness substrate** — `vol:engine_updated_at` is the producer for Item 5's freshness gate; `_safe_redis()` caller wiring required for consumer-side freshness checks. Alternatively, Spec §11 must explicitly mark freshness gate as MVP-deferred. | open |
| B-AI-005-17 | AI-SPEC-005 §10.2 B17 | Spec §1 line 74 (`90 clean post-Commit-4 sessions`) + Spec §11 line 729 (`6 months of post-Commit-4 data`) | Spec uses "Commit-4" abstract anchor (same as AI-SPEC-010 Spec §6) for calibration-eligibility cutover for HAR-RV training set. | Spec is silent on the producer-surface contract for marking pre-`fc64840` realized-vol records as `calibration_eligible = false`. **Bilateral with `B-AI-002-6` legacy cutover policy** for `trading_positions`. **Bilateral with `C-AI-010-5`** operator-locked Commit-4 anchor. | Add Class B note: V0.2 ship scope includes a `calibration_eligible` classifier for the new daily-realized-vol historical table (per `B-AI-005-3` option (c)). Records with `produced_at < fc64840 deploy timestamp (2026-04-20 18:43 -0400)` are marked `calibration_eligible = false` (pre-fix EOD gate timing); records `produced_at >= fc64840` are marked `calibration_eligible = true` provisionally; records `produced_at >= <Commit_4_deploy_date>` are marked `calibration_eligible = true` definitively (per `C-AI-010-5` operator-locked anchor). HAR-RV training queries filter `calibration_eligible = true` structurally per the cross-spec `calibration_eligible` producer-surface contract. **Folds into AI-SPEC-002 `B-AI-002-6` legacy cutover policy framework + AI-SPEC-010 `C-AI-010-5` cutover anchor lock.** | open |
| B-AI-006-1 | AI-SPEC-006 §10.2 B1 | Spec §0 line 8 + §13 lines 744–757 (the keystone meta-labeler engine — three-part hybrid Bayesian + logistic + memory-blender) | Spec proposes Bayesian three-part hybrid engine but file/module not named. No Layer 2 file at HEAD. | No `backend/meta_labeler_engine.py` at HEAD. Layer 1 `bf41175` scaffold is LightGBM-only at `backend/model_retraining.py:722` + `backend/execution_engine.py:328–399`. Layer 1 dormant (`backend/models/meta_label_v1.pkl` absent — `ls -la backend/models/` returns only `.gitkeep`). | Add Class B note to Spec §13 V0.2 ship scope: Layer 2 engine file location is **`backend/meta_labeler_engine.py`** (recommended canonical name; matches AI-SPEC-005's `vol_fair_value_engine.py` and AI-SPEC-010's `counterfactual_engine_v2.py` per-spec naming pattern). Spec §13 V0.2 build scope explicitly names this module. **Verification command:** `grep -rln "meta_labeler_engine\|MetaLabelerEngine\|three_part_hybrid" backend/` returns 0 matches at HEAD. Falsifiable. | open |
| B-AI-006-2 | AI-SPEC-006 §10.2 B2 | Spec §3.1 lines 220–238 (18 features from Item 5 EV Table — `ev_per_margin`, `ev_dollars`, `pop`, `p_max_loss`, `cvar_5`, `iv_rv_ratio`, `rv_forecast_move`, `implied_move`, `skew_z`, `smile_asymmetry_score`, `smile_width_score`, `tail_width_score`, `engine_confidence`, `authority_level`, `estimated_slippage`, `slippage_to_ev_ratio`, `breakeven_distance_points`, `breakeven_distance_vs_forecast_move`) | Spec assumes Item 5 EV Table emits all 18 fields. | All 18 fields absent at HEAD. Item 5 V0.2 has 17 open Class B entries (`B-AI-005-1` through `B-AI-005-17`); `strategy_ev_table` returns 0 matches in `backend/` and `supabase/`. | **CALENDAR-BLOCKER (Guard 4 — Class B finding tracking surface).** Add Class B note: Item 6 V0.2 ship is calendar-blocked on Item 5 V0.2 ship-completion. Specifically bilateral with `B-AI-005-1` (HAR-RV engine), `B-AI-005-5` (output schema + `vol_fair_value_snapshots` table), `B-AI-005-9` (strategy-conditional EV table generator), `B-AI-005-10` (engine reliability state machine — produces `engine_confidence` + `authority_level`), `B-AI-005-16` (output Redis namespace `vol:*`). **Verification command:** `grep -rn "ev_per_margin\|ev_dollars\|p_max_loss\|cvar_5\|engine_confidence\|estimated_slippage\|slippage_to_ev_ratio\|breakeven_distance_points\|rv_forecast_move\|implied_move\|skew_z\|smile_asymmetry_score\|authority_level" backend/ supabase/` returns 0 matches for 17 of 18 fields (only `iv_rv_ratio` partially via `polygon_feed.py:51` comment). Falsifiable. | open |
| B-AI-006-3 | AI-SPEC-006 §10.2 B3 | Spec §3.3 lines 285–289 (5 Rules Engine fields: `rules_regime`, `rules_confidence`, `strategy_selected_by_rules`, `strategy_allowed_by_map`, `distance_from_regime_boundary`) | Spec assumes Rules Engine emits all 5 fields. | All 5 fields return 0 matches in `backend/`. Existing Rules Engine logic in `backend/strategy_selector.py` produces strategy decisions but does not emit named structured fields matching spec. | Add Class B note: V0.2 ship scope includes Rules Engine schema extension (or new producer module) emitting the 5 named fields. Bilateral with `backend/strategy_selector.py` refactor. **Verification command:** `grep -rn "rules_regime\|rules_confidence\|strategy_selected_by_rules\|strategy_allowed_by_map\|distance_from_regime_boundary" backend/` returns 0 matches across all 5 fields. Falsifiable. | open |
| B-AI-006-4 | AI-SPEC-006 §10.2 B4 | Spec §3.4 lines 293–305 (5 LightGBM Direction Model fields: `p_bull`, `p_bear`, `direction_confidence`, `direction_edge`, `strategy_direction_alignment`) | Spec reads 5 fields from a "LightGBM Direction Model" producer. | LightGBM Direction Model entirely absent at HEAD. `p_bull` / `p_bear` exist only in `backend/tests/test_consolidation_s5.py:211–241` as AI synthesis fields (NOT a LightGBM direction model); `direction_confidence` exists only as `macro_agent` direction_bias proxy. Bilateral with `C-AI-006-4` (LightGBM Direction Model spec ownership). | Add Class B note: V0.2 ship scope LightGBM Direction Model is **calendar-blocked** on operator decision per `C-AI-006-4`. Default option 2 (new spec AI-SPEC-014) defers Item 6 V0.2 ship until AI-SPEC-014 audit + V0.x build complete. **Verification command:** `grep -rn "direction.*model\|p_bull\|lightgbm.*direction" backend/ --include="*.py"` shows existing LightGBM uses (polygon_feed feature engineering, model_retraining meta-label scaffold) but no direction model. Falsifiable. | open |
| B-AI-006-5 | AI-SPEC-006 §10.2 B5 | Spec §3.5 lines 309–322 (9 AI Risk Governor structured fields: `event_class`, `novelty_score`, `uncertainty_score`, `signal_conflict_score`, `data_freshness_warning`, `short_gamma_block_flag`, `allowed_strategy_class_flag`, `governor_size_cap`, `review_required`) | Spec assumes Risk Governor emits all 9 structured fields. | All 9 fields absent at HEAD per AI-SPEC-001 `B-AI-001-4` (`ai_governor_decisions` table absent); `event_class` exists only as test fixture in `backend/tests/test_phase_2a_calendar.py:28,48`. | **CALENDAR-BLOCKER.** Add Class B note: Item 6 V0.2 ship calendar-blocked on Item 1 V0.1 schema-landing milestone. Bilateral with `B-AI-001-4` (governor decisions table) + `B-AI-001-2` (Card schema delta). **Verification command:** `grep -rn "novelty_score\|uncertainty_score\|signal_conflict_score\|data_freshness_warning\|short_gamma_block_flag\|allowed_strategy_class_flag\|governor_size_cap\|review_required" backend/` returns 0 matches. Falsifiable. | open |
| B-AI-006-6 | AI-SPEC-006 §10.2 B6 | Spec §3.6 lines 326–340 (10 Memory Retrieval Top-20 Analogs fields: `n_similar_cases`, `similarity_weighted_win_rate`, `similarity_weighted_avg_utility`, `similarity_weighted_p25_utility`, `same_strategy_case_count`, `same_event_case_count`, `same_regime_case_count`, `similar_case_tail_loss_rate`, `analog_disagreement_score`, `most_recent_similar_case_age`) | Spec assumes Memory Retrieval substrate emits all 10 fields. | All 10 fields absent at HEAD; underlying Memory Retrieval substrate (pgvector / case_retrieval / trade_decision_cases / similar_cases) entirely absent. Memory Retrieval is itself a separate AI architecture spec (Item 7) — not yet audited. | **CALENDAR-BLOCKER (deepest in cascade).** Add Class B note: Item 6 V0.2 ship calendar-blocked on Item 7 V0.x ship — Item 7 itself un-audited (future P1.3.7). **Verification command:** `grep -rn "n_similar_cases\|similarity_weighted_win_rate\|similarity_weighted_avg_utility\|same_strategy_case_count\|analog_disagreement_score\|pgvector\|case_retrieval\|trade_decision_cases\|memory_retrieval\|similar_cases" backend/` returns 0 matches across all 10 fields and underlying substrate. Falsifiable. | open |
| B-AI-006-7 | AI-SPEC-006 §10.2 B7 | Spec §3.2 lines 245–255 (4 derived features: `vol_edge`, `edge_after_slippage`, `ev_to_cvar`, `breakeven_to_forecast`) | Spec computes 4 derived features from Item 5 EV Table inputs. | Bundled with `B-AI-006-2` calendar-blocker on Item 5 V0.2; no separate gap. | Add Class B note: 4 derived features computed at decision time inside Item 6 engine module (`backend/meta_labeler_engine.py` per `B-AI-006-1`). Falsifiable as part of `B-AI-006-2`. | open |
| B-AI-006-8 | AI-SPEC-006 §10.2 B8 | Spec §1 lines 30–141 (per-strategy utility label calculators `U_IC`, `U_IB`, `U_PCS`, `U_STRADDLE`, `U_DCS`) | Spec defines 5 per-strategy utility functions with hurdle rates + penalties. | No utility label calculator at HEAD. Layer 1 `bf41175` trains on `outcome_correct` (binary) — NOT on per-strategy utility. | Add Class B note: V0.2 ship scope includes utility label calculator as method on `backend/meta_labeler_engine.py` (or separate `backend/strategy_utility_labels.py`). Bilateral with AI-SPEC-002 `outcome_mode` mapping per `B-AI-002-4` (need to verify `MAE_R`, `max_loss_zone_peak`, `max_loss_zone_twap`, `short_strike_breach_flag`, `event_day_short_gamma_flag`, etc. are derivable from `strategy_attribution` + `closed_trade_path_metrics` rows). Bundled with `B-AI-002-2` `closed_trade_path_metrics` substrate (canonical owner Item 10 V0.2 per `B-AI-010-2`). **Verification command:** `grep -rln "U_IC\|U_PCS\|U_DCS\|y_take\|MAE_R\|max_loss_zone_peak\|max_loss_zone_twap\|short_strike_breach_flag\|event_day_short_gamma_flag\|stall_after_entry_flag" backend/` returns 0 matches. Falsifiable. | open |
| B-AI-006-9 | AI-SPEC-006 §10.2 B9 | Spec §3.5 + Spec §6 — Item 6 must emit `decision_outcome` value to `strategy_attribution` per AI-SPEC-002 `C-AI-002-1` | Spec is silent on which `decision_outcome` enum value Item 6 emits when blocking / down-weighting trades. | `grep -nE "blocked_meta_labeler\|blocked_by_meta\|decision_outcome" ITEM_6_META_LABELER_LOCKED.md` returns 0 matches. AI-SPEC-002 audit `C-AI-002-1` flags this as bilateral with Item 6 spec. | Add Class B note: V0.2 ship scope must specify the `decision_outcome` enum value Item 6 emits. Recommended canonical: `blocked_meta_labeler` (matches AI-SPEC-002 audit's pre-audit assumption). Bilateral with D-023 enrichment item (t). Folds into Item 2 enum extension. **Verification command:** `grep -nE "blocked_meta_labeler\|blocked_by_meta\|decision_outcome" trading-docs/04-modules/ai-architecture/archive/raw-locked-specs-2026-04-26/ITEM_6_META_LABELER_LOCKED.md` returns 0 matches. Falsifiable. | open |
| B-AI-006-10 | AI-SPEC-006 §10.2 B10 | Spec §4.1 lines 380–390 (Train on Candidate Decisions, Not Just Executed Trades) | Spec specifies training-data filter on candidate decisions but is silent on `calibration_eligible` filter. | `grep -nE "calibration_eligible\|eligible" ITEM_6_META_LABELER_LOCKED.md` returns 0 matches. Cross-spec contract per AI-SPEC-002 + AI-SPEC-004 + AI-SPEC-005 + AI-SPEC-010 mandates `calibration_eligible = true` filter on training queries. | Add Class B note: V0.2 ship scope explicitly applies `calibration_eligible = true` filter on training-set assembly. Specifically: queries to `strategy_attribution` filter on `calibration_eligible = true`; queries to `trade_counterfactual_cases` filter on `calibration_eligible = true` AND `simulation_status = 'success'` (per `B-AI-010-5` accessor enforcement); queries to `replay_eval_cases` filter on `calibration_eligible = true` (per `C-AI-004-2`). **Verification command:** `grep -nE "calibration_eligible" trading-docs/04-modules/ai-architecture/archive/raw-locked-specs-2026-04-26/ITEM_6_META_LABELER_LOCKED.md` returns 0 matches. Falsifiable. | open |
| B-AI-006-11 | AI-SPEC-006 §10.2 B11 | Spec §13 V0.2 Build Scope (lines 744–764) — silent on Supabase persistence target | No `meta_labeler_decisions` table or equivalent named in spec. | 0 matches in `supabase/migrations/` for any meta-labeler-decision table. | Add Class B note: V0.2 ship scope adds 1 new migration for `meta_labeler_decisions` table with full DDL covering: `decision_id UUID PK`, `decision_time TIMESTAMPTZ NOT NULL`, `strategy_type TEXT NOT NULL` (canonical 10 per AI-SPEC-005 D-023 item (m); see `B-AI-006-12`), `time_bucket TEXT NOT NULL`, `regime TEXT NOT NULL`, `event_class TEXT`, `p_prior NUMERIC`, `p_logistic NUMERIC`, `p_memory NUMERIC`, `p_take NUMERIC`, `p_hat NUMERIC`, `p_low NUMERIC`, `p_high NUMERIC`, `ci_width NUMERIC`, `p_eff NUMERIC`, `meta_size_cap NUMERIC`, `reason_codes JSONB`, `n_similar_cases INTEGER`, `analog_disagreement_score NUMERIC`, `feature_vector JSONB`, `governor_size_cap_at_decision NUMERIC`, `final_size_after_arbiter NUMERIC`, `decision_outcome TEXT NOT NULL`, `calibration_eligible BOOLEAN NOT NULL DEFAULT false`, `version_tag TEXT NOT NULL`. Recommended migration filename: `2026MMDD_add_meta_labeler_decisions.sql`. **Verification command:** `grep -rln "meta_labeler_decisions" supabase/migrations/` returns 0 matches. Falsifiable. | open |
| B-AI-006-12 | AI-SPEC-006 §10.2 B12 | Spec §1 lines 30–141 (5 strategies: IC, IB, PCS, Long Straddle, DCS) vs canonical strategy taxonomy | Spec enumerates 5 strategies. | Migration `20260419_add_strategy_types.sql:11–22` defines 10 canonical values; AI-SPEC-005 D-023 item (m) defines canonical 8. Item 6's 5 is a strict subset missing `call_credit_spread`, `cash_secured_put`, `covered_call`, `long_call`, `long_put`, AND inconsistent on `long_straddle` + `debit_call_spread` membership. | Add Class B note: Item 6 V0.2 must use canonical 8-strategy IDs per D-023 item (m); spec wording amendment required to enumerate the missing strategies AND reconcile `long_straddle` / `debit_call_spread` taxonomy with AI-SPEC-005 / migration. Default position (per consolidated plan §5 conservative-first principle): canonical 8 from D-023 item (m); spec amends Spec §1 utility-label section to add `U_CCS` (call credit spread), `U_CSP` (cash secured put), `U_CC` (covered call), `U_LC` (long call), `U_LP` (long put) where applicable, OR Item 6 V0.2 ship explicitly enumerates which subset of canonical 8 is in V0.2 ship-scope. **Verification command:** Strategy ID list comparison; spec line 30–141 has 5 named strategy utility-label sections; migration has 10. Falsifiable. | open |
| B-AI-006-13 | AI-SPEC-006 §10.2 B13 | Spec §4.1 lines 384–388 (Training case weights: actual=1.0, rules-not-taken=0.6, counterfactual=0.4, synthetic=0.1) vs Spec §3.6 lines 339–342 (Memory case weights: actual=1.0, counterfactual=0.5, synthetic=0.2) | Two case-weight tables in same spec. | Internal inconsistency: counterfactual 0.4 vs 0.5; synthetic 0.1 vs 0.2. AND cross-spec drift: AI-SPEC-010 `case_weight` column (per AI-SPEC-010 §4 lines 198–225) mandates 1.0 / 0.5 / 0.2 — matches §3.6 ONLY, not §4.1. | Add Class B note: V0.2 ship scope must reconcile case-weight tables. Default position: harmonize §4.1 with §3.6 (drop the 0.6 rules-not-taken weight; use 1.0 / 0.5 / 0.2 across both training and memory analogs); preserve §4.1's `synthetic NOT for production` constraint as a separate flag. **Verification command:** Read Spec §4.1 + §3.6 verbatim; values differ. Falsifiable. | open |
| B-AI-006-14 | AI-SPEC-006 §10.2 B14 | Spec §4.1 + Spec §3.1 — un-executed candidate decisions have NO trade-time slippage features (`slippage_to_ev_ratio`, `estimated_slippage`) | Spec §4.1 says "every rules-proposed trade becomes a labeled candidate" but spec §3.1 reads trade-time computed fields. | For un-executed candidates, slippage fields would be NULL or replay-imputed; spec is silent on fallback. | Add Class B note: V0.2 ship scope must specify imputation policy for un-executed candidates: (a) replay-imputed via Item 5 spread filter + Item 10 slippage formula (bilateral with `B-AI-010-6`); OR (b) NULL with `slippage_imputed = false` flag in feature vector — meta-labeler reduces feature set by 2 for un-executed candidates; OR (c) drop un-executed candidates from training (revert §4.1 to "executed only"). Default position: option (a). **Verification command:** Read Spec §3.1 + §4.1; no fallback documented. Falsifiable. | open |
| B-AI-006-15 | AI-SPEC-006 §10.2 B15 | Spec §3.1 reads `engine_confidence` + `authority_level` from Item 5 EV Table — but Spec is silent on Item 5 reliability state degradation effect | When Item 5 reliability state is `degraded` or `halted` (per AI-SPEC-005 §5), what does Item 6 do? | Spec doesn't specify whether Item 6 down-weights, skips, or falls back when Item 5 reliability state degrades. Bilateral with AI-SPEC-005 §5 reliability state machine (`B-AI-005-10`). | Add Class B note: V0.2 ship scope must encode Item 5 reliability state as a meta-label input (e.g., `vol_engine_reliability_state` feature) AND/OR fall-back logic (e.g., when Item 5 is `halted`, Item 6 forces `meta_size_cap = 0` regardless of `p_take`). Default position: Item 5 `halted` triggers Item 6 advisory-only mode for that decision time-bucket. **Verification command:** Read Spec §3.1 + §3.7 + §9; no Item-5-reliability-state encoding documented. Falsifiable. | open |
| B-AI-006-16 | AI-SPEC-006 §10.2 B16 | Spec §0 line 17 (LLM consumes meta-labeler output via Item 1 Card) — Item 6 outputs flow into Item 1 Card schema | Bilateral with AI-SPEC-001 `B-AI-001-2` (Card schema delta from 5 fields to 18+ fields). | `synthesis_agent.py` Card output is currently 5 fields; Spec proposes 18+ field Card; Item 6 outputs (`p_take`, `meta_size_cap`, `reason_codes`) are 3 of the 18+. | Add Class B note: V0.2 ship scope coordinates with Item 1 V0.1 Card schema landing — Item 6's 3 Card fields land alongside Item 5's 9 Card fields per `B-AI-001-2`. Bundled bilateral. **Verification command:** Read AI-SPEC-001.md `B-AI-001-2` row; Card schema delta documented. Falsifiable. | open |
| B-AI-006-17 | AI-SPEC-006 §10.2 B17 | Spec §13 V0.2 Build Scope is silent on output Redis namespace (`meta:*`) | Spec is silent on whether engine outputs to a `meta:*` Redis namespace OR only to `meta_labeler_decisions` Supabase table. | At HEAD, no `meta:*` Redis keys exist; only `model:meta_label:enabled` Layer 1 control flag. | Add Class B note: V0.2 ship scope adds Redis output namespace `meta:*` (recommended canonical) for live consumers — Items 1 (Card features), 9 (Exit Optimizer reads meta-label authority), 13 (Drift Detection reads meta-label decisions). Specifically: `meta:p_take` (TTL = 60s), `meta:meta_size_cap` (TTL = 60s), `meta:reason_codes` (TTL = 60s), `meta:authority_level:{strategy}:{time_bucket}` (TTL = 3600s — per-scope authority granularity per Spec §9.4 lines 610–615), `meta:degradation_flags` (TTL = 3600s), `meta:rolling_30_brier` / `meta:rolling_50_brier` / `meta:rolling_ece` (TTL = 3600s), `meta:engine_updated_at` (TTL = 3600s, mirrors `gex:updated_at` pattern). **Bilateral with `B-AI-001-6` freshness substrate** — `meta:engine_updated_at` is the producer for Item 6's freshness gate; `_safe_redis()` caller wiring required. **Verification command:** `grep -rln "^meta:" backend/ --include="*.py"` shows existing `meta:*` Redis usage is limited to `model:meta_label:enabled`; no engine-output keys. Falsifiable. | open |
| B-AI-008-1 | AI-SPEC-008 §10.2 B1 | Spec §0 + §4 + §12 line 549 (`flow_alpha_engine` module — deterministic feature producer with separate Railway worker process) | No `flow_alpha_engine` file in `backend/` or `backend_agents/`. | Verified 2026-04-28 — `grep -rln "flow_alpha_engine\|FlowAlpha" backend/ backend_agents/` returns ZERO matches across production code (excluding `.venv` site-packages). Closest existing logic: `backend/databento_feed.py` (452 lines at HEAD; Layer 0 raw OPRA trade-stream consumer producing 3 Redis keys `databento:opra:trades` / `:gex_block` / `:confidence_impact`). | Add Class B note to Spec §12: engine file location is **`backend/flow_alpha_engine.py`** (recommended canonical name; matches AI-SPEC-005 `vol_fair_value_engine.py` per `B-AI-005-1` and AI-SPEC-006 `meta_labeler_engine.py` per `B-AI-006-1` per-spec naming pattern). Spec §12 V0.3 build scope explicitly names this module. Tests in `backend/tests/test_flow_alpha_engine.py`. | open |
| B-AI-008-2 | AI-SPEC-008 §10.2 B2 | Spec §3 lines 175–191 (`opra:flow_alpha:*` Redis namespace — 4 keys: `:latest` 15-min TTL, `:bars:1m` LTRIM 120 bars, `:bars:5m` LTRIM 80 bars, `:health` 5-min TTL) | All 4 keys named with TTLs but producer/consumer wiring unspecified. | Verified 2026-04-28 — `grep -rn "opra:flow_alpha\|opra:bbo\|opra:flow:" backend/ backend_agents/` returns ZERO matches. | Add Class B note: V0.3 ship scope adds `opra:flow_alpha:*` Redis namespace producer in `flow_alpha_engine.py`; consumer wiring at Items 5 + 6 + Governor sites. **Bilateral with `B-AI-008-16`** for `opra:flow_alpha:health` freshness substrate + `_safe_redis()` caller wiring at consumer sites. **Verification command:** Re-run V2 grep post-implementation. Falsifiable. | open |
| B-AI-008-3 | AI-SPEC-008 §10.2 B3 | Spec §3 lines 195–221 (`opra_flow_feature_bars` + `opra_flow_events` Supabase tables) | Two tables named with skeletal column lists; full DDL absent (no PK / NOT NULL / indexes / `calibration_eligible BOOLEAN` flag). | Verified 2026-04-28 — `ls supabase/migrations/ \| wc -l = 68`; `grep -rln "opra_flow_feature_bars\|opra_flow_events" supabase/` returns ZERO matches across all 68 migrations + `backend/` + `frontend/`. Spec is silent on `calibration_eligible` flag — Item 8 is NOT a 6th producer surface at spec level (theme stays at 5 surfaces: Items 2, 4, 5, 6, 10) BUT scheduled buildout SHOULD include the column for cross-spec hygiene. | Add Class B note: V0.3 ship scope adds 1 new migration for both tables with full DDL. **`opra_flow_feature_bars`:** `bar_id BIGSERIAL PRIMARY KEY`, `timestamp TIMESTAMPTZ NOT NULL`, `window_seconds INTEGER NOT NULL CHECK (window_seconds IN (60, 300, 900))`, `symbol TEXT NOT NULL`, `feature_version TEXT NOT NULL`, `dte_bucket TEXT NOT NULL CHECK (dte_bucket IN ('0DTE','1-3DTE','4-10DTE','10+DTE','all'))`, 20 production-feature NUMERIC columns, `features_jsonb JSONB`, `data_quality_flags JSONB`, `lee_ready_inferred_share NUMERIC`, `unknown_aggressor_share NUMERIC`, **`calibration_eligible BOOLEAN NOT NULL DEFAULT false`** (cross-spec substrate hygiene per scheduled buildout — Item 8 becomes 6th producer surface for the flag), `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. **`opra_flow_events`:** `event_id BIGSERIAL PRIMARY KEY`, `timestamp TIMESTAMPTZ NOT NULL`, `event_type TEXT NOT NULL CHECK (event_type IN ('sweep','block','burst'))`, `expiry DATE`, `strike NUMERIC`, `right CHAR(1) CHECK (right IN ('C','P'))`, `premium NUMERIC`, `aggressor_sign INTEGER CHECK (aggressor_sign IN (-1, 0, 1))`, `direction TEXT`, `confidence NUMERIC`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`. Recommended migration filename: `2026MMDD_add_opra_flow_substrate.sql`. | open |
| B-AI-008-4 | AI-SPEC-008 §10.2 B4 | Spec §1 lines 22–76 (the 20 production features `opra_premium_z_5m` through `flow_price_divergence_5m`) | All 20 features formula-defined with mathematical specifications. | Verified 2026-04-28 — `grep -rln "opra_premium_z_5m\|directional_pressure_5m\|customer_delta\|aggressor_sign\|sweep_premium_share\|key_strike_flow_concentration\|dealer_hedge_pressure_z" backend/ backend_agents/` returns ZERO matches across production code (V1). | Add Class B note: V0.3 ship scope implements all 20 features as methods on `flow_alpha_engine.py` (recommended) or as a feature-engineering module (`backend/opra_flow_features.py`). Each feature gets a unit test against synthetic OPRA tape data validating the formula. | open |
| B-AI-008-5 | AI-SPEC-008 §10.2 B5 | Spec §1 lines 33–46 (Lee-Ready aggressor inference + NBBO age tracking + `customer_delta = aggressor_sign * option_delta * size * 100`) | Tick-rule aggressor classification with quote-stale guard + 30s prior-tick lookup. | Verified 2026-04-28 — `grep -rln "lee_ready\|aggressor_sign\|nbbo_age\|quote_stale" backend/ backend_agents/` returns ZERO matches (V5). `databento_feed.py` parses raw trades but does NOT compute aggressor side, NBBO age, or option-delta-weighted customer_delta. | Add Class B note: V0.3 ship scope adds Lee-Ready aggressor inference as method `infer_aggressor_sign(trade, current_nbbo, prior_tick)` on `flow_alpha_engine.py`. Requires NBBO data class subscription + sub-second timestamp granularity from Databento (bilateral with `C-AI-008-3` subscription scope). Unit tests cover: trade above mid → +1; trade below mid → -1; trade within epsilon of mid → tick-rule lookup; NBBO_age > 1s → quote_stale = true → aggressor_sign = 0; locked/crossed market → aggressor_sign = 0; tick-rule fallback within 30s window. | open |
| B-AI-008-6 | AI-SPEC-008 §10.2 B6 | Spec §1 lines 114–126 (Sweep cluster definition: same underlying/expiry/strike/right; same aggressor_sign; within 100ms; price within 2 ticks; ≥3 exchanges OR ≥4 prints; cluster_premium ≥ threshold) | Multi-exchange print disambiguation requirement. | Verified 2026-04-28 — `grep -rln "sweep_cluster\|sweep_detection\|sweep_premium" backend/ backend_agents/` returns ZERO matches. | Add Class B note: V0.3 ship scope adds sweep cluster detection as method `detect_sweep_clusters(trades_buffer)` on `flow_alpha_engine.py`. Requires multi-exchange print disambiguation (Databento exchange-of-execution field) — bilateral with `C-AI-008-3` subscription scope. Unit tests cover: positive cluster (3 exchanges + within 100ms + same direction); negative cluster (only 2 exchanges); negative cluster (4 prints but >100ms span); negative cluster (cluster_premium below threshold). | open |
| B-AI-008-7 | AI-SPEC-008 §10.2 B7 | Spec §1 lines 128–133 (Block trade detection: single-print premium ≥ 95th percentile same-time-bucket OR size ≥ 95th percentile same-symbol trailing distribution) | Rolling percentile distributions per time-bucket and per-symbol. | Verified 2026-04-28 — `grep -rln "block_trade\|block_premium" backend/ backend_agents/` returns ZERO matches. | Add Class B note: V0.3 ship scope adds block trade detection as method `detect_block_trades(trade, time_bucket, symbol)` on `flow_alpha_engine.py`. Requires rolling percentile distributions per time-bucket and per-symbol (compute on engine startup from prior 20 trading days; refresh nightly). Unit tests cover: 95th percentile premium triggers block flag; 95th percentile size triggers block flag; sub-95th trades do not trigger. | open |
| B-AI-008-8 | AI-SPEC-008 §10.2 B8 | Spec §1 lines 142–148 (Quality tracking features: `lee_ready_inferred_share_5m` + `unknown_aggressor_share_5m` — explicit data-quality flags for downstream de-weighting) | Quality flags persist per-bar so downstream consumers can de-weight inferred aggregates. | Verified 2026-04-28 — `grep -rln "lee_ready_inferred_share\|unknown_aggressor_share" backend/ backend_agents/` returns ZERO matches. | Add Class B note: V0.3 ship scope adds quality-flag emission to `opra_flow_feature_bars.lee_ready_inferred_share` + `.unknown_aggressor_share` columns AND to `opra:flow_alpha:latest` JSONB output. Bilateral with Spec §4 lines 260–262 (`unknown_aggressor_share > threshold → reduce feature confidence`) — threshold value parametric (recommended 0.30 default; operator-tunable in `system-state.md`). | open |
| B-AI-008-9 | AI-SPEC-008 §10.2 B9 | Spec §4 line 247 (Architecture: separate Railway worker process to isolate latency risk from trading cycle) | Engine runs as separate Railway worker. | `databento_feed.py` runs inside main Railway service (verified by no separate Procfile entry; consumed by `backend/main.py` start-up wiring). The spec's separate-process discipline is unmet. | Add Class B note: V0.3 ship scope adds new Procfile entry `flow_alpha_worker: python -m backend.flow_alpha_engine` (or equivalent process manager wiring). Ensures feature computation latency does NOT block the trading cycle. | open |
| B-AI-008-10 | AI-SPEC-008 §10.2 B10 | Spec §4 lines 249–264 (Backpressure handling: feed bursts → preserve aggregate counters + drop noncritical event-detail logging + mark `data_quality = degraded`; lag > 5 seconds → downstream treats OPRA features as stale; `unknown_aggressor_share > threshold` → reduce feature confidence) | Multi-mode degradation state machine. | No backpressure handling at HEAD. `databento_feed.py` has no degraded-state machine, no 5-second staleness flag, no unknown_aggressor_share threshold. | Add Class B note: V0.3 ship scope adds `BackpressureMonitor` class on `flow_alpha_engine.py` with state machine (normal / degraded / stale). State changes trigger Redis flag updates (`opra:flow_alpha:health` JSONB with `state`, `lag_seconds`, `unknown_aggressor_share`); `_safe_redis()` consumer wiring at Items 5/6/Governor sites reads the flag and de-weights / falls back accordingly (`B-AI-008-16` bilateral). | open |
| B-AI-008-11 | AI-SPEC-008 §10.2 B11 | Spec §6 lines 322–334 (Replay process — `Reconstruct only prior OPRA trades; Emit features exactly as live engine would; Join to SPX bars, option chain, Item 5 EV row, Item 6 label`) | Walk-forward replay validation harness. | **Calendar-blocked on Item 4 V0.1** (`replay_eval_cases` table absent per `B-AI-004-1`); **bilateral with `C-AI-004-4`** chain-archive substrate; **bilateral with `B-AI-006-1`** Item 6 V0.2 ship for label provider. | Add Class B note: V0.3 ship scope replay validation is **calendar-blocked** on (a) Item 4 V0.1 ship + chain-archive substrate sufficiency per `C-AI-004-4`; (b) Item 6 V0.2 Layer 2 engine module ship per `B-AI-006-1`. V0.3 advisory-only ship (per `C-AI-008-4`) runs without replay validation; production-acceptance gate (Spec §6 lines 395–407) deferred to V0.4+. **Confirms cross-spec FK cascade theme in 6 audits.** | open |
| B-AI-008-12 | AI-SPEC-008 §10.2 B12 | Spec §6 lines 395–407 (Production acceptance: 90+ clean replay sessions; 30+ paper-shadow sessions; Item 6 + OPRA beats Item 6 alone joint test) | Production-acceptance gate requires Item 6 V0.2 calibration-grade ship + 90+ historical replay sessions + 30+ paper-shadow live sessions. | **Calendar-blocked** on Item 6 V0.2 ship per `B-AI-006-1`; chain-archive sufficiency per `C-AI-004-4`. | Add Class B note: V0.3 ship scope production acceptance is **calendar-blocked** — Item 8 V0.3 ships as feature-producer with logging only; production-acceptance flip to V0.4+ contingent on Item 6 V0.2 ship + chain-archive maturation + 90+/30+ session accumulation. | open |
| B-AI-008-13 | AI-SPEC-008 §10.2 B13 | Spec §5 lines 270–281 (Item 5 integration shim — 8 features) | Item 5 reads 8 specific OPRA features for vol fair-value engine. | **Bilateral with `C-AI-008-1`** — Item 5 spec is silent on the 8-feature contract at the field-name level (V7); Item 5 acknowledges OPRA flow abstractly only (lines 390 + 549). | Add Class B note: V0.3 ship scope provides Item 5 integration shim (8 features) as `opra:flow_alpha:latest` Redis JSONB output OR as `opra_flow_feature_bars` Supabase row read; cross-spec contract ratification via `C-AI-008-1` operator decision required before integration tests can be authored. | open |
| B-AI-008-14 | AI-SPEC-008 §10.2 B14 | Spec §5 line 290 (Item 6 integration shim — all 20 features + quality flags) | Meta-labeler reads all 20 features + 1 quality flag (per `A-AI-008-2` count correction). | **Bilateral with `C-AI-008-1`** — Item 6 spec is fully silent on Item 8 as a producer surface (V8 + V10 zero matches); Item 6's 47 named feature fields per `B-AI-006-2`–`B-AI-006-6` come from 5 producer surfaces with Item 8 NOT named. | Add Class B note: V0.3 ship scope provides Item 6 integration shim (20 features + 1 quality flag) as `opra:flow_alpha:latest` Redis JSONB output AND/OR as `opra_flow_feature_bars` Supabase row read; cross-spec contract ratification via `C-AI-008-1` operator decision required. **Calendar-blocked on Item 6 V0.2 ship** per `B-AI-006-1`. | open |
| B-AI-008-15 | AI-SPEC-008 §10.2 B15 | Spec §5 lines 305–313 (Governor compact-summary emission — 5-field JSON dictionary `flow_direction`, `flow_strength`, `vol_demand`, `sweeps`, `price_alignment`, `confidence`) | Compact summary for Risk Governor consumption. | No Governor compact-summary emission at HEAD. **Bilateral with Item 1 Card schema per `B-AI-001-2`** (Card schema delta from 5 fields to 18+ fields includes Item 8's compact summary). | Add Class B note: V0.3 ship scope provides Governor compact-summary emission as `opra:flow_alpha:governor_summary` Redis JSONB output (TTL = 5 minutes). Bilateral with Item 1 Card schema per `B-AI-001-2`. **`A-AI-008-2`-bilateral count check:** verify spec example shows 5 fields or 6; if 6, amend §5 framing from "Compact Summary Only" to "6-field JSON dictionary". | open |
| B-AI-008-16 | AI-SPEC-008 §10.2 B16 | Spec §3 line 189 (`opra:flow_alpha:health` TTL = 5 minutes) + Spec §4 lines 257–258 (`If lag > 5 seconds: downstream treats OPRA features as stale`) | Freshness substrate via `opra:flow_alpha:health` Redis key serving 5-second staleness handling at consumer sites. | **`_safe_redis()` is dead code at HEAD** (V4 — only def + docstring example caller in `prediction_engine.py:100` + `:120`); consumer wiring at Items 5/6/Governor sites absent. **Carry-forward #4 — 4th audit confirmation; HIGH IMPACT.** | Add Class B note: V0.3 ship scope adds (a) `opra:flow_alpha:health` producer (engine writes JSONB with `state`, `lag_seconds`, `last_emit_at`, `unknown_aggressor_share` every 5-second tick); (b) `_safe_redis()` caller wiring at Items 5/6/Governor sites for stale-flag consumption (or bilateral implementation per `B-AI-001-6` if `_safe_redis` design is replaced); (c) `system-state.md` `opra_flow_alpha.health_state` field for operator visibility. **Theme escalation:** Item 8's freshness substrate is **safety-critical for Cluster B alpha-generation tier** because stale OPRA features could silently inform Items 5/6/Governor decisions after feed lag. | open |
| B-AI-008-17 | AI-SPEC-008 §10.2 B17 | Spec §3 lines 175–191 + §3 line 193 (Storage namespace divergence) | Spec uses `opra:flow_alpha:*` namespace; HEAD uses `databento:opra:*` for legacy keys. Spec §3 line 193 `Commit 2 LTRIM fix already deployed handles existing databento:opra:trades` — applies LTRIM to **legacy** namespace, not new namespace. | Cutover policy unspecified: do `databento:opra:*` keys coexist with `opra:flow_alpha:*` (Layer 0 raw feed kept separate from Layer 2 alpha) OR are they superseded? | Add Class B note: V0.3 ship scope adopts **coexistence default** — `databento:opra:*` namespace remains as Layer 0 raw-feed substrate (continues serving GEX engine via `databento_feed.py:349 lpush`); `opra:flow_alpha:*` namespace ships as Layer 2 alpha-feature output. Cutover is therefore additive (no migration of `databento:opra:trades` consumers). **Folds into D-023 enrichment item (v) flow_agent / Layer 0 / Layer 2 cutover policy** per `C-AI-008-2`. | open |
| B-AI-008-18 | AI-SPEC-008 §10.2 B18 | Spec §1 lines 78–82 (Cross-DTE Derived Feature `zero_dte_minus_1_3dte_pressure`) + Spec §1 lines 86–112 (`flow_persistence_15m` formula) | Two derived features beyond the 20 production features list. | Both features absent at HEAD. Spec §1 line 72 confirms `flow_persistence_15m` IS feature #16 (not separate). `zero_dte_minus_1_3dte_pressure` is a separate cross-DTE derived feature beyond the 20. | Add Class B note: V0.3 ship scope adds `zero_dte_minus_1_3dte_pressure` to `opra_flow_feature_bars` as 21st column with explicit `is_derived = true` flag; `flow_persistence_15m` is implemented as feature #16 per spec. | open |

---

## 3. Class A Mechanical Corrections (Batch-Clear At Gate 1)

Per `CONSOLIDATED_PLAN_v1.2_APPROVED.md` §3 + template `_template.md` §10.1, Class A corrections are mechanical (wrong file paths, stale line numbers, wrong counts, naming inconsistencies). **Any AI corrects; Cursor verifies; rubber-stamped at end of audit phase (Gate 1).** NOT per-finding operator approval.

This section is a count-and-summary table. Full per-correction detail lives in each per-item redline's §10.1.

| Spec | Class A Count | Summary | Status |
|------|---------------|---------|--------|
| AI-SPEC-001 | 2 | **A1** — `backend_agents/` count: spec §24 says "8 specialist agents", actual count is 7 non-`__init__` `.py` files at HEAD `b0cd6b3` (`economic_calendar.py`, `feedback_agent.py`, `flow_agent.py`, `macro_agent.py`, `sentiment_agent.py`, `surprise_detector.py`, `synthesis_agent.py`) — correct to 7 OR define an explicit 8th not-yet-built agent. **A2** — Strategy-class taxonomy naming inconsistency: `risk_engine.py:105–106` uses `debit_call_spread` / `debit_put_spread`; `synthesis_agent.py:40–41` and MASTER_PLAN feature-flag boilerplate use `bull_debit_spread` / `bear_debit_spread`. Recommended: keep spec wording as canonical Governor enum; flag separate cleanup pass. | open |
| AI-SPEC-002 | 1 | **A1** — Spec §2 line 43 writes `position_id UUID NULL REFERENCES trading_positions(position_id)` but `trading_positions` PK is `id UUID` (per `supabase/migrations/20260416172751_*.sql:160`); no `position_id` column exists on the table. Correct to `position_id UUID NULL REFERENCES trading_positions(id)`. (Note: the spec's `strategy_attribution.position_id` field NAME is fine and self-consistent within the new table; only the FK target column is wrong.) | open |
| AI-SPEC-004 | 0 | **No Class A corrections.** Item 4's spec is pure forward-design and does not cite specific `backend/<file>.py:NN` line numbers, table primary-key column names, or other mechanically verifiable constants that could drift from repo reality. The cross-cutting findings around `paper_phase_criteria` schema, `counterfactual_pnl` table-vs-column shape, and decision-table canonical names are all captured as Class B (implementation-status drift) or Class C (architectural-intent decisions). | n/a |
| AI-SPEC-010 | 2 | **A1** — Spec §1 line 42 says `backend/counterfactual_engine.py (349 lines, 9 tests)`; actual at HEAD `1237746` is **406 lines** (`wc -l backend/counterfactual_engine.py = 406`; +57 lines from the `2400e98` baseline: +42 from `4970095` health/UI fix + 21 net from `fc64840` Section 13 batch). Correct line count to 406. **A2** — Spec §1 line 42 test count "9 passing tests"; actual at HEAD is **11 tests** (`grep -c "^def test_" backend/tests/test_counterfactual_engine.py = 11`; +2 tests from `4970095`: `test_run_counterfactual_job_writes_idle_health_on_success` line 337 + `test_run_counterfactual_job_writes_error_health_on_exception` line 363 — both part of Spec §13 Gap 4 fix already shipped). Correct test count to 11. **Note:** the A1+A2 drift is because the spec was locked 2026-04-25, 5 days AFTER `4970095` and `fc64840` shipped (both 2026-04-20) — spec's "What Already Exists" inventory is stale by those two commits. Spec §13 Gap 4 (health writes) is therefore already RESOLVED at HEAD; Spec §13 Gap 5 is partially resolved (`builtStatus: 'live'` shipped via `4970095`, but `shippedDate` + `shippedCommit` fields not added — tracked as `B-AI-010-4`). | open |
| AI-SPEC-005 | 0 | **No Class A corrections.** Item 5's spec is forward-design (HAR-RV regression model + EV table + reliability state machine + Monte Carlo simulator do not exist at HEAD). The spec does NOT cite specific `backend/<file>.py:NN` line numbers, line counts, or test counts that could mechanically drift from repo reality. The closest existing surface (`backend/polygon_feed.py:_append_spx_daily_return_if_due` writing `polygon:spx:realized_vol_20d` at line 486 — Layer 1 candidate per Cluster A theme #7) is an INPUT to a future HAR-RV model, not an existing implementation of the spec. The cross-cutting findings around D-016 governance interaction, multi-year RV history substrate, freshness gate substrate, and chain-archive bilateral with `C-AI-004-4` are all captured as Class B (implementation-status drift) or Class C (architectural-intent decisions). | n/a |
| AI-SPEC-006 | 0 | **No Class A corrections.** Item 6's spec is pure forward-design with a Layer 1 (`bf41175`) silence; it does not cite specific `backend/<file>.py:NN` line numbers, table primary-key column names, or other mechanically verifiable constants that could drift from repo reality. The Layer 1 cutover policy gap, producer-surface absences (47 fields across 5 producer surfaces), and authority-recovery escalation are all captured as Class B (implementation-status drift) or Class C (architectural-intent decisions). The closest "would-be Class A" candidate (Spec §13 V0.2 Build Scope silence on `bf41175` Layer 1 scaffold) is correctly classified as `C-AI-006-2` (architectural-intent governance escalation), not Class A (mechanical drift). | n/a |
| AI-SPEC-008 | 2 | **A1** — Spec §0 line 5 + §11 lines 530–533 declare tier as **V0.2** (`Tier (V0.2 short-vol-bias optimization)` + matrix §1 line 50 also marks V0.2); Spec §12 lines 552–561 V0.3 Build Scope however explicitly names V0.3 (`V0.3 Build Scope`); cross-internal-consistency drift between Spec §0 + §11 (V0.2) and Spec §12 (V0.3). **Recommended correction:** harmonize tier as **V0.3** (matches §12 buildout-commitment label which is the most operationally-binding section); amend §0 line 5 + §11 lines 530–533 + matrix §1 line 50 (per Output 3 CCEM tier-correction). Verified 2026-04-28 by direct read of `ITEM_8_OPRA_FLOW_ALPHA_LOCKED.md`. **A2** — Spec §5 lines 305–313 framing says `Compact Summary Only` (suggesting 1 dictionary) but the example dictionary shown in spec contains **6 fields** (`flow_direction`, `flow_strength`, `vol_demand`, `sweeps`, `price_alignment`, `confidence`), not 5; introduction-vs-example count drift. **Recommended correction:** harmonize §5 lines 305–313 framing to `6-field JSON dictionary` (matches the example verbatim); preserves the canonical compact-summary contract Item 1 Card schema delta references via `B-AI-001-2`. Verified 2026-04-28 by direct read of `ITEM_8_OPRA_FLOW_ALPHA_LOCKED.md`. | open |

**Total Class A corrections in audit phase:** 5 (unchanged — AI-SPEC-006 contributes 0; will grow as remaining audits complete)

---

## 4. Resolved Findings

When the operator decides on a Class C item, when a Class B is approved-and-applied (Phase 2), or when a Class A is batch-cleared at Gate 1, **MOVE** the entry from sections 1–3 to here. Include `Resolution Date` (when), `Resolution` (operator-approved decision text or applied-correction reference), and `Notes` (link to the PR / commit / D-XXX entry that closes it).

| ID | From | Resolution Date | Resolution | Notes |
|----|------|-----------------|------------|-------|
| C-AI-002-2 | AI-SPEC-002 §10.3 C2 | 2026-04-26 | **Resolved by AI-SPEC-010 audit (P1.3.4) in favor of option 1.** AI-SPEC-010 spec §4 lines 198–225 authoritatively ships `trade_counterfactual_cases` as a NEW table (BIGSERIAL PRIMARY KEY, FK `source_decision_id` references `trading_prediction_outputs(id)`, full case-typed schema with `case_type`, `case_weight`, `simulation_status`, `calibration_eligible`, `counterfactual_pnl_after_slippage` for training-grade labels, etc.). Existing 3-column triple (`counterfactual_pnl` / `counterfactual_strategy` / `counterfactual_simulated_at` on `trading_prediction_outputs` per `20260421_add_counterfactual_pnl.sql:8–14`) deprecates to **Layer 1 legacy observability surface** with `label_quality = 'legacy_observability_only'` lockdown applied via Spec §6 lines 423–432 cutover migration (anchor pending operator decision on `C-AI-010-5`). Cross-spec implications: AI-SPEC-002 `B-AI-002-7` resolves transitively (table is the canonical surface; Item 2's `strategy_attribution.case_id` references the new table); AI-SPEC-004 `B-AI-004-8` resolves (Item 4 reads the new table per spec §1 line 37 — wording stays as-is); PRE-P11-4 carry-forward resolves (column-not-table debt closed by Layer 1/Layer 2 split — Layer 1 = columns, Layer 2 = table). | AI-SPEC-010 redline at `trading-docs/08-planning/ai-architecture-audits/AI-SPEC-010.md` §3.2 + §7.2 + §8 (carry-forward #5 marked AUTHORITATIVE). Cross-references: AI-SPEC-002 `B-AI-002-7` (resolved transitively), AI-SPEC-004 `B-AI-004-8` (resolved), PRE-P11-4 carry-forward (resolved). |

---

## 5. Cross-Spec Themes

Findings that appear in multiple audits OR governance debt that affects multiple specs. Update as themes emerge across audits. Status `open — confirmed in N audit(s)` is incremented as subsequent audits confirm; status changes to `closed` only when the underlying debt is resolved (Phase 2 correction applied, governance change ratified, or theme superseded by a Phase 3 buildout).

| Theme | Affected Specs | Description | Status |
|-------|----------------|-------------|--------|
| Strategy-class taxonomy naming inconsistency | AI-SPEC-001 (confirmed, A2); AI-SPEC-002 (confirmed — uses `debit_call_spread` / `debit_put_spread` matching `risk_engine.py:105–106` and migration `20260419_add_strategy_types.sql:11–22`, NOT the `bull_debit_spread` / `bear_debit_spread` alternative); AI-SPEC-004 (confirmed — Spec §9 line 916 silently honors canonical naming `debit_call_spread` / `debit_put_spread` in strategy sample-size tier table; carry-forward PRE-P11-7 silently honored); AI-SPEC-010 (confirmed — Spec §4 line 207 (`strategy_used TEXT`) and Spec §3 lines 174–186 (default-to-iron_condor fallback critique at `counterfactual_engine.py:213–223`) both reference strategy taxonomy; spec wording is silent on canonical naming choice but the existing Layer 1 hardcodes `iron_condor` literal which matches the canonical DB enum); AI-SPEC-005 (confirmed via `B-AI-005-9` — Spec §1 lines 211–293 EV table strategy enumeration lists 8 strategies (`iron_condor`, `iron_butterfly`, `put_credit_spread`, `call_credit_spread`, `cash_secured_put`, `covered_call`, `long_call`, `long_put`) using canonical naming consistent with DB CHECK constraint and `counterfactual_engine.py` literal; alignment with Item 2's `outcome_mode` per-strategy enums and forward-reference Item 6 strategy classes required); AI-SPEC-006 (confirmed via `B-AI-006-12` — Spec §1 lines 30–141 enumerates only 5 strategies (`iron_condor`, `iron_butterfly`, `put_credit_spread`, `long_straddle`, `debit_call_spread`) — strict subset of Item 5's canonical 8, missing `call_credit_spread`, `cash_secured_put`, `covered_call`, `long_call`, `long_put`; AND inconsistent membership with `long_straddle` + `debit_call_spread`; reconciliation required to canonical 8 per D-023 enrichment item (r); migration `20260419_add_strategy_types.sql:11–22` confirms 10 canonical DB CHECK values (`iron_condor`, `iron_butterfly`, `put_credit_spread`, `call_credit_spread`, `cash_secured_put`, `covered_call`, `debit_call_spread`, `debit_put_spread`, `long_call`, `long_put`)); likely also AI-SPEC-007, AI-SPEC-011, AI-SPEC-012 (per `CROSS_CUTTING_EVIDENCE_MATRIX.md` §3 strategy-enum touchpoints) | `risk_engine.py:105–106` uses `debit_call_spread` / `debit_put_spread`; `synthesis_agent.py:40–41` + `MASTER_PLAN.md` feature-flag boilerplate use `bull_debit_spread` / `bear_debit_spread`. System-wide cleanup task; not Item 1's, Item 2's, Item 4's, Item 5's, Item 6's, or Item 10's scope to fix individually. AI-SPEC-002 + AI-SPEC-004 + AI-SPEC-010 + AI-SPEC-005 + AI-SPEC-006 confirmations strengthen the case that `debit_call_spread` / `debit_put_spread` is the canonical naming (matches DB CHECK constraint, existing `counterfactual_engine.py` literal, AND Item 5 EV table strategy enumeration). **AI-SPEC-006 P1.3.6 audit adds the strongest internal-consistency case**: Item 6 is THE keystone admissibility decision-maker per architectural principle 1; its strategy taxonomy must match Items 2 + 5 exactly for the per-strategy utility-label calculator (`U_IC`, `U_IB`, `U_PCS`, etc. per `B-AI-006-8`) to align with the canonical DB enum AND for the meta-labeler decision rows in `meta_labeler_decisions.strategy_type` per `B-AI-006-11` to round-trip with `strategy_attribution.strategy_type` and `vol_fair_value_snapshots.strategy_type` (Item 5 EV table strategies). | open — confirmed in 6 audits |
| `_safe_redis()` dead code (carry-forward #4) | AI-SPEC-001 (confirmed high-impact, B-AI-001-6); AI-SPEC-002 (ruled out — Item 2 is EOD batch over closed positions, no freshness-gate dependency); AI-SPEC-004 (ruled out — Item 4 is offline replay over historical archives, no live freshness gate); AI-SPEC-010 (ruled out — Item 10 Layer 1 + Layer 2 are EOD batch jobs over the prior trading session; no live data-freshness gates required; `counterfactual_engine.py` does not import `_safe_redis` and does not stamp freshness flags on counterfactual rows); AI-SPEC-005 (**confirmed high-impact**, `B-AI-005-16` — HAR-RV engine reads live volatility inputs (`polygon:spx:realized_vol_20d`, `polygon:vix:z_score`, `polygon:vvix:z_score`, possibly `gex:updated_at`); freshness gate is essential for runtime correctness; without it, stale Redis values would silently feed HAR-RV regression and propagate to EV table + Risk Governor + Meta-Labeler; bilateral with AI-SPEC-001 `B-AI-001-6` — same substrate buildout serves both items); AI-SPEC-006 (**confirmed high-impact**, `B-AI-006-17` — Item 6 V0.2 produces `meta:engine_updated_at` Redis key as the producer for Item 6's freshness gate; mirrors Item 5's `vol:engine_updated_at` and Item 1's `gex:updated_at` pattern; `_safe_redis()` caller wiring required at consumer sites (Items 1 / 9 / 13 reading `meta:p_take`, `meta:meta_size_cap`, `meta:reason_codes` for trade authorization decisions); **without the freshness gate, stale meta-labeler outputs could silently authorize trades after the engine has degraded — a CRITICAL runtime correctness gap given Item 6's authority over admissibility per architectural principle 1**); potential AI-SPEC-013 (drift-detection freshness) | Function defined at `prediction_engine.py:100` but never called at HEAD (verified 2026-04-27 — `grep -n "_safe_redis(" backend/*.py` returns only the definition at `prediction_engine.py:100–131`). Any spec relying on freshness gates must include freshness substrate buildout (caller wiring + `gex:updated_at` producer + `gex:atm_iv` producer + `polygon:spx:updated_at` producer + `polygon:vix:updated_at` producer + `vol:engine_updated_at` producer per `B-AI-005-16` + `meta:engine_updated_at` producer per `B-AI-006-17`). **Theme escalates from "open governance debt" to "load-bearing safety substrate" with Item 6 P1.3.6 audit confirmation** — Item 6 has admissibility authority; freshness gate is now safety-critical, not just observability. **AI-SPEC-008 P1.3.7 confirms 4th HIGH-IMPACT consumer** — `B-AI-008-16` adds `opra:flow_alpha:health` Redis key (TTL = 5 minutes, JSONB with `state` / `lag_seconds` / `last_emit_at` / `unknown_aggressor_share`); Item 8's 5-second backpressure handling per Spec §4 lines 257–258 (`If lag > 5 seconds: downstream treats OPRA features as stale`) makes freshness substrate even more time-sensitive than Items 1/5/6 (60-second TTL Redis keys). Without `_safe_redis()` caller wiring at Items 5/6/Governor consumer sites for `opra:flow_alpha:*` keys, stale OPRA features could silently inform Items 5/6/Governor decisions after Databento feed lag — bilateral with `B-AI-001-6` + `B-AI-005-16` + `B-AI-006-17` HIGH-IMPACT confirmations. **Theme is now safety-critical for the entire Cluster B alpha-generation tier.** | open — confirmed in 4 audits HIGH-IMPACT, ruled out in 3 audits |
| `calibration_eligible` flag missing | AI-SPEC-001 (confirmed as **consumer**, B-AI-001-5); AI-SPEC-002 (confirmed as **producer** — `calibration_eligible BOOLEAN NOT NULL DEFAULT false` on `strategy_attribution` per Spec §2 line 120; structurally enforced by trigger + application accessor + meta-labeler training accessor); AI-SPEC-004 (confirmed as **dual surface** — Item 4 is BOTH a consumer of Item 2's `strategy_attribution.calibration_eligible` (Spec §1 line 36 + §12 line 1068) AND a producer of its own `replay_eval_cases.calibration_eligible` per Spec §1 line 88–90 / §4 lines 388–432, used as the canonical surface for Spec §12 "≥200 decision cards" gate per `C-AI-004-2`); AI-SPEC-010 (confirmed as **third producer surface** — Item 10 Layer 2's `trade_counterfactual_cases.calibration_eligible BOOLEAN` per Spec §4 line 213 is the canonical surface for counterfactual labels (`B-AI-010-5`); structurally enforced via Spec §6 lines 391–410 API accessor functions `get_meta_labeler_training_data()`, `get_vol_engine_replay_cases()`, `get_adversarial_block_outcomes()`, `get_exit_optimizer_training_paths()` that filter on `calibration_eligible = true` + `simulation_status = 'success'`); AI-SPEC-005 (confirmed as **consumer + new producer surface**, `B-AI-005-5` + `B-AI-005-7` + `B-AI-005-15` — Item 5 V0.2 reads from Items 2 + 10 calibration_eligible substrate (consumer side) AND produces its own `vol_fair_value_snapshots.calibration_eligible BOOLEAN` per `B-AI-005-7` recommended schema (FOURTH producer surface, scheduled for V0.2 ship); ALSO produces `spx_realized_vol_history.calibration_eligible BOOLEAN` per `B-AI-005-15` recommended schema for pre-fix-`fc64840` row marking — Pre-B1-2 contamination handling mirrors Item 10 `C-AI-010-2` resolution pattern); AI-SPEC-006 (confirmed as **consumer + 5th producer surface (its own decisions)**, `B-AI-006-10` + `B-AI-006-11` — Item 6 V0.2 reads from ALL FOUR upstream producer surfaces (Items 2 + 4 + 5 + 10) filtered on `calibration_eligible = true`; ALSO produces its own `meta_labeler_decisions.calibration_eligible BOOLEAN NOT NULL DEFAULT false` per `B-AI-006-11` recommended schema (FIFTH producer surface, scheduled for V0.2 ship — Item 6's own training data carries the flag for downstream Items 9 + 13); training-set assembly uses `calibration_eligible = true` filter on `strategy_attribution`, `trade_counterfactual_cases` AND `simulation_status = 'success'`, `replay_eval_cases`, `vol_fair_value_snapshots`); likely also AI-SPEC-009, AI-SPEC-012 (per matrix §3 calibration-input touchpoints — all consumers waiting for Items 2 + 4 + 5 + 6 + 10 V0.x ships) | Originally framed as "depends on Item 10 deliverable" in P1.3.1; AI-SPEC-002 corrected this — Item 2 is producer for trade attribution. AI-SPEC-004 P1.3.3 added a SECOND producer surface — Item 4's `replay_eval_cases.calibration_eligible` is the canonical card surface for cross-spec promotion-gate counting. AI-SPEC-010 P1.3.4 adds the THIRD producer surface — Item 10 Layer 2's `trade_counterfactual_cases.calibration_eligible` is the canonical surface for counterfactual labels feeding Items 5/6/7/9. AI-SPEC-005 P1.3.5 adds the FOURTH producer surface — Item 5's `vol_fair_value_snapshots.calibration_eligible` + `spx_realized_vol_history.calibration_eligible` are the canonical surfaces for vol-engine outputs and pre-B1-2 contamination handling. **AI-SPEC-006 P1.3.6 adds the FIFTH producer surface** — Item 6's `meta_labeler_decisions.calibration_eligible` is the canonical surface for meta-labeler decision audit trail; Item 6 is also the **deepest cross-spec cascade consumer** (reads from ALL FOUR upstream producer surfaces: Items 2 + 4 + 5 + 10). Cross-spec sequencing constraint: Items that consume this flag (1, 6, 7, 9, 12, 13) are calendar-blocked on Items 2 + 4 + 5 + 10 V0.x ships; Items 9 + 13 additionally calendar-blocked on Item 6 V0.2 ship. Layer 1 legacy `counterfactual_pnl` columns on `trading_prediction_outputs` do NOT carry the flag — they are observability-only (resolved by `C-AI-002-2`). | open — 5 producer surfaces + consumers identified across 6 audits |
| `_RISK_PCT` non-monotonic ladder (evidence pack §9.4) | AI-SPEC-001 (must not worsen, B-AI-001-7); AI-SPEC-002 (not applicable — Item 2 is observational labeling, no sizing concerns); AI-SPEC-004 (not applicable — Item 4 is research-only offline replay, no live sizing concerns); AI-SPEC-012 (Item 12 owns ladder fix); AI-SPEC-013 (drift-triggered demotion path interacts with ladder) | Phase 1 (0.010) > Phase 2 (0.0075) at HEAD `risk_engine.py:79–84`. Pre-existing governance debt re-introduced by 2026-04-20 SPX width widening. Matrix §4 proposed module-level merge order **1 → 13 → 12** for `risk_engine.py`. | open — pre-existing debt (predates audit phase) |
| `pgvector` / embedding infrastructure missing | AI-SPEC-001 (confirmed, B-AI-001-1); AI-SPEC-002 (not applicable — Item 2 has no embedding dependency); AI-SPEC-004 (not applicable — Item 4 is offline replay over archived data, no case retrieval / embedding dependency); likely also AI-SPEC-003 (case retrieval), AI-SPEC-007 (any case-based reasoning) | `pgvector` extension not enabled in any of 68 migrations at HEAD. Several specs assume case-store with embeddings exists. Pre-V0.1 implementation prerequisite, not existing capability. | open — confirmed in 1 audit |
| Cross-spec FK dependency cascade | AI-SPEC-001 (FK target `ai_governor_decisions(decision_id)` referenced by Item 2 + Item 4; canonical name proposed in `B-AI-001-4`); AI-SPEC-002 (confirmed, B-AI-002-3 + B-AI-002-7 — Item 2 has 4 FK references into Items 1 / 6 / 7 / 10 unshipped tables); AI-SPEC-004 (confirmed, B-AI-004-7 + B-AI-004-8 — Item 4 reads from Items 1 / 2 / 10 unshipped tables; calibration-grade ship blocked on Items 1 + 2 schema-landing milestones); AI-SPEC-005 (confirmed via `B-AI-005-2` + `B-AI-005-8` + `B-AI-005-12` — Item 5 V0.2 calibration-grade output reads from Items 2 (`strategy_attribution.calibration_eligible`) + 10 (`trade_counterfactual_cases.calibration_eligible`) substrates; LightGBM tilt is calendar-blocked on Items 1 + 6 schema-landing milestones; replay calibration harness is calendar-blocked on Item 4 V0.1; Item 5 also produces NEW tables (`vol_fair_value_snapshots`, `strategy_ev_table`, `spx_realized_vol_history`, `vol_engine_reliability_log`) that Items 1 + 6 will FK-reference (forward dependency); calibration-grade ship blocked on Items 2 + 10 V0.x landings — same flavor as Item 4); AI-SPEC-006 (**confirmed as the deepest cross-spec cascade consumer**, `B-AI-006-2` + `B-AI-006-5` + `B-AI-006-6` — Item 6 V0.2 reads from FIVE upstream producer surfaces totaling 47 named fields: 18 fields from Item 5 EV Table (calendar-blocked on Item 5 V0.2), 5 fields from Rules Engine (`backend/strategy_selector.py` extension), 5 fields from LightGBM Direction Model (calendar-blocked on `C-AI-006-4` operator decision — possibly new spec AI-SPEC-014), 9 fields from Item 1 AI Risk Governor structured-fields surface (calendar-blocked on Item 1 V0.1), 10 fields from Item 7 Memory Retrieval substrate (calendar-blocked on un-audited Item 7 future P1.3.7); Item 6 ships canonical FK provider `meta_labeler_decisions(decision_id UUID PK)` per `B-AI-006-11` referenced by Items 9 (Exit Optimizer) + 13 (Drift Detection); Item 6's `meta_labeler_decisions.case_id` references `trade_counterfactual_cases(id)` from Item 10 V0.2 per Item 10 V0.2 schema; **Item 6 calendar-blocker depth: 5 upstream specs (Items 1, 2, 4, 5, 7) + Item 10 V0.2 Layer 2 transitively**); AI-SPEC-007 (FK target — canonical name TBD in P1.3.7 audit); AI-SPEC-010 (**confirmed as canonical FK provider** — Item 10 V0.2 ships `trade_counterfactual_cases(id BIGSERIAL PRIMARY KEY)` per Spec §4 lines 198–225; Items 2 / 4 / 5 / 6 / 9 reference this table via `case_id`; Item 10 itself reads from Item 2's `strategy_attribution(id)` for the per-trade strategy-utility-label mapping per Spec §4 lines 232–269 + Spec §6 lines 380–389 EOD orchestration; **bilateral FK between Item 2 and Item 10**) | Multiple specs (Items 2, 4, 5, 6, 7, 9) have FK / read dependencies into tables that don't exist at HEAD. Sequencing tension: Items 2 + 4 + 10 V0.2 are critical-path roots per matrix §7, yet structurally depend on each other (bilateral Item 2 ↔ Item 10 via `strategy_attribution.case_id` ↔ `trade_counterfactual_cases.source_decision_id` cross-FKs) and on Items 1 / 6 / 7 schema landings. **AI-SPEC-006 P1.3.6 audit confirms Item 6 as the deepest cross-spec cascade consumer** — calendar-blocker depth of 5 upstream specs (Items 1, 2, 4, 5, 7) plus transitive dependency on Item 10 V0.2 Layer 2 — making Item 6 V0.2 the LAST Cluster B alpha-generation phase to ship (Phase 4B per AI-SPEC-006 §11 governance updates); Items 9 + 13 V0.x ship AFTER Phase 4B (consume Item 6 outputs). Resolution per B-AI-002-3: split migrations into (a) initial without cross-FK constraints, (b) follow-up adding constraints once dependent specs ship. Item 4 mirrors: scaffolding ship can land independently, calibration-grade ship blocked on dependent schemas. Item 5 mirrors: scaffolding ship (engine + EV table + reliability state machine) can land independently, calibration-grade ship blocked on Items 2 + 10 + chain-archive substrate (`C-AI-004-4` bilateral). Item 10 V0.2 Layer 2 ships table BEFORE Items 5 / 6 / 7 / 9 since Cluster B/C audits depend on Layer 2 outputs. **Item 6 V0.2 ships LAST in Cluster B since it reads from the most upstream producer surfaces of any spec.** | open — confirmed in 5 audits (P1.3.2 + P1.3.3 + P1.3.4 + P1.3.5 + P1.3.6) |
| `closed_trade_path_metrics` substrate gap | AI-SPEC-002 (confirmed, B-AI-002-2 — Spec §6 Phase 1 step 2.a copies path metrics from this nonexistent table; spec §15 V0.1 ship scope does NOT list building it); AI-SPEC-004 (confirmed indirectly, B-AI-004-9 — Item 4 reads path metrics during reconstruction (Spec §2 Step 0 Test A pricing reconstruction; §2 Step 1 Item 5 Vol calibration likely needs path metrics for vol-edge validation); reconstruction quality bounded by Item 2 V0.1 ship scope on path metrics); AI-SPEC-010 (confirmed, B-AI-010-2 — Item 10 V0.2 Spec §4 lines 276–294 ships `closed_trade_path_metrics` as a NEW table OWNED by Item 10 (proposed schema with `position_id UUID NULL`, `mae_R NUMERIC`, `mfe_R NUMERIC`, `peak_unrealized_pnl NUMERIC`, `path_samples_count INTEGER`, etc.); **Item 10 is the canonical owner**, not Item 2; Item 2's per-trade path metrics rows on `strategy_attribution` are copy-on-insert from this Item-10-owned table); likely also AI-SPEC-009 (Exit Optimizer reads path metrics from `strategy_attribution`, transitively depends on this substrate — per matrix §3 line 294) | **Resolved-by-AI-SPEC-010-V0.2-ship.** The path-metrics source-of-truth table is referenced by Item 2's spec at §6 + §7 + Item 4's spec implicitly via reconstruction tests; **AI-SPEC-010 V0.2 Spec §4 lines 276–294 ships it as a new Item-10-owned table** alongside the new `position_path_samples` table for raw per-minute samples (Spec §4 lines 299–308). Resolution per `B-AI-010-2` + `B-AI-002-2`: Item 2's V0.1 ship can either (a) wait for Item 10 V0.2 to land the substrate (preferred — single owner), or (b) ship V0.1 with NULL path-metric placeholders, with copy-on-insert from Item 10's substrate added once Item 10 V0.2 lands. **Default position: option (a)** — Item 10 V0.2 Layer 2 ships first (sequenced before Item 2 V0.1 in Phase 4). | open — owner identified (AI-SPEC-010); will close on AI-SPEC-010 V0.2 ship |
| Decision outcome enum coordination across authority surfaces | AI-SPEC-001 (writer — `blocked_governor` for vetos, ambiguous for size-cap channel per `C-AI-001-1`); AI-SPEC-002 (owner of `decision_outcome` CHECK constraint, see `C-AI-002-1`); AI-SPEC-004 (writer/consumer — Item 4's `replay_eval_cases.realized_outcome JSONB` (Spec §4 line 408) must internally use Item 2's enum, see `B-AI-004-9`/`B-AI-004-10` reference and §8 P1.3.2-T3 carry-forward); AI-SPEC-005 (consumer-of-strategy-taxonomy via `B-AI-005-9` — Item 5's EV table strategy enumeration uses 8 strategy IDs that must align with Item 2's `outcome_mode` per-strategy enums and Item 6's strategy classes; Item 5 does NOT write to `decision_outcome` directly — it produces `vol_fair_value_snapshots` rows + EV table top-3 ranking — but its strategy-taxonomy IDs MUST match the canonical enum names); AI-SPEC-006 (**confirmed as writer**, `B-AI-006-9` + `B-AI-006-12` — Item 6 writes `blocked_meta_labeler` to `strategy_attribution.decision_outcome` when meta-labeler skips a trade; Item 6 writes `reduced_meta_labeler` (or equivalent) when meta-labeler reduces size below normal sizing per `B-AI-006-9`; Item 2 enum CHECK constraint extension required in same migration as Item 6 V0.2 schema-landing per D-023 enrichment item (t); also confirms strategy-taxonomy alignment via `B-AI-006-12` — Item 6's per-strategy utility-label calculator (`U_IC`, `U_IB`, `U_PCS`, etc.) must align with Items 2 + 5's canonical 8-strategy enumeration; AND Item 6's `meta_labeler_decisions.strategy_type TEXT NOT NULL` per `B-AI-006-11` recommended schema must round-trip with Item 2's `strategy_attribution.strategy_type`); AI-SPEC-007 (writer — `blocked_adversarial`); AI-SPEC-010 (parallel vocabulary — owns `counterfactual_reason` enum (6 values: `no_trade | governor_block | meta_skip | adversarial_block | halt_block | alternative_strategy` per Spec §4 line 209–210) for the `trade_counterfactual_cases` table; **bilateral with Item 2's `decision_outcome` enum AND with Item 6's `meta_skip` value (which now maps to Item 6's `blocked_meta_labeler` decision_outcome per `B-AI-006-9`)**; mapping table required between the two vocabularies per `B-AI-010-10`) | Item 2's `decision_outcome` enum has 8 values that downstream Items 1, 4, 6, 7 will write into. The `size_multiplier_cap` channel proposed in AI-SPEC-001 §7 has no corresponding enum value — it is neither `opened_traded` nor `blocked_governor`. Item 4 must additionally encode the enum inside its `realized_outcome JSONB` for replay cards. Item 10's `counterfactual_reason` (6 values, per-counterfactual-case scope) is an INDEPENDENT vocabulary from Item 2's `decision_outcome` (8 values, per-decision scope) but the two share concepts (`blocked_governor` ↔ `governor_block`; `halted_blocked_cycle` ↔ `halt_block`; `blocked_meta_labeler` ↔ `meta_skip`). Item 5's EV table strategy enumeration extends the cross-spec coordination to STRATEGY-TAXONOMY alignment (8 strategy IDs across Items 2 + 5 + 6 must match canonical DB CHECK enum names). Item 6's writer commitment (`blocked_meta_labeler` + `reduced_meta_labeler`) closes the previously-pending writer entry for Item 6 in this theme. Without explicit coordination, both Item 1 size-cap writes, Item 4 replay-card outcome encoding, Item 5 EV table strategy IDs, Item 6 meta-labeler writes, AND Item 10 counterfactual_reason rows will diverge. Resolution folds into D-023 wording (proposed in AI-SPEC-001 §11 covering the AI authority boundary), now extended with explicit `counterfactual_reason ↔ decision_outcome` mapping table per `B-AI-010-10` AND canonical strategy-taxonomy enumeration in TASK_REGISTER §14 cross-cutting reference per `B-AI-005-13` AND Item 6 writer commitment per D-023 enrichment item (t) per `B-AI-006-9`. | open — confirmed in 5 audits |
| Legacy `attribution_*` columns on `trading_positions` (architectural commitment violation) | AI-SPEC-002 (confirmed, `C-AI-002-3`); AI-SPEC-004 (not applicable — Item 4 reads `trading_positions` for closed-trade replay but does not interact with the dead `attribution_*` columns); potentially AI-SPEC-009 / AI-SPEC-010 / AI-SPEC-011 if they query `trading_positions` for these columns | `trading_positions` baseline migration `20260416172751_*.sql:203–206` adds 4 BOOLEAN attribution columns (`attribution_direction`, `attribution_structure`, `attribution_timing`, `attribution_vol`) that no production code path writes (`grep -rln` returns zero matches in `backend/`). Spec §1 architectural commitment ("trading_positions remains the execution ledger only — Don't overload it with attribution") directly contradicted by HEAD reality. Resolution per `C-AI-002-3`: deprecate-in-place (default), formally drop, or repurpose. | open — confirmed in 1 audit, n/a in 1 audit |
| `paper_phase_criteria` vs `item_promotion_records` authority overlap | AI-SPEC-004 (introduced via `C-AI-004-1` — Spec §4 lines 465–498 proposes per-item authority gate `item_promotion_records` operating alongside existing `paper_phase_criteria` (12 GLC criteria per D-013, system-wide go-live gate, migration `20260417000001_paper_phase_criteria.sql`); Spec is silent on coordination); future audits AI-SPEC-001 / AI-SPEC-005 / AI-SPEC-006 (consumers — read `item_promotion_records` at startup per Spec §4 line 521–525); D-013 (governance touchpoint — paper_phase_criteria authority anchor); pending D-023 (governance touchpoint — per-item authority promotion encoded here per `C-AI-004-3`) | Two independent gate authorities: `paper_phase_criteria` (system-level "is paper phase complete?" gate, 12 GLC criteria, D-013-anchored) and `item_promotion_records` (per-item "what authority does this item have?" gate, proposed by Item 4). Without explicit coordination wording, ambiguity arises about whether GLC failures block per-item promotions, whether `item_promotion_records.authority_level = 'production'` requires GLC-005 (Sharpe ≥ 1.5) to pass first, etc. Default position per consolidated plan §5 conservative-first principle: option 1 (independent gates with explicit non-conflict statement; preserves D-013 unchanged). Operator decision required at audit close. | open — emerged in P1.3.3 |
| Decision card definition / counting ambiguity (Spec §12 200-card threshold) | AI-SPEC-001 (introduced via `B-AI-001-8` — Spec §12 V0.2 paper-binding gate requires "≥ 200 replay/advisory decision cards"; ambiguous source); AI-SPEC-004 (resolved candidate via `C-AI-004-2` — Spec §4 lines 388–432 defines `replay_eval_cases.calibration_eligible = true` as the canonical card surface; explicit recommendation that `shadow_predictions` rows do NOT count); pending D-023 (governance — encoded as part of D-023 wording per AI-SPEC-001 §11 + AI-SPEC-002 §11 addendum + AI-SPEC-004 §11) | Cross-spec counting contract ambiguity: at least three candidates for "decision card" exist (`replay_eval_cases.calibration_eligible = true` rows from Item 4; `shadow_predictions` rows from `backend/shadow_engine.py`; some "advisory" surface that doesn't yet exist). Without explicit coordination, the AI-SPEC-001 §12 gate cannot be evaluated. Default position: option 1 (canonical = `replay_eval_cases.calibration_eligible = true`; matches Spec §1 line 49 hard rule "No archived chain = no calibration-grade option pricing replay"). Folds into existing pending D-023 wording. | open — emerged in P1.3.3 |
| Archived option-chain data substrate (V0.1 hard prerequisite) | AI-SPEC-004 (introduced via `C-AI-004-4` + `B-AI-004-3` — Spec §1 line 47–66 establishes "Hard Rule: No Theoretical Pricing — No archived chain = no calibration-grade option pricing replay"; at HEAD `tradier_feed.py` does not persist chains, no `tradier_chain_archive` migration in 68 migrations, `databento:opra:*` keys are 300s-TTL Redis); AI-SPEC-010 (confirmed bilateral, B-AI-010-6 — Item 10 V0.2 Layer 2 slippage formula `half_spread_estimate * f(vix_z, spread_z, time_of_day, size)` per Spec §5 lines 332–376 requires archived option-chain data for `half_spread_estimate` baseline; **Layer 2's after-slippage P&L is calibration-grade-capable only when the chain archive substrate is sufficient**; same prerequisite as Item 4); AI-SPEC-005 (confirmed bilateral via `B-AI-005-2` + `B-AI-005-8` + `B-AI-005-10` + `C-AI-005-3` — Item 5 V0.2 calibration-grade promotion gate "Baseline E beats Baseline F" (Spec §7 lines 569–583) requires archived chains for IV ATM extraction during replay calibration; HAR-RV training also requires multi-year 5-minute SPX bar history (separate-but-bilateral substrate); IV extraction utility (`B-AI-005-10`) is bilateral with Item 4's `replay_data_reconstructor.py`; **Item 5 V0.2 promotion is calendar-blocked on Item 4 V0.1 calibration-grade-capable status — same operator decision (`C-AI-004-4`) resolves both per `C-AI-005-3`**); AI-SPEC-006 (**confirmed transitively** via `B-AI-006-2` (18 fields from Item 5 EV Table) + `B-AI-006-10` (`replay_eval_cases.calibration_eligible = true` filter) + `B-AI-006-14` (un-executed candidate slippage imputation requires Item 10 slippage formula bilateral with `B-AI-010-6`) — Item 6 V0.2 calibration-grade ship requires upstream chain-archive substrate via Items 4, 5, 10; if chain-archive is insufficient at Item 6 V0.2 ship time, Item 6 V0.2 ships in advisory-only mode with `meta_labeler.calibration_grade_capable = false` mirroring Items 4/5/10 fallback patterns); cross-cutting matrix §6 line 660; future audits AI-SPEC-008 (OPRA Flow Alpha) likely depend on same substrate | Hard architectural prerequisite for V0.1 calibration-grade replay AND Item 10 V0.2 Layer 2 calibration-grade attribution AND Item 5 V0.2 calibration-grade promotion AND Item 6 V0.2 calibration-grade promotion (transitive). Forward archival is months-long (estimated 6+ months for 90 sessions per Spec §6 sample-size tiers); paid-tier historical-chain access is a contract dependency. **Load-bearing for Item 4 V0.1, Item 10 V0.2, Item 5 V0.2, AND Item 6 V0.2 calendars (4 specs share dependency).** Spec §10 lines 985–989 (Item 4) acknowledges the failure mode (V0.1 advisory-only); AI-SPEC-010 Spec §5 implicitly mirrors — Layer 2 can ship operational state with `chain_archive_capable = false` (slippage formula degrades to operator-conservative defaults; `calibration_eligible` set false for affected rows); AI-SPEC-005 Spec §7 Fallback Level 3 explicitly anticipates "logging only, no production authority" — Item 5 V0.2 can ship scaffolding-only with `calibration_grade_capable = false`. **Item 6 V0.2 inherits the chain-archive dependency transitively** — without sufficient chain archive, Item 6 V0.2 ships in advisory-only mode (cannot reduce Governor size below pure-rules baseline; `meta_size_cap` always = 1.0 except for hard skips). Default position: option 3 (explicit V0.x advisory-only acceptance) with parallel-track exploration of options 1 (forward archival) and 2 (paid historical archive). No new D-XXX strictly required for default; explicit operator acceptance logged in `system-state.md` `replay_harness.calibration_grade_capable = false` AND `counterfactual_engine.layer_2_chain_archive_capable = false` AND `vol_fair_value_engine.calibration_grade_capable = false` AND `meta_labeler.calibration_grade_capable = false`. **`C-AI-004-4` resolution cascades to `C-AI-005-3` AND Item 10 V0.2 layer_2_chain_archive_capable AND Item 6 V0.2 calibration_grade_capable.** | open — confirmed in 4 audits (emerged P1.3.3, confirmed bilateral P1.3.4, trilateral P1.3.5, quadrilateral P1.3.6 transitive) |
| News/event `published_at` field substrate (point-in-time gating) | AI-SPEC-004 (introduced via `B-AI-004-6` — Spec §1 line 45 + §5 Timestamp Boundary line 609 require minute-level `published_at` field; at HEAD no `published_at` exists — `sentiment_agent.py` stores `generated_at` (when WE generated brief), `economic_calendar.event_date` is date-level only, `earnings_calendar.earnings_date DATE` + `announce_time TEXT` ('pre' / 'post' / 'unknown') only); AI-SPEC-010 (not applicable — Item 10 is EOD batch over closed/blocked decisions; news/event timing already collapsed into the source decision row at decision-time, no point-in-time replay needed); future audits AI-SPEC-005 (Vol Fair-Value news look-ahead concern), AI-SPEC-008 (OPRA Flow Alpha event timing) likely depend on same substrate | Without `published_at TIMESTAMPTZ` distinct from `ingested_at`/`generated_at`, point-in-time replay cannot enforce sub-day look-ahead bias prevention — a leakage risk for any spec that conditions decisions on news/events. Affects: `sentiment_agent.py` raw news fetch, `economic_calendar.py` calendar rows, `earnings_calendar` table, `trading_ai_briefs` schema (or successor). Resolution: Item 4 V0.1 ship scope adds `published_at` extension; legacy rows excluded from Spec §5 line 609 enforcement (treated as `event_unavailable` per Spec §1 line 151). | open — confirmed in 1 audit, n/a in 1 audit |
| LightGBM artifact-log substrate (model-state-source for replay) | AI-SPEC-004 (introduced via `B-AI-004-10` + `B-AI-004-13` — Spec §3 lines 306–336 LightGBM Model State Policy + Spec §5 line 686 "Going forward: log model artifacts daily" requires daily artifact serialization + prediction-correlation > 0.95 substrate; at HEAD `model_retraining.py` (12K, dormant; bf41175 commit) trains models in-process, no artifact-log table, `trading_calibration_log` is event-log not artifact-log); AI-SPEC-010 (not applicable — Item 10 Layer 2's slippage formula is parametric (multiplicative coefficients on z-scores), not LightGBM-driven; no dependency on artifact-log substrate); AI-SPEC-006 (**confirmed bilateral consumer + producer**, `B-AI-006-1` + `B-AI-006-4` + `C-AI-006-2` — Item 6 V0.2's regularized logistic scorecard (sklearn `LogisticRegression(penalty='l2', C=0.5)` per Spec §2.2) requires daily artifact serialization for replay reproducibility; Item 6 V0.2 also reads `p_bull` / `p_bear` / `direction_confidence` / `direction_edge` / `strategy_direction_alignment` from a "LightGBM Direction Model" producer per Spec §3.4 (calendar-blocked on `C-AI-006-4` operator decision — possibly new spec AI-SPEC-014); AND Item 6 inherits the existing Layer 1 `bf41175` LightGBM meta-label scaffold's `model_retraining.train_meta_label_model()` artifact pattern — Layer 1 stays put as legacy observability per `C-AI-006-2` default; Layer 2 ships fresh artifact-log integration); future audits AI-SPEC-005 (Vol Fair-Value model versioning), AI-SPEC-013 (Drift Detection model-version coordination) likely depend | Without daily LightGBM artifact serialization + a `model_artifact_log` table, Spec §3 case 3 ("reconstruct model") cannot fire and Spec §3 case 4 (model unavailable) is the V0.1 default for all replay runs. **AI-SPEC-006 P1.3.6 confirms theme expands to cover BOTH (a) Item 6 logistic scorecard artifact serialization AND (b) Item 6's LightGBM Direction Model dependency — possibly two separate model classes both needing artifact-log substrate.** Resolution: V0.1 ship scope (Item 4) includes either (a) daily artifact serialization to S3-equivalent + `model_artifact_log` table, OR (b) explicit V0.1 deferral with `model_state_source = 'unavailable'` for all calibration paths until artifact-log ships. Bilateral with Item 6 V0.2 (consumer + producer) and possible AI-SPEC-014 (LightGBM Direction Model spec — pending operator decision per `C-AI-006-4`). | open — confirmed in 2 audits, n/a in 1 audit |
| `counterfactual_pnl` shape: column-triple vs `trade_counterfactual_cases` table (PRE-P11-4 carry-forward / `C-AI-002-2`) | AI-SPEC-002 (originated, B-AI-002-7 escalated to C-AI-002-2 — Item 2's `strategy_attribution.case_id UUID REFERENCES trade_counterfactual_cases(id)` assumed table-shaped); AI-SPEC-004 (confirmed bilateral, B-AI-004-8 — Item 4's Spec §1 line 37 reads `trade_counterfactual_cases`); **AI-SPEC-010 (AUTHORITATIVE RESOLUTION SURFACE, P1.3.4 — Spec §4 lines 198–225 ships `trade_counterfactual_cases` as a NEW table with full case-typed schema; existing 3-column triple on `trading_prediction_outputs` deprecates to Layer 1 legacy observability surface per Spec §6 lines 423–432 cutover migration)** | **RESOLVED by AI-SPEC-010 audit (P1.3.4) in favor of option 1 (new table; existing columns deprecate to Layer 1 legacy observability).** PRE-P11-4 carry-forward closes with the Layer 1 / Layer 2 split: Layer 1 = column triple (observability-only, frozen contract per migration `20260421_add_counterfactual_pnl.sql:4–5`); Layer 2 = `trade_counterfactual_cases` table (calibration-grade, advisory-authority, feeds Items 5/6/7/9 training). `C-AI-002-2` moves to §4 Resolved Findings. `B-AI-002-7` resolves transitively. `B-AI-004-8` resolves (Item 4 spec §1 line 37 wording stays as-is). Item 2 spec §2 line 42 FK target stays as-is. **Theme closes when Item 10 V0.2 Layer 2 ships and the `<Commit_4_deploy_date>` cutover anchor is operator-locked per `C-AI-010-5`.** | resolved by AI-SPEC-010 audit; theme closes on V0.2 ship |
| Layer 1 / Layer 2 architectural pattern (legacy + calibration-grade duality) | AI-SPEC-010 (introduced — Spec §0 lines 11–28 + §11 lines 558–582 establishes Layer 1 (existing `counterfactual_engine.py` 406 lines, observability-only, "DO NOT REWRITE") + Layer 2 (new `backend/counterfactual_engine_v2.py` per `B-AI-010-1` canonical name proposal, calibration-grade, advisory authority, feeds Items 5/6/7/9 training accessors with `calibration_eligible = true` filtering per Spec §6 lines 391–410)); AI-SPEC-005 (**confirmed second adopter** via `B-AI-005-1` + `C-AI-005-2` — Item 5 has Layer 1 candidate at `backend/polygon_feed.py:_append_spx_daily_return_if_due` lines 416–501 producing `polygon:spx:realized_vol_20d` Redis key (line 486; daily snapshot, observability-only) AND proposes Layer 2 HAR-RV regression model at recommended canonical name `backend/vol_fair_value_engine.py` per `B-AI-005-1`; Layer 1 contract preserved (operator decision pending on `C-AI-005-2` cutover policy — default option 1 stays-put-as-backward-compat); Layer 2 produces NEW Redis namespace `vol:*` + new tables `vol_fair_value_snapshots` + `strategy_ev_table` + `spx_realized_vol_history` + `vol_engine_reliability_log`; downstream consumers Items 1 + 6 read from Layer 2 outputs filtered on `calibration_eligible = true`; pre-fix-`fc64840` Layer 1 rows in `spx_realized_vol_history` marked `calibration_eligible = false` per `B-AI-005-15` mirroring Item 10 `C-AI-010-2` lockdown anchor pattern); AI-SPEC-006 (**confirmed THIRD adopter — pattern now established as architectural template** via `B-AI-006-1` + `C-AI-006-2` — Item 6 has Layer 1 candidate at commit `bf41175` (`backend/model_retraining.py:722` `train_meta_label_model()` + `backend/execution_engine.py:328–399` inference block + `model:meta_label:enabled` Redis flag, dormant pass-through with `backend/models/meta_label_v1.pkl` absent — verified `ls -la backend/models/` returns only `.gitkeep`) AND proposes Layer 2 Bayesian three-part hybrid engine at recommended canonical name `backend/meta_labeler_engine.py` per `B-AI-006-1`; Layer 1 contract preserved (operator decision pending on `C-AI-006-2` cutover policy — default option 1 stays-put-as-legacy-observability matching Item 10 + Item 5 precedent); Layer 2 produces NEW Redis namespace `meta:*` + new table `meta_labeler_decisions` per `B-AI-006-11`; if Layer 1 is ever activated (operator places pkl), `label_quality = 'legacy_observability_only'` is flagged on any persisted Layer 1 outputs to prevent training contamination); potentially AI-SPEC-008 (OPRA Flow Alpha may parallel — existing flow logic + calibration-grade extension) | The Layer 1 / Layer 2 pattern is a **template for retrofit upgrades** of existing AI-architecture surfaces. Properties: (a) Layer 1 contract preserved as observability-only with explicit "frozen" migration header OR dormant pass-through with `label_quality` flagging; (b) Layer 2 ships as a separate file/table with `calibration_eligible BOOLEAN` flag enforcing structural data-quality gating; (c) downstream training consumers (Items 5/6/7/9) read ONLY from Layer 2 surfaces (Layer 1 rows pre-cutover-date marked `legacy_observability_only` to prevent contamination); (d) operator can run Layer 1 + Layer 2 in parallel during V0.x validation via independent Redis feature flags (`feedback:counterfactual:enabled` for Layer 1, `feedback:counterfactual:layer2:enabled` for Layer 2 per `B-AI-010-9`; analogous flag pattern for Items 5 + 6 — Layer 1 keeps existing producer (12A writer / `bf41175` scaffold), Layer 2 produces parallel `vol:*` / `meta:*` keys with independent flags); (e) Layer 1 receives surgical bug fixes only (per `C-AI-010-1` operator decision) — no full rewrites. **AI-SPEC-006 P1.3.6 confirms the pattern as an architectural template** — now confirmed in 3 audits (AI-SPEC-010 + AI-SPEC-005 + AI-SPEC-006). The pattern applies when an existing scaffold is identified at HEAD that should not be rewritten but cannot satisfy calibration-grade requirements. **Document this pattern in cross-cutting evidence matrix as the canonical retrofit pattern for AI-architecture surfaces.** Future P1.3.7+ audits should test for Layer 1 / Layer 2 applicability when an existing scaffold is identified. **Item 6 P1.3.6 expands the pattern with the `dormant pass-through` variant** — Layer 1 can be dormant (pkl absent, scaffolds present) AND still trigger the same cutover policy framework; the variant adds `system-state.md` `meta_labeler.layer_1_pkl_present` field for runtime tracking. **AI-SPEC-008 P1.3.7 expands the pattern with the `Layer 0 + Layer 2 coexistence` variant** — `databento_feed.py` (452 lines at HEAD) is **Layer 0 raw OPRA trade-stream consumer** producing 3 Redis keys (`databento:opra:trades` / `:gex_block` / `:confidence_impact`) per `B-AI-008-17`; new `flow_alpha_engine.py` (proposed canonical filename per `B-AI-008-1`) is **Layer 2 alpha-feature producer** producing `opra:flow_alpha:*` namespace; **NO LAYER 1** observability scaffold exists between them — Item 8 jumps directly from Layer 0 (raw feed) to Layer 2 (alpha extractor); coexistence default per `C-AI-008-2` resolution + `B-AI-008-17`: `databento:opra:*` namespace remains Layer 0 raw-feed substrate (continues serving GEX engine via `databento_feed.py:349 lpush`); `opra:flow_alpha:*` namespace ships as Layer 2 alpha-feature output; cutover is therefore additive (no migration of `databento:opra:trades` consumers); `flow_agent.py` (343 lines at HEAD; brief generator) narrows to LLM summarizer over Item 8's 5/6-field Governor compact summary per `C-AI-008-2` default option 1. **AI-SPEC-008 also introduces a sibling pattern**: cross-spec producer→consumer contract gap pattern (Item 8's spec §5 unilaterally claims contracts with Items 5+6 that are not yet bilaterally ratified per `C-AI-008-1` — see new theme below). | open — confirmed in 4 audits with 3 variants (pattern is now an architectural template family — Layer 0/Layer 2 coexistence; Layer 1/Layer 2 retrofit; dormant Layer 1 + Layer 2 fresh ship) |
| Single-strategy contamination risk in counterfactual simulation (matrix carry-forward dim 9) | AI-SPEC-010 (introduced — Layer 1 at `counterfactual_engine.py:213–223` hardcodes `strategy = "iron_condor"` when `strategy_hint` missing on the source row; downstream training that treats this label as the optimal counterfactual — rather than the conservative one — would induce "iron-condor bias" in any model trained on Layer 1 data; Spec §3 lines 174–186 mandates Layer 1 + Layer 2 fix returning `counterfactual_pnl = NULL` with `simulation_status = "insufficient_strategy_context"` instead — but Layer 1 fix is conditional on `C-AI-010-1` operator decision); future audits AI-SPEC-006 (Meta-Labeler) + AI-SPEC-009 (Exit Optimizer) — both consume `trade_counterfactual_cases` outputs and must respect `simulation_status = 'success'` + `calibration_eligible = true` filtering per Spec §6 lines 391–410 to avoid contamination | Risk LOW today (Layer 1 is observability-only, no consumer trains on it). Risk HIGH the moment Items 5/6/7/9 V0.x ships and consumes counterfactual data. Resolution per `C-AI-010-1` + `B-AI-010-3`: surgical Layer 1 fix (return NULL when `strategy_hint` missing) is consistent with Spec §0 "do not rewrite" because it is a bug-fix not a rewrite; Layer 2 ships the corrected logic uniformly; Spec §6 lockdown migration retroactively marks pre-fix rows `legacy_observability_only` to prevent contamination if operator chooses option 1. | open — emerged in P1.3.4 |
| Z-score producer authority (`vix_z` / `spread_z` cross-spec features) | AI-SPEC-001 (consumer — Card features include vix_z, observed via Spec §3 lines 251–260 LLM Output schema); AI-SPEC-005 (**confirmed consumer + partial producer** via `B-AI-005-4` — Item 5 V0.2 reads existing `polygon:vix:z_score` (production key at `polygon_feed.py:400`) and `polygon:vvix:z_score` (production key at `polygon_feed.py:241`) for HAR-RV input vector; reads existing `polygon:vix:z_score_daily` per `B-AI-005-4`; does NOT produce `vix_z` / `spread_z` itself but its outputs feed Item 10 slippage formula consumers); AI-SPEC-010 (introduced via `C-AI-010-3` + `B-AI-010-6` — slippage formula `vol_multiplier = 1.0 + 0.25 * max(0, vix_z); spread_multiplier = 1.0 + 0.30 * max(0, spread_z)` per Spec §5 lines 363–376; producer authority undefined); future audit AI-SPEC-007 (Adversarial Review) likely consumer too | No `vix_z` / `spread_z` Redis key or producer at HEAD with the exact name (verified 2026-04-27 — `grep -n 'vix_z\|spread_z' backend/*.py` returns matches only inside spec text references, not production producers). Existing surfaces: `polygon:vix:z_score` (production key at `polygon_feed.py:400`) and `polygon:vvix:z_score` (production key at `polygon_feed.py:241`) — these MAY satisfy Item 10's `vix_z` reference if naming is harmonized; `spread_z` (bid-ask spread z-score across SPX option chain) has NO existing producer. Z-score calculation requires a rolling-window normalizer (e.g., 60-day rolling z) implementation. Three options: (1) Item 1 owns as Card-feature engineering; (2) Item 5 owns as Vol Fair-Value Engine surface; (3) shared producer in stable namespace (e.g., `features:vix_z`, `features:spread_z`) owned by calibration_engine per D-019. Default position per consolidated plan §5: option 3 (decoupled). **Item 5 P1.3.5 adds nuance:** existing `polygon:vix:z_score` production key satisfies the `vix_z` half but `spread_z` requires NEW producer (likely owned by Item 5 if it produces the spread tolerance metric, or by a shared calibration_engine surface). Folds into D-023 wording. | open — confirmed in 2 audits (emerged P1.3.4, refined P1.3.5) |
| D-016 governance interaction (sigma blending vs HAR-RV) | AI-SPEC-005 (introduced via `C-AI-005-1` — Spec is **completely silent** on D-016 (`grep -n "D-016\|0\.70\|max.*realized.*implied" ITEM_5_VOLATILITY_FAIR_VALUE_ENGINE_LOCKED.md` returns ZERO matches); D-016 (approved-decisions.md lines 90–93) defines `sigma = max(realized, 0.70 × implied)` as the canonical sigma-blending formula; Item 5's HAR-RV `rv_forecast_final` is the natural successor to the `realized` term; **D-016 is documented BUT NOT implemented at HEAD** — `docs/04-modules/exit-engine.md:61-66` documents the `sigma_effective = max(sigma_30m_realized, sigma_10m_realized * 0.85, sigma_5m_realized * 0.75, sigma_atm_implied * 0.70)` formula but `prediction_engine.py:706` uses simpler `vix_val < rv_val * 1.10` filter — verified 2026-04-27 — `grep -n 'sigma_effective\|0\.70\|0\.85\|0\.75' backend/*.py` returns ZERO matches); AI-SPEC-001 (consumer — Risk Governor's exit-decision composition layer reads sigma_effective per Spec §10 Constitutional Gates; will inherit D-016 amendment if `C-AI-005-1` chooses option 1 or 3); future audits AI-SPEC-006 (Meta-Labeler — exit-EV training on sigma-blended thresholds), AI-SPEC-009 (Exit Optimizer — directly consumes sigma_effective per matrix §3) | **Two-layer governance debt.** Layer A: D-016 is documented but not implemented in `backend/` code at HEAD (`docs/04-modules/exit-engine.md` shows the formula; `prediction_engine.py:706` shows the actual deployed simpler IV/RV filter — separate cleanup task tracked as cross-spec follow-up, T-Rule 4 protected). Layer B: Item 5 V0.2 introduces HAR-RV as `rv_forecast_final` — operator must take a position on whether HAR-RV REPLACES the `realized` term in D-016 (option 1, requires D-016 amendment + new D-XXX), runs alongside D-016 unchanged (option 2, default — HAR-RV is NEW signal feeding EV table only), or two-phase with D-016 amendment after V0.2 promotion (option 3). Default position per `C-AI-005-1`: option 2 (no D-016 amendment; HAR-RV runs as new signal). T-Rule 4: D-016 cannot be modified without owner approval + new decision record. **Folds into D-023 wording** — D-023 enrichment item (k): "Item 5's authority is over `vol:*` namespace + `vol_fair_value_snapshots` table; Item 5 does NOT have authority to amend D-016." If operator chooses option 1 or 3 future, new D-XXX (e.g., D-024) ratifies the amendment with explicit sigma-blending replacement. | open — emerged in P1.3.5 |
| Multi-year SPX 5-minute bar history substrate (HAR-RV training prerequisite) | AI-SPEC-005 (introduced via `B-AI-005-2` + `B-AI-005-15` — HAR-RV regression model requires multi-year 5-minute SPX bar history with multi-component lag structure (15m, 60m, 240m, 1d, 5d, 22d per Spec §2 lag structure); at HEAD `polygon_feed.py` produces `polygon:spx:return_5m`, `polygon:spx:return_30m`, `polygon:spx:return_1h`, `polygon:spx:return_4h` — all Redis keys with **300s TTL only** (verified 2026-04-27 — `grep -n 'return_5m\|return_30m\|return_1h\|return_4h\|setex\|TTL' backend/polygon_feed.py | head -30` shows TTL=300s at lines 877–880); `spx_history` deque at `backend/polygon_feed.py:62` is in-memory only and resets on process restart; multi-year persisted history substrate is MISSING; `polygon:spx:realized_vol_20d` is daily snapshot, NOT 5-min bars; no `spx_5min_bars` Supabase table); future audits AI-SPEC-006 (Meta-Labeler training on 5-min market microstructure features) and AI-SPEC-008 (OPRA Flow Alpha) likely depend on similar substrate | Substrate gap parallel to chain-archive substrate (`C-AI-004-4`) but separate. HAR-RV training requires either (a) NEW Supabase table `spx_5min_bars` (date-partitioned) with backfill from Polygon `/v2/aggs/ticker/I:SPX/range/5/minute/{from}/{to}` — operator must verify Polygon paid tier supports historical 5-min bars (free tier supports daily SPX bars per `polygon_feed.py:706` evidence; 5-min may require upgrade), OR (b) calibration-grade replay deferred to V0.3+ once forward-archival accumulates sufficient history (estimated 1+ year minimum for HAR-RV stability). **Bilateral with `C-AI-004-4` chain-archive substrate** — same flavor of "data substrate not code" missing-piece. Default position per consolidated plan §5: option (b) deferral with parallel-track exploration of option (a) Polygon paid tier. Logged in `system-state.md` `vol_fair_value_engine.har_rv_training_history_sufficient = false` until operator decision. | open — emerged in P1.3.5 |
| `realized_vol_20d` Layer 1 cutover anchor (B1-2 fix `fc64840`) | AI-SPEC-005 (introduced via `B-AI-005-15` + `C-AI-005-2` — Item 5 V0.2 reads from Layer 1 candidate `backend/polygon_feed.py:_append_spx_daily_return_if_due` lines 416–501 producing `polygon:spx:realized_vol_20d` (line 486; daily snapshot); B1-2 fix at commit `fc64840` (verified 2026-04-27 via `git show fc64840 --format="%ci %s"` returning `2026-04-20 18:43:14 -0400 fix(EOD): move daily return append from 19:00 UTC to 21:00 UTC to capture full SPX session`); pre-fix-`fc64840` realized-vol values may be mistimed (EOD gate fired at 19 UTC instead of 21 UTC, missing last 2 hours of session); HAR-RV training data must mark pre-fix rows `calibration_eligible = false` per `B-AI-005-15` recommended `spx_realized_vol_history.calibration_eligible BOOLEAN`); AI-SPEC-006 (**confirmed transitive consumer** — Item 6's training-set assembly per `B-AI-006-10` filters on `calibration_eligible = true` across all upstream substrates including `spx_realized_vol_history`; Item 6 inherits `fc64840` cutover anchor when consuming RV history features for the logistic scorecard's `iv_rv_ratio` / `rv_forecast_move` features); future audits AI-SPEC-009 (Exit Optimizer training data filtering) likely inherit the same lockdown anchor when consuming RV history | Item 5 V0.2 ship scope must include realized-vol history persistence (`spx_realized_vol_history` table per `B-AI-005-15` recommended schema) with explicit `calibration_eligible = false` for all rows where `(date < 2026-04-21)` (the day after `fc64840` deploy at 2026-04-20 18:43 ET, ensuring the next full session at 2026-04-21 onward has correct EOD gating). Mirrors Item 10's Layer 1 lockdown migration pattern but with concrete commit-hash anchor (no operator decision required for the anchor itself; `C-AI-005-2` resolves the broader Layer 1 cutover policy). | open — confirmed in 2 audits (emerged P1.3.5, transitive P1.3.6) |
| Bayesian prior parameter governance (operator-tunable vs code-bound) | AI-SPEC-006 (**introduced via `C-AI-006-3`** — Spec §2 hardcodes Bayesian prior parameters: `alpha=1, beta=1` (line 188), `k=20` (line 198), `w_prior` values 0.55 / 0.40 / 0.25 (lines 168/173/178); Spec contains zero references to `system-state.md`, `approved-decisions.md`, or operator-tunable language; prior parameters cannot be retuned without a code change); future audits AI-SPEC-013 (Drift Detection model-version coordination — likely has analogous hardcoded threshold parameters) | Three-option resolution per `C-AI-006-3`: (option 1) operator-tunable via `system-state.md` `meta_labeler.prior_parameters` field; (option 2) operator-tunable via `approved-decisions.md` D-024 ratification; (option 3) code-bound until first promotion-gate review. **Default position (per consolidated plan §5 conservative-first principle):** option 1 (operator-tunable via `system-state.md`); folds into D-023 enrichment item (s); first promotion-gate review revisits the question. T-Rule 2 (Governance Documents Are Authoritative) — operator-tunable parameters should be governance-tracked. **Theme template applies to ALL future ML-based AI architecture specs that hardcode hyperparameters.** | open — emerged in P1.3.6 |
| LightGBM Direction Model spec ownership (catalog completeness) | AI-SPEC-006 (**introduced via `C-AI-006-4`** — Spec §3.4 reads 5 fields (`p_bull`, `p_bear`, `direction_confidence`, `direction_edge`, `strategy_direction_alignment`) from a "LightGBM Direction Model" producer that has NO spec in the AI architecture catalog; closest existing logic at HEAD: `backend/polygon_feed.py` LightGBM features for SPX 5-min bars + `backend/model_retraining.py` LightGBM training infrastructure for meta-label scaffold — but no LightGBM direction model exists; bilateral with `B-AI-006-4`); future audit possible AI-SPEC-014 (LightGBM Direction Model — pending operator decision per `C-AI-006-4` option 2) | Three-option resolution per `C-AI-006-4`: (option 1) add as Item 6 V0.2 ship sub-scope (same engine module or new module); (option 2) add new spec AI-SPEC-014 to catalog (operator approves catalog extension; new audit P1.3.14); (option 3) remove the 5 fields from Item 6 §3.4 (Item 6 V0.2 ships without direction-tilt features). **Default position (per consolidated plan §5 conservative-first principle):** option 2 (new spec AI-SPEC-014); minimizes Item 6 V0.2 scope creep but adds Cluster B audit cycle. **Possible new D-XXX (e.g., D-024) for AI architecture catalog extension.** Bilateral with chain-archive substrate (Item 4 V0.1 archive must support LightGBM direction model training) AND Item 5 EV table (direction model output may feed Item 5's strategy ranking). | open — emerged in P1.3.6 |
| Authority Recovery automatic-vs-operator-mediated (architectural principle 1 boundary) | AI-SPEC-006 (**introduced via `C-AI-006-1`** — CRITICAL Class C escalation per Guard 1; Spec §9.4 line 622 specifies AUTOMATIC authority recovery (advisory → reduced → normal) via `30-session cooldown + 20 qualified signals` — verbatim "Mirror Item 5: minimum 30-session cooldown, then one-step recovery (advisory → reduced → normal) after demonstrated recalibration over 20 qualified signals"; per architectural principle 1 ("AI controls admissibility, not authority promotion") + T-Rule 4 (Locked Decisions Are Final), authority RECOVERY (increasing AI authority) should require explicit operator action; **bilateral with AI-SPEC-005 §5 reliability state machine** which has the SAME automatic-recovery pattern (Item 6 spec line 622 explicitly references "Mirror Item 5")); AI-SPEC-005 (**confirmed bilateral** — Item 5 §5 reliability state machine has same automatic-recovery pattern; Item 5 P1.3.5 audit predates Guard 1; Item 6 P1.3.6 retroactively flags Item 5's pattern as bilateral CRITICAL Class C); future audits AI-SPEC-009 (Exit Optimizer authority degradation) likely follows same pattern | Three-option resolution per `C-AI-006-1`: (option 1) automatic recovery accepted (D-023 enrichment encodes per-channel automatic recovery within `item_promotion_records` operator-locked phase enum bounds); (option 2) operator-mediated recovery only (cooldown + qualified-signals threshold becomes ELIGIBILITY gate, operator approves recovery; bilateral amendment to AI-SPEC-005 §5); (option 3) hybrid (degradation automatic, recovery operator-mediated through `item_promotion_records` with 30+20 as ELIGIBILITY gate). **Default position (per consolidated plan §5 conservative-first principle):** option 2 (operator-mediated recovery only); D-023 enrichment item (p). **CRITICAL Class C escalation** — first authority-boundary-violation Class C in Cluster B; bilateral resolution mandatory with AI-SPEC-005 §5 to avoid cross-spec inconsistency. **MOST CONSEQUENTIAL OPERATOR DECISION in P1.3.6 audit.** Folds into D-023 wording. | open — emerged in P1.3.6 (CRITICAL bilateral with AI-SPEC-005 §5) |
| Memory Retrieval substrate dependency (`pgvector` + Top-20 analog query pipeline) | AI-SPEC-006 (**introduced via `B-AI-006-6`** — Item 6 V0.2 reads 10 Memory Retrieval Top-20 Analogs fields per Spec §3.6 lines 326–340; underlying Memory Retrieval substrate (pgvector / case_retrieval / trade_decision_cases / similar_cases) entirely absent at HEAD — verified `grep -rn "n_similar_cases\|pgvector\|case_retrieval\|trade_decision_cases\|memory_retrieval\|similar_cases" backend/` returns 0 matches; **Memory Retrieval is itself a separate AI architecture spec (Item 7) — not yet audited**; bilateral with AI-SPEC-001 `B-AI-001-1` `pgvector` enablement prerequisite); future P1.3.7 audit (Item 7 Memory Retrieval) | **Deepest calendar-blocker in cross-spec cascade.** Item 6 V0.2 ship calendar-blocked on Item 7 V0.x ship — Item 7 itself un-audited (future P1.3.7). Resolution: (a) future P1.3.7 audit must complete BEFORE Item 6 V0.2 ship; (b) Item 7 V0.x ship must complete BEFORE Item 6 V0.2 ship; (c) Item 6 V0.2 advisory-only fallback if Item 7 V0.x ship slips — `w_memory = 0` on the three-part hybrid (Bayesian prior + logistic scorecard only). **`B-AI-001-1` `pgvector` enablement is the substrate prerequisite for both Item 7 V0.x and Item 6 V0.2.** New `system-state.md` field per `B-AI-006-17`: `memory_retrieval_substrate.{ status, pgvector_enabled, trade_decision_cases_table_present, embedding_pipeline_active, last_index_rebuilt_at }` populated once Item 7 audit lands. | open — emerged in P1.3.6 (calendar-blocked on un-audited Item 7) |
| Cross-spec producer→consumer contract ratification (unilateral spec claims vs bilateral confirmation) | AI-SPEC-008 (**introduced via `C-AI-008-1`** — Item 8 spec §5 lines 270–281 unilaterally claims an 8-feature contract with Item 5 (`vol_demand_pressure_5m`, `zero_dte_vs_1_3dte_ratio`, `sweep_premium_share_1m`, `aggressive_put_call_imbalance_5m`, `short_delta_pressure_5m`, `atm_delta_pressure_5m`, `dealer_hedge_pressure_z_5m`, `flow_price_divergence_5m`) AND a 20-feature contract with Item 6 (all 20 production features per `A-AI-008-2` corrected count + 1 quality flag); verified 2026-04-28 — `grep -n "OPRA\|opra:flow_alpha\|opra_flow" trading-docs/04-modules/ai-architecture/archive/raw-locked-specs-2026-04-26/ITEM_5_VOLATILITY_FAIR_VALUE_ENGINE_LOCKED.md` returns 2 abstract matches only (line 390 + line 549; no field-name enumeration); `grep -n "OPRA\|opra:flow_alpha\|opra_flow\|Item 8" trading-docs/04-modules/ai-architecture/archive/raw-locked-specs-2026-04-26/ITEM_6_META_LABELER_LOCKED.md` returns ZERO matches); AI-SPEC-005 (**confirmed silent producer-side** — Item 5 spec is silent on Item 8 as one of its 5 producer surfaces enumerated in `B-AI-006-2` through `B-AI-006-6`; AI-SPEC-005 audit register `B-AI-005-9` strategy-conditional EV table generator does NOT list Item 8 as a producer-surface input); AI-SPEC-006 (**confirmed silent producer-side** — Item 6's 47 named feature fields per `B-AI-006-2` through `B-AI-006-6` come from 5 producer surfaces with Item 8 NOT named as 6th surface); future audits AI-SPEC-009 (Exit Optimizer reads OPRA flow features per Spec §5.1 unspecified contract surface — likely same pattern); future P1.3 amendment cycle for Items 5 + 6 cross-spec contract bilateral confirmation | **Sibling pattern to Layer 0/1/2 architectural template — emerges when a downstream spec ships with explicit producer→consumer contracts that consumer specs are silent on (or contradict).** Three resolution options per `C-AI-008-1`: (option 1 — RATIFY) amend Item 5 §3/§4 + Item 6 §3 to add Item 8 as named producer surface in next P1.3 amendment cycle (cleanest; requires bilateral spec amendment); (option 2 — REVOKE) treat OPRA features as research-only inputs that downstream consumers may opt into post-V0.3 maturation via future spec amendment (most conservative; defers integration); (option 3 — DEFAULT, **DEFERRED BILATERAL CONFIRMATION**) accept Item 8 §5 as forward-looking unilateral claim with bilateral confirmation deferred to next P1.3.6 / P1.3.5 amendment cycle (post-Cluster B closure); preserves audit-trail clarity at cost of delayed cross-spec coherence. **Folds into D-023 enrichment item (w)** — cross-spec contract ratification process governance (governs how producer specs unilaterally claim contracts with consumer specs and how those contracts get bilaterally ratified). **Theme template applies to ALL future AI architecture specs that ship producer→consumer contracts; should be tested for in future P1.3.x audits.** | open — emerged in P1.3.7 |
| Databento OPRA subscription scope (procurement decision distinct from D-008 budget) | AI-SPEC-008 (**introduced via `C-AI-008-3`** — Spec §1 lines 33–46 Lee-Ready aggressor inference assumes NBBO ticks at sub-second cadence (Spec line 48: `NBBO_age > 1 second → quote_stale = true`); Spec §1 lines 114–126 sweep cluster detection assumes same-millisecond cluster reconstruction across exchanges; Spec §1 lines 128–133 block trade detection assumes 95th-percentile rolling distributions; verified at HEAD via `wc -l backend/databento_feed.py = 452`; subscription detail not visible via grep alone — operator pre-implementation verification required); D-008 (Databento subscription budget — `~$150–200/month` envelope) | **Procurement decision distinct from D-008 budget governance.** D-008 governs the budget envelope but not specific subscription tier scope. Three-option resolution per `C-AI-008-3`: (option 1) Databento subscription tier upgraded within D-008 budget — confirm sub-second NBBO + sweep tape availability at current pricing; no new D-XXX needed; (option 2) Databento subscription tier upgrade requires D-008 budget revision (new D-XXX D-025 candidate raising the cap from `$150–200/mo` to whatever the new tier costs); highest-impact: changes governance D-008; (option 3) Item 8 V0.3 spec amendment to relax Lee-Ready inference + sweep detection requirements to fit current Databento subscription tier; spec amendment to §1 — fall back to mid-rule aggressor sign without prior-tick lookup AND coarser sweep cluster definition; (option 4 — **DEFAULT, PRE-IMPLEMENTATION OPERATOR GATE**) operator verifies Databento subscription tier scope against Spec §1 requirements **BEFORE V0.3 implementation begins**; if subscription tier sufficient, proceed with no governance change; if subscription tier insufficient, operator decides between options 2 and 3 at V0.3 implementation start; V0.3 implementation cannot begin until this verification completes. **Folds into D-023 enrichment item (u)** — Item 8 authority boundary (spec authority over Databento subscription scope is operator-only; Item 8 specifies what it needs but does not procure). | open — emerged in P1.3.7 (pre-implementation operator gate) |

---

## 6. Governance Updates Required (Roll-Up From All Audits)

Aggregated checklist of what governance documents must change. Each per-item audit's §11 contributes to this section. Future audits **append** to the relevant subsection; do not modify existing items unless a later audit narrows or supersedes a prior proposal (in which case add a "see also" note rather than overwriting).

### `approved-decisions.md`
- [ ] **D-023 — AI Authority Boundary** (proposed in AI-SPEC-001 §11; covers C-AI-001-1, C-AI-001-2, C-AI-001-3 disposition). Wording must cover (a) Governor's `size_multiplier_cap` channel composing with `_RISK_PCT[phase]`, (b) explicit non-overridability of D-005 / D-010 / D-011 / D-014 / D-021 / D-022 with reductive-only constraint, (c) opportunity-lean channel disposition (folded into D-023 or deferred to separate spec), (d) promotion-gate authority handoff between automated criteria and operator approval. **AI-SPEC-002 P1.3.2 addendum (`C-AI-002-1`):** D-023 wording must additionally encode the `decision_outcome` enum coordination contract — either (option A) Item 2's enum extends with `cap_governor` / `reduced_governor` values that Item 1 writes for size-cap decisions, or (option B) D-023 mandates Item 1 writes `opened_traded` for size-cap decisions and the size-cap channel is logged separately on Item 1's own `ai_governor_decisions` row. Without this coordination, Item 1 size-cap writes will fail Item 2's CHECK constraint. **AI-SPEC-004 P1.3.3 addendum (`C-AI-004-2` + `C-AI-004-3`):** D-023 wording must additionally encode (e) the canonical "decision card" definition for Spec §12 thresholds (= `replay_eval_cases.calibration_eligible = true`; explicit exclusion of `shadow_predictions` rows from the 200-card count), and (f) the `item_promotion_records` startup-read contract: "any item subject to AI authority promotion (Items 1, 5, 6, and any future per-item-promotion-gated specs) MUST read its current authority level from `item_promotion_records` at startup; absence of a current row defaults to `advisory` (for items already paper-binding-eligible) or `disabled` (for items not yet paper-binding-eligible); items MUST NOT invent their own promotion status." **AI-SPEC-010 P1.3.4 addendum (`C-AI-010-1` + `C-AI-010-3` + `C-AI-010-5` + `B-AI-010-10`):** D-023 wording must additionally encode (g) the Layer 1 / Layer 2 cutover policy: "Item 10 Layer 1 (`counterfactual_engine.py`) is observability-only; Layer 2 (`counterfactual_engine_v2.py` per `B-AI-010-1`) is calibration-grade advisory authority; downstream training consumers (Items 5/6/7/9) MUST read ONLY from Layer 2 outputs filtered on `calibration_eligible = true` + `simulation_status = 'success'`; Layer 1 surgical bug fixes (e.g., NULL-return on missing `strategy_hint`) ARE permitted under "do not rewrite" provided they preserve the observability contract"; (h) the z-score producer authority assignment (`vix_z` / `spread_z` shared producer in `features:*` Redis namespace owned by calibration_engine per D-019); (i) the `counterfactual_reason ↔ decision_outcome` enum mapping table (Item 10 owns the mapping function in Layer 2 engine, exposed for cross-spec consumers); (j) the legacy-rows lockdown anchor (`<Commit_4_deploy_date>` cutover anchor for retroactive `label_quality = 'legacy_observability_only'` marking — operator-locked once Commit 4 of IC/IB sequence ships). **AI-SPEC-005 P1.3.5 addendum (`C-AI-005-1` + `C-AI-005-2` + `C-AI-005-3` + `B-AI-005-9`):** D-023 wording must additionally encode (k) Item 5's authority boundary: "Item 5's authority is over `vol:*` Redis namespace + `vol_fair_value_snapshots` Supabase table + `strategy_ev_table` Supabase table + `spx_realized_vol_history` Supabase table + `vol_engine_reliability_log` Supabase table; Item 5 outputs measurements consumed by Items 1 (Risk Governor) and 6 (Meta-Labeler) and 9 (Exit Optimizer); Item 5 does NOT have authority to amend D-016 (sigma blending formula `sigma = max(realized, 0.70 × implied)`) — operator decision required on `C-AI-005-1` for D-016 disposition (default option 2: HAR-RV is NEW signal feeding EV table only; D-016 stays unchanged)"; (l) the Item 5 Layer 1 / Layer 2 cutover policy (extends item (g)): "Item 5 Layer 1 = `polygon_feed.py:_append_spx_daily_return_if_due` writing `polygon:spx:realized_vol_20d` Redis key (observability-only; existing 12A); Layer 2 = `vol_fair_value_engine.py` per `B-AI-005-1` HAR-RV regression model + EV table + reliability state machine + Monte Carlo simulator; downstream consumers Items 1 / 6 / 9 read from Layer 2 outputs filtered on `calibration_eligible = true`; default position per `C-AI-005-2`: Layer 1 stays put as backward-compat surface; Layer 2 produces parallel `vol:*` keys"; (m) the canonical strategy-taxonomy enumeration (extends item (i)): "Items 2 + 5 + 6 + 10 MUST use canonical 10-strategy IDs (`put_credit_spread`, `call_credit_spread`, `iron_condor`, `iron_butterfly`, `debit_put_spread`, `debit_call_spread`, `long_put`, `long_call`, `long_straddle`, `calendar_spread`) matching DB CHECK constraint `20260419_add_strategy_types.sql:11-22` and `risk_engine.py:103-111` `_DEBIT_RISK_PCT` literal + `strategy_selector.py:17-28` `STATIC_SLIPPAGE_BY_STRATEGY` (production validity set used at line 1021) — not the `bull_debit_spread` / `bear_debit_spread` alternative"; (n) the chain-archive substrate cascade (extends item (j) + cross-references `C-AI-004-4` + `C-AI-005-3`): "Item 4 V0.1 + Item 5 V0.2 + Item 10 V0.2 share the same chain-archive substrate dependency; operator decision on `C-AI-004-4` cascades to all three — same option 1 / 2 / 3 disposition resolves all bilateral; logged in `system-state.md` across `replay_harness.calibration_grade_capable`, `counterfactual_engine.layer_2_chain_archive_capable`, `vol_fair_value_engine.calibration_grade_capable` (all three default false until operator decision)." **AI-SPEC-006 P1.3.6 addendum (`C-AI-006-1` + `C-AI-006-2` + `C-AI-006-3` + `C-AI-006-4` + `B-AI-006-9` + `B-AI-006-12`):** D-023 wording must additionally encode (o) Item 6's authority boundary: "Item 6 (Meta-Labeler) is **the keystone** between Item 5's `strategy_ev_table` and the AI Risk Governor (per Spec §0 line 8); Item 6 has authority to BLOCK trades and REDUCE size but NO authority to FORCE, UPSIZE, or override Item 1 Governor veto (per Spec §10 lines 670–672); Item 6 size cap composes with Items 1 / Rules / Constitutional caps via `min()` only (Spec §6 line 446 + §0 line 18); Item 6 outputs flow into Item 1 Card per `B-AI-001-2`; Item 6 outputs `decision_outcome` value `blocked_meta_labeler` (per `B-AI-006-9` resolution; bilateral with `C-AI-002-1`)"; (p) Item 6 (and Item 5) Authority Recovery disposition (per `C-AI-006-1`): "**default position option 2 (operator-mediated recovery only) is encoded in D-023**; Spec §9.4 line 622 + AI-SPEC-005 §5 reliability state machine recovery wording amended to require explicit operator approval for any one-step authority recovery (advisory → reduced → normal); cooldown (30 sessions) + qualified-signals threshold (20 qualified signals) become ELIGIBILITY gates, not automatic triggers; degradation triggers remain automatic (authority reduction always allowed); recovery flows through `item_promotion_records` change-control. **Bilateral resolution with AI-SPEC-005 mandatory** — CRITICAL Class C escalation per Guard 1; FIRST authority-boundary-violation Class C in Cluster B"; (q) Layer 1 / Layer 2 cutover policy for Item 6 (per `C-AI-006-2` — extends items (g) + (l)): "Item 6 Layer 1 = `bf41175` LightGBM scaffold at `backend/model_retraining.py:722` `train_meta_label_model()` + `backend/execution_engine.py:328–399` inference block + `model:meta_label:enabled` Redis flag (dormant pass-through with `backend/models/meta_label_v1.pkl` absent); Layer 2 = `backend/meta_labeler_engine.py` per `B-AI-006-1` Bayesian three-part hybrid; default position option 1 — Layer 1 stays put as legacy observability; if Layer 1 is ever activated (operator places pkl), `label_quality = 'legacy_observability_only'` is flagged on any persisted Layer 1 outputs"; (r) strategy taxonomy reconciliation for Item 6 (per `B-AI-006-12` — extends item (m)): "Item 6 V0.2 ships utility-label calculators for the 2 long-side canonical strategies omitted from spec's existing 5 (adds `U_LC`, `U_LP`). **Deferred to D-023 ratification (Action 6, Days 5–7):** whether Item 6 V0.2 additionally ships utility-label calculators for the 3 canonical strategies not covered by spec's existing 5 + this addition (`call_credit_spread`, `debit_put_spread`, `calendar_spread`). **Default deferred-decision = ship subset whose live-emission sample size meets [C-AI-006-3] Bayesian prior thresholds at Action 6 evaluation; alternative = ship all 3 ahead of sample-size validation to maximize calibration coverage.**"; (s) Bayesian prior parameter governance (per `C-AI-006-3` default option 1): "Item 6's prior parameters (`alpha`, `beta`, `k`, `w_prior` values per sample-size band) are operator-tunable via `system-state.md` `meta_labeler.prior_parameters` field; default values match Spec §2 hardcoded values; operator amendment triggers re-initialization at next promotion-gate review; first promotion-gate review revisits the operator-tunable-vs-code-bound question"; (t) `decision_outcome` enum coordination — Item 6 emits `blocked_meta_labeler` (per `B-AI-006-9` — extends item (b)): "Item 6 writes `blocked_meta_labeler` to `strategy_attribution.decision_outcome` when meta-labeler skips a trade; Item 6 writes `reduced_meta_labeler` (or equivalent) when meta-labeler reduces size below normal sizing; Item 2 enum CHECK constraint extended in same migration as Item 6 V0.2 schema-landing; bilateral with Item 10's `counterfactual_reason = 'meta_skip'` value (mapping table extends item (i))." **AI-SPEC-008 P1.3.7 addendum (`C-AI-008-1` + `C-AI-008-2` + `C-AI-008-3`):** D-023 wording must additionally encode (u) Item 8 authority boundary: "Item 8 (OPRA Flow Alpha) is **the deterministic feature producer** between Databento OPRA stream and Items 5/6/Governor consumers (per Spec §0); Item 8 produces 20 production features + 1 quality flag (per `A-AI-008-2` corrected count) into `opra:flow_alpha:*` Redis namespace + `opra_flow_feature_bars` / `opra_flow_events` Supabase tables per `B-AI-008-2` + `B-AI-008-3`; **Item 8 has authority over `opra:flow_alpha:*` namespace + `opra_flow_feature_bars` + `opra_flow_events` tables ONLY; Item 8 specifies Databento subscription requirements but does NOT procure** — Databento subscription scope decisions per `C-AI-008-3` are operator-only (folds D-008 budget governance with new procurement-scope clause); Item 8 outputs are **advisory measurements consumed by Items 5/6/Governor**; Item 8 ships V0.3 in advisory-only mode with `opra_flow_alpha.calibration_grade_capable = false` per `C-AI-008-4` default (production-binding deferred to V0.4+ contingent on Item 6 V0.2 ship + chain-archive maturation)"; (v) flow_agent / Layer 0 / Layer 2 cutover policy for Item 8 (per `C-AI-008-2` — extends items (g) + (l) + (q)): "Item 8 introduces **Layer 0 + Layer 2 coexistence variant** of the architectural template; Layer 0 = `databento_feed.py` (raw OPRA trade-stream consumer producing `databento:opra:*` namespace + GEX engine input via `databento_feed.py:349 lpush` — observability-only, kept put for backward-compat with `gex_engine.py` consumers); Layer 2 = new `backend/flow_alpha_engine.py` per `B-AI-008-1` (deterministic feature producer producing `opra:flow_alpha:*` namespace + `opra_flow_feature_bars` + `opra_flow_events` tables — calibration-grade advisory authority); **NO LAYER 1** observability scaffold exists between them — Item 8 jumps directly Layer 0 → Layer 2; cutover is additive (no migration of `databento:opra:*` consumers). **flow_agent narrowing per `C-AI-008-2` default option 1**: existing `backend_agents/flow_agent.py` (343 lines at HEAD; brief generator producing `ai:flow:brief` Redis key consumed by `synthesis_agent.py:93` + `main.py:1917`) **narrows to LLM summarizer** reading from `flow_alpha_engine` output (5/6-field Governor compact summary per `B-AI-008-15`); existing `ai:flow:brief` Redis key continues but content shifts from Unusual Whales / put-call ratio brief to narrative summary over Item 8's compact summary; preserves backward-compat with synthesis_agent input contract"; (w) cross-spec contract ratification process governance (per `C-AI-008-1`): "When a downstream spec ships explicit producer→consumer contracts that consumer specs are silent on (or contradict), bilateral confirmation is required before V0.x ship: (i) consumer spec must ratify the producer surface in next P1.3 amendment cycle; (ii) until ratification, the producer ships in **'unilateral claim' status** with `cross_spec_contract_ratified = false` flag in `system-state.md`; (iii) downstream training accessors filter on `cross_spec_contract_ratified = true` only — preventing Item 5/6 from training on unratified Item 8 features; (iv) operator authorizes unilateral-to-bilateral transition via P1.3 amendment cycle merge. **Default position per consolidated plan §5 conservative-first principle: option 3 (DEFERRED BILATERAL CONFIRMATION)** — Item 8 V0.3 ships with cross-spec contracts (Item 5: 8 features; Item 6: 20 features + 1 quality flag) in `unilateral claim` status; bilateral confirmation deferred to next P1.3.6 / P1.3.5 amendment cycle (post-Cluster B closure); Items 5 + 6 V0.x consumers may opt into Item 8 features at decision-time but training queries filter on `cross_spec_contract_ratified = true` only."
- [ ] **Possible new D-XXX (D-024 candidate) for LightGBM Direction Model spec ownership** (AI-SPEC-006 §11 per `C-AI-006-4`). If operator chooses option 2 (new spec AI-SPEC-014), a new D-XXX (e.g., D-024) ratifies the AI architecture catalog extension and adds P1.3.14 to the consolidated plan. **Default: option 2 — new D-XXX required (D-024 candidate).** Bilateral with `C-AI-006-3` D-XXX numbering choice if option 2 of `C-AI-006-3` chosen (operator-tunable via approved-decisions.md D-XXX ratification).
- [ ] **No existing D-XXX modifications required by AI-SPEC-001** — Spec defers cleanly to D-005, D-010, D-011, D-014, D-021, D-022 without amendment.
- [ ] **No new D-XXX required by AI-SPEC-002 standalone** (AI-SPEC-002 §11). Item 2 is observational labeling and does not introduce new operational rules. All Item 2 cross-spec coordination (decision_outcome enum, FK cascade, attribution_* legacy column disposition) folds into existing D-023 wording or operator decisions on `C-AI-002-2` / `C-AI-002-3`. Spec defers cleanly to D-001, D-005, D-010, D-013, D-022 without amendment.
- [ ] **D-013 Paper Phase non-amendment** (AI-SPEC-004 §11, default position per `C-AI-004-1`). If operator chooses option 1 (independent gates), D-013 is NOT amended; Spec §4 documentation explicitly states `paper_phase_criteria` and `item_promotion_records` are independent surfaces. If operator chooses option 2 (unified gate adding GLC-013), D-013 requires amendment via a new D-XXX (e.g., D-024) adding GLC-013 row to the 12-criteria list. **Default: option 1 — no D-013 amendment.**
- [ ] **Possible new D-XXX for "calibration-grade chain archive substrate"** (AI-SPEC-004 §11, conditional on `C-AI-004-4` resolution). If operator chooses option 1 (forward archival) or option 2 (paid historical), a new D-XXX may be warranted to formalize the data-substrate ownership and retention policy. If operator chooses option 3 (V0.1 advisory-only acceptance), no new D-XXX needed; explicit operator acceptance of Spec §10 Failure Mode logged in `system-state.md`. **Default: option 3 — no new D-XXX.**

### `MASTER_PLAN.md`
- [ ] **New phase entry: Phase 3A (or equivalent) — AI Risk Governor V0.1** (AI-SPEC-001 §11). Per Spec §28 V0.1 ship-scope: 4–6 weeks; 11 build items decomposed at `AI-SPEC-001.md` §12.2.
- [ ] **New phase entry: Phase 3B (or equivalent) — Strategy Attribution V0.1** (AI-SPEC-002 §11). Per Spec §15 V0.1 ship-scope: 3–4 weeks; 8 build items decomposed at `AI-SPEC-002.md` §12.2 (14B.0 schema migration, 14B.1 EOD attribution engine, 14B.2 path-metrics substrate or NULL deferral, 14B.3 calibration_eligible classifier, 14B.4 EOD job orchestration, 14B.5 backfill / cutover script for legacy positions, 14B.6 trigger / RLS for path-metric immutability, 14B.7 tests). Sequencing: must follow Phase 3A (Item 1) and at least the schema landing of Items 6 / 7 / 10 to declare FK constraints — see `B-AI-002-3` resolution plan.
- [ ] **New phase entry: Phase 3C (or equivalent) — Replay Harness V0.1** (AI-SPEC-004 §11). Per Spec §13 V0.1 ship-scope: 14 build items decomposed at `AI-SPEC-004.md` §12.2 (14C.0 schema bootstrap; 14C.1 chain-archive substrate or formal V0.1 deferral; 14C.2 Polygon historical 1-min fetch; 14C.3 news/event `published_at` field; 14C.4 8 `replay_*.py` files; 14C.5 Step 0 validation; 14C.6 walk-forward; 14C.7 decision-card JSON sub-schema; 14C.8 LightGBM artifact-log substrate or V0.1 NULL deferral; 14C.9 deprecate `backend/scripts/backtest_gex_zg.py`; 14C.10 `item_promotion_records` startup-read wiring in Items 1 / 5 / 6). Estimated 6–10 weeks for scaffolding-only ship; calibration-grade ship blocked on operator decision on `C-AI-004-4` (chain archive substrate path). Sequencing: must follow Phase 3A (Item 1) and Phase 3B (Item 2) schema-landing milestones to read FK targets.
- [ ] **New phase entry: Phase 3D (or equivalent) — Counterfactual P&L Attribution Layer 2 V0.2** (AI-SPEC-010 §11). Per Spec §11 V0.2 ship-scope: 15 build items decomposed at `AI-SPEC-010.md` §12.2 (14D.0 5 schema migrations — 4 new tables + 1 ALTER on `trading_prediction_outputs` for 3 columns per `B-AI-010-2`; 14D.1 `backend/counterfactual_engine_v2.py` Layer 2 engine per `B-AI-010-1`; 14D.2 surgical Layer 1 fix at `counterfactual_engine.py:213–223` per `B-AI-010-3` + `C-AI-010-1`; 14D.3 4 API accessor functions with `calibration_eligible` filtering per `B-AI-010-5`; 14D.4 slippage formula implementation per `B-AI-010-6`; 14D.5 halt-cycle row-creation wiring at `risk_engine.py` + `prediction_engine.py` per `B-AI-010-7`; 14D.6 versioned width table per `B-AI-010-8`; 14D.7 `feedback:counterfactual:layer2:enabled` Redis flag per `B-AI-010-9`; 14D.8 `counterfactual_reason ↔ decision_outcome` enum mapping function per `B-AI-010-10`; 14D.9 governance update to `system-state.md` per `B-AI-010-11`; 14D.10 Spec §6 cutover migration with operator-locked anchor per `C-AI-010-5`; 14D.11 frontend `useActivationStatus.ts` extension with `shippedDate` + `shippedCommit` fields per `B-AI-010-4`; 14D.12 z-score producer wiring per `C-AI-010-3` operator decision; 14D.13 `closed_trade_path_metrics` + `position_path_samples` table writers (intraday from `position_monitor.py`); 14D.14 tests). Estimated 4–6 weeks for V0.2 ship. **Sequencing: Item 10 V0.2 ships AFTER Phase 3A (Item 1) + Phase 3B (Item 2) schema-landing milestones (FK target dependencies on `ai_governor_decisions`, `strategy_attribution`); Items 5/6/7/9 V0.x ships ONLY AFTER Item 10 V0.2 lands (Items 5/6/7/9 are blocked on Layer 2 calibration-grade attribution).** Layer 2 calibration-grade ship is bilateral with `C-AI-004-4` chain-archive substrate decision (shares dependency).
- [ ] **New phase entry: Phase 4B (or equivalent) — Meta-Labeler V0.2** (AI-SPEC-006 §11). Per Spec §13 V0.2 build scope: estimated **6–10 weeks for V0.2 advisory-only ship** (engine scaffolding + Bayesian three-part hybrid + utility label calculator + bucket calibration + drift detection — assuming Items 1, 2, 4, 5 V0.x ship-completion as upstream prerequisites) + **6+ months for V0.2 production-skip-reduce ship** (gated on accumulating 200+ labeled candidates per Spec §10 line 634). 17 build items decomposed at `AI-SPEC-006.md` §12.2 (canonical numbering 14F.0 through 14F.17). **Sequencing: Phase 4B ships AFTER Phase 3A (Item 1) + Phase 3B (Item 2) + Phase 3C (Item 4) + Phase 3D (Item 10 V0.2 Layer 2) + Phase 3E (Item 5 V0.2) + Phase 4C (Item 7 Memory Retrieval V0.x — un-audited). Item 6 V0.2 IS THE LAST Cluster B alpha-generation phase to ship; Items 9 (Exit Optimizer) and 13 (Drift Detection) consume Item 6 outputs and ship after Phase 4B.** Calendar-blocker depth: 5 upstream specs (Items 1, 2, 4, 5, 7) plus transitive Item 10 V0.2 Layer 2.
- [ ] **New phase entry: Phase 4D (or equivalent) — OPRA Flow Alpha V0.3** (AI-SPEC-008 §11). Per Spec §12 V0.3 build scope: estimated **6–8 weeks for V0.3 advisory-only ship** (engine scaffolding + 20 production features + Lee-Ready aggressor inference + sweep cluster detection + block trade detection + backpressure handling + storage architecture + integration shims). 18 build items decomposed at `AI-SPEC-008.md` §12.2 (canonical numbering 14H.0 through 14H.17). **Sequencing: Phase 4D ships AFTER Phase 4B (Item 6 V0.2 — bilateral cross-spec contract ratification per `C-AI-008-1` deferred-confirmation default option 3); Phase 4D ships ALONGSIDE Phase 4B (Item 6) since Item 8's 5/6/Governor integration shims feed directly into Item 6 V0.2 outputs.** Phase 4D **calendar-blocked on operator pre-implementation gate** per `C-AI-008-3` Databento subscription scope verification. Phase 4D production-binding ship deferred to V0.4+ contingent on Item 6 V0.2 ship + chain-archive maturation per `C-AI-008-4` default option 3 (advisory-only acceptance).
- [ ] **New phase entry: Phase 4C (or equivalent) — Memory Retrieval V0.x** (AI-SPEC-006 §11, conditional on future P1.3.7 audit). Item 7 Memory Retrieval substrate is **calendar-blocker for Item 6 V0.2** per `B-AI-006-6` — Item 6 reads 10 fields from Memory Retrieval Top-20 Analogs surface (`n_similar_cases`, `similarity_weighted_win_rate`, `similarity_weighted_avg_utility`, `similarity_weighted_p25_utility`, `same_strategy_case_count`, `same_event_case_count`, `same_regime_case_count`, `similar_case_tail_loss_rate`, `analog_disagreement_score`, `most_recent_similar_case_age`); underlying substrate (pgvector + `trade_decision_cases` + Top-20 analog query pipeline) absent at HEAD. Phase 4C must complete BEFORE Phase 4B; bilateral with AI-SPEC-001 `B-AI-001-1` `pgvector` enablement prerequisite. **Sequencing: Phase 4C is sized in P1.3.7 audit (un-scheduled at HEAD).** Fallback: if Phase 4C slips, Phase 4B ships in advisory-only mode with `w_memory = 0` on the three-part hybrid (Bayesian prior + logistic scorecard only).
- [ ] **New phase entry: Phase 3E (or equivalent) — Volatility Fair-Value Engine V0.2** (AI-SPEC-005 §11). Per Spec §11 V0.2 ship-scope: 18 build items decomposed at `AI-SPEC-005.md` §12.2, organized into (a) schema bootstrap — 1 new migration for `vol_fair_value_snapshots` per `B-AI-005-5` + 1 ALTER on `item_promotion_records` adding `channel TEXT NULL` per `B-AI-005-10` (bilateral with Item 4 V0.1 ship for pre-emptive inclusion); (b) `backend/vol_fair_value_engine.py` HAR-RV regression engine with multi-component lag structure + EWMA fallback per `B-AI-005-1`; (c) multi-year 5-min SPX bar + daily realized-vol history substrate (NEW `daily_realized_vol_history` and/or `spx_5min_bars` tables OR formal V0.3+ deferral) per `B-AI-005-2` + `B-AI-005-3` + `B-AI-005-15`, bilateral with `B-AI-004-4`; (d) lag-structure feature engineering — `polygon:spx:overnight_gap` producer + `vix_change_from_prior_close` + `event_day_flag` + `RV_so_far_scaled` per `B-AI-005-4`; (e) IV extraction layer `backend/iv_extraction.py` (7 sub-modules: chain cleaning, forward price, model-free implied variance, ATM straddle proxy, 16-delta skew, surface features, term structure) per `B-AI-005-6`, bilateral with `C-AI-005-3` chain archive; (f) terminal distribution simulator (10,000 scenarios + grid MLE over `df ∈ [3, 12]` + regime-conditional df) per `B-AI-005-7`; (g) LightGBM directional tilt **calendar-blocked** on Items 1 + 6 schema-landing milestones per `B-AI-005-8`; (h) strategy-conditional EV table writer (per-strategy quality scores + top-3 ranking; persists to NEW `strategy_ev_table` Supabase table) per `B-AI-005-9`; (i) reliability score state machine (4 states: calibrating / healthy / degraded / halted) with per-channel authority levels + `vol:engine_reliability` Redis key + NEW `vol_engine_reliability_log` table for transition audit trail per `B-AI-005-10`; (j) per-leg spread filter per `B-AI-005-11`, bilateral with `C-AI-005-3` chain archive + `B-AI-010-6` slippage formula via `C-AI-010-3` shared `features:*` namespace; (k) replay calibration harness (Baselines A–F) **calendar-blocked** on Item 4 V0.1 + chain archive + Items 2 + 10 calibration_eligible substrate per `B-AI-005-12`, escalates to `C-AI-005-3`; (l) retraining cadence orchestration (daily HAR + weekly Student-t df + weekly EWMA + monthly decision-score weights) per `B-AI-005-13`, bilateral with `B-AI-004-4` + `B-AI-004-10`; (m) output Redis namespace `vol:*` (`vol:rv_forecast_remaining`, `vol:engine_reliability`, `vol:ev_table_top3`, `vol:engine_updated_at`) + freshness gate substrate buildout — wire `_safe_redis()` caller for vol-engine input keys (`polygon:spx:realized_vol_20d`, `polygon:vix:z_score`, `polygon:vvix:z_score`, `gex:updated_at`) AND stand up producers for staleness keys (`polygon:vix:updated_at`, `polygon:spx:updated_at`, `gex:updated_at`) per `B-AI-005-16` (HIGH-IMPACT confirmation of carry-forward #4; bilateral with AI-SPEC-001 `B-AI-001-6` — same substrate buildout serves both items); (n) LLM-facing compact summary (9 fields per Spec §9) consumed by Item 1 Card per `B-AI-001-2`; (o) D-016 disposition wiring per `C-AI-005-1` operator decision — default option 2: HAR-RV is NEW signal feeding EV table only; D-016 stays unchanged; existing `polygon:spx:realized_vol_20d` consumers continue reading legacy 12A key; (p) Layer 1 / Layer 2 cutover policy wiring per `C-AI-005-2` operator decision — default option 1: Layer 1 stays put as backward-compat surface; Layer 2 produces parallel `vol:*` keys; (q) calibration-eligible classifier for daily-RV history per `B-AI-005-17` — pre-`fc64840` rows marked `calibration_eligible = false` (concrete commit-hash anchor `fc64840` dated 2026-04-20 18:43:14 -0400 verified via `git show fc64840 --format="%ci %s"`); (r) tests covering HAR-RV regression, EWMA fallback, IV extraction, EV table generation, reliability state-machine transitions, spread filter, replay calibration baselines A–F, retraining cadence, freshness gate. Estimated **8–12 weeks for V0.2 advisory-only ship** (engine scaffolding + Layer 2 buildout) + **6+ months for V0.2 calibration-grade ship** (gated on chain archive + multi-year RV history substrate maturation per `C-AI-005-3`). **Sequencing: Item 5 V0.2 ships AFTER Phase 3B (Item 2 schema) + Phase 3C (Item 4 schema + scaffolding) + Phase 3D (Item 10 Layer 2 schema + `trade_counterfactual_cases` table + `get_vol_engine_replay_cases()` accessor) — Item 5 reads `strategy_attribution.calibration_eligible` + `trade_counterfactual_cases.calibration_eligible` substrates; calibration-grade promotion (Spec §7 Baseline E beats Baseline F) is bilateral with Phase 3C (Item 4 V0.1 calibration-grade-capable status). Item 5 V0.2 ships BEFORE Items 6 / 9 V0.x (Items 6 / 9 consume Item 5 EV table outputs).**

### `TASK_REGISTER.md`
- [ ] **New section: §14A — Risk Governor implementation** (AI-SPEC-001 §11). Sub-items per `AI-SPEC-001.md` §12.2 (14A.0 schema bootstrap, 14A.1 freshness substrate, 14A.2 Card generator, 14A.3 Governor LLM agent, 14A.4 Final Arbiter, 14A.5 caching, 14A.6 champion/challenger, 14A.7 cost circuit breaker, 14A.8 observability, 14A.9 replay calibration, 14A.10 promotion-gate machinery).
- [ ] **New section: §14B — Strategy Attribution implementation** (AI-SPEC-002 §11). Sub-items per `AI-SPEC-002.md` §12.2: (14B.0 migrations for `strategy_attribution` + `strategy_utility_labels` + optional `closed_trade_path_metrics` substrate, FK constraints deferred per `B-AI-002-3` two-phase plan; 14B.1 `backend/eod_attribution_engine.py` orchestrator + `attribution:*` Redis namespace; 14B.2 path-metrics copy-on-insert OR explicit NULL deferral path per `B-AI-002-2`; 14B.3 `calibration_eligible` classifier per Spec §2 line 120 with operator-controlled false/true threshold; 14B.4 EOD job wiring at 5:00 PM slot per HANDOFF_4 schedule; 14B.5 legacy `trading_positions` row backfill / cutover script per `B-AI-002-6`; 14B.6 path-metric immutability trigger or RLS; 14B.7 tests covering FK NULL paths, idempotency, halted-blocked-cycle synthetic case, and `outcome_mode` ↔ `exit_reason` mapping per `B-AI-002-4`; 14B.8 disposition of legacy `attribution_*` BOOLEAN columns on `trading_positions` per `C-AI-002-3`).
- [ ] **New section: §14C — Replay Harness implementation** (AI-SPEC-004 §11). Sub-items per `AI-SPEC-004.md` §12.2: (14C.0 schema bootstrap — 4 migrations for `replay_eval_runs` + `replay_eval_cases` + `replay_eval_results` + `item_promotion_records`, with `environment = 'research'` CHECK constraints + seed `item_promotion_records` initial rows for Items 1 / 5 / 6 with default `'advisory' or 'disabled'`; 14C.1 chain-archive substrate — `tradier_chain_archive` / `databento_opra_archive` table + writer hooks OR formal V0.1 deferral with operator-accepted advisory-only ship per `C-AI-004-4`; 14C.2 Polygon historical 1-min fetch helper wrapping `/v2/aggs/ticker/I:SPX/range/1/minute/{from}/{to}` per `B-AI-004-4`; 14C.3 news/event `published_at TIMESTAMPTZ` extension on `trading_ai_briefs` + sentiment_agent + economic_calendar + earnings_calendar per `B-AI-004-6`; 14C.4 8 `backend/replay_*.py` files per Spec §4 lines 532–554; 14C.5 Step 0 validation tests — 4 binary checks (pricing $0.10 / SPX 99% bars / VIX correlation 0.99 / slippage MAE $0.10) per Spec §2 lines 168–229; 14C.6 walk-forward implementation — rolling 60/20/20 + 10-session step + graduated-authority sample-size tier table per Spec §6 lines 698–732; 14C.7 decision-card JSON sub-schema spec for the 7 JSONB columns of `replay_eval_cases` landed as `trading-docs/04-modules/ai-architecture/replay-decision-card-schema.md` per `B-AI-004-9` + `C-AI-004-2`; 14C.8 LightGBM artifact-log substrate — `model_artifact_log` table OR V0.1 NULL deferral with `model_state_source = 'unavailable'` per `B-AI-004-10`; 14C.9 deprecate `backend/scripts/backtest_gex_zg.py` (470 lines) — move to `backend/scripts/_legacy/` or delete; retire `test_b2_backtest.py` + `test_backtest_gex_zg.py` per `B-AI-004-2` + `B-AI-004-5`; 14C.10 `item_promotion_records` startup-read wiring in Items 1, 5, 6 per Spec §4 lines 521–525 + `C-AI-004-3`). Sequencing: must follow Phase 3A (Item 1) and Phase 3B (Item 2) schema-landing milestones.
- [ ] **New section: §14D — Counterfactual P&L Attribution Layer 2 implementation** (AI-SPEC-010 §11). Sub-items per `AI-SPEC-010.md` §12.2: (14D.0 5 schema migrations — 4 new tables (`trade_counterfactual_cases`, `strategy_utility_labels`, `closed_trade_path_metrics`, `position_path_samples`) + 1 ALTER on `trading_prediction_outputs` adding `strategy_hint TEXT NULL`, `strategy_structure JSONB NULL`, `candidate_source TEXT NULL` per Spec §3 lines 148–153 + `B-AI-010-2`; 14D.1 `backend/counterfactual_engine_v2.py` (canonical name per `B-AI-010-1`) Layer 2 engine producing case-typed rows for `trade_counterfactual_cases` with `case_type ∈ {actual, counterfactual, synthetic}` and `case_weight ∈ {1.0, 0.5, 0.2}` per Spec §4 lines 198–225; 14D.2 surgical Layer 1 bug fix at `counterfactual_engine.py:213–223` — replace hardcoded `strategy = "iron_condor"` fallback with NULL-return path when `strategy_hint` missing on source row, conditional on `C-AI-010-1` operator decision (option 1 default); update existing tests to cover NULL-return path; 14D.3 4 API accessor functions in Layer 2 module — `get_meta_labeler_training_data()`, `get_vol_engine_replay_cases()`, `get_adversarial_block_outcomes()`, `get_exit_optimizer_training_paths()` — with `calibration_eligible = true` + `simulation_status = 'success'` filtering enforced at function-signature layer per Spec §6 lines 391–410 + `B-AI-010-5`; 14D.4 parametric slippage formula implementation as `backend/replay_slippage_model.py` (or method on Layer 2 engine) per Spec §5 lines 332–376 + `B-AI-010-6`, with explicit dependencies on `vix_z` / `spread_z` producers (resolved by `C-AI-010-3`) and `half_spread_estimate` source (bilateral with `C-AI-004-4`); 14D.5 halt-cycle counterfactual row-creation wiring at `risk_engine.py:234–235` (3-loss reduction) AND `prediction_engine.py:688–691` (5-loss session halt) inserting `trade_counterfactual_cases` rows with `simulation_status = 'pending_eod_label'` per Spec §7 lines 442–454 + `B-AI-010-7`; 14D.6 versioned width table — new `vix_spread_width_table` Supabase table OR YAML config in `backend/config/` per `B-AI-010-8` (default option (a) versioned table); 14D.7 `feedback:counterfactual:layer2:enabled` Redis feature flag (fail-closed DISABLED initially) gating Layer 2 EOD job independently of Layer 1 per `B-AI-010-9` + reuse `set-feature-flag` Edge Function; 14D.8 `counterfactual_reason ↔ decision_outcome` enum mapping function exposed for cross-spec consumers per `B-AI-010-10`; 14D.9 `system-state.md` `counterfactual_engine` field tracking `{ layer_1_phase, layer_2_phase, calibration_eligible_classifier_active, chain_archive_capable }` per `B-AI-010-11`; 14D.10 Spec §6 cutover migration applying retroactive `label_quality = 'legacy_observability_only'` lockdown to pre-cutover-anchor rows in `counterfactual_pnl` columns + new `trade_counterfactual_cases` rows per `C-AI-010-5` operator decision (default option 1: concrete commit hash post-Commit-4 ship); 14D.11 frontend `src/hooks/trading/useActivationStatus.ts` extension — add `shippedDate?: string` + `shippedCommit?: string` to `ActivationFlag` interface; populate Counterfactual Tracking entry with `shippedDate: '2026-04-20'` + `shippedCommit: '2400e98'` (Layer 1) per `B-AI-010-4`; 14D.12 z-score producer wiring per `C-AI-010-3` operator decision (default option 3: shared producer in `features:vix_z` / `features:spread_z` Redis namespace owned by calibration_engine per D-019); 14D.13 `closed_trade_path_metrics` + `position_path_samples` writer hooks integrated into `position_monitor.py` per Spec §4 lines 276–308 (intraday samples + EOD aggregation); 14D.14 tests covering Layer 2 engine, accessor functions with calibration_eligible filtering, slippage formula edge cases, halt-cycle row creation, NULL-return Layer 1 fix, and case-type weighting). **Sequencing: Item 10 V0.2 ships AFTER Phase 3A (Item 1 schema) + Phase 3B (Item 2 schema) but BEFORE any Cluster B/C item ships (Items 5/6/7/9 are calendar-blocked on Layer 2 calibration-grade outputs).**
- [ ] **New section: §14E — Volatility Fair-Value Engine implementation** (AI-SPEC-005 §11). Sub-items per `AI-SPEC-005.md` §12.2 (canonical numbering 14E.0 through 14E.18): (**14E.0** Schema bootstrap — 1 migration for `vol_fair_value_snapshots` per `B-AI-005-5` with full DDL covering output fields + reliability state + EV table top-3 JSONB + `calibration_eligible BOOLEAN NOT NULL DEFAULT false`; 1 ALTER on `item_promotion_records` adding `channel TEXT NULL` per `B-AI-005-10` — bilateral with Item 4 V0.1 ship for pre-emptive inclusion; **14E.1** `backend/vol_fair_value_engine.py` (canonical name per `B-AI-005-1`) HAR-RV regression engine with method orchestrator class — `compute_har_rv_forecast()`, `compute_ewma_fallback()`, `extract_implied_volatility()`, `simulate_terminal_distribution()`, `apply_lightgbm_tilt()`, `compute_strategy_ev_table()`, `compute_engine_reliability_score()`, `apply_spread_filter()`, `produce_compact_summary_for_card()`; reuses `polygon_feed.py:_compute_spx_features` for live 5-min bar consumption; **14E.2** Multi-year RV history substrate per `B-AI-005-2` + `B-AI-005-3` + `B-AI-005-15` — NEW `daily_realized_vol_history` table (`session_date DATE PRIMARY KEY`, `realized_vol_20d NUMERIC`, `produced_at TIMESTAMPTZ`, `calibration_eligible BOOLEAN NOT NULL DEFAULT false`, `cutover_anchor TEXT`) + writer hook in `polygon_feed.py:_append_spx_daily_return_if_due` (line 416) that ALSO writes to the new table; bilateral with `B-AI-004-4` Polygon historical-bars fetch — backfill script populates 252-session minimum history at V0.2 ship time; Layer 1 12A writer (`polygon_feed.py:486`) stays put per `C-AI-005-2` default option 1; **14E.3** Lag-structure feature engineering per `B-AI-005-4` — new methods `compute_overnight_gap()` (producer for `polygon:spx:overnight_gap` — currently consumer-only orphan), `compute_vix_change_from_prior_close()`, `compute_event_day_flag()` (joins `economic_calendar` + `earnings_calendar` on session date), `compute_rv_so_far_scaled()` (intraday running variance from 5-min bar archive); **14E.4** EWMA fallback per `B-AI-005-1` — lambda calibrated in replay; persists to new `ewma_calibration_history` table (or method on engine) with `lambda` + `last_calibrated_at` + `qlike_vs_har_rv` for fallback decision logic (Spec §1 line 90 — fallback when HAR-RV underperforms by >15% QLIKE over 20 sessions); **14E.5** IV extraction layer per `B-AI-005-6` — `backend/iv_extraction.py` (or methods on engine) implementing 7 sub-modules: chain cleaning, forward price via put-call parity, model-free implied variance via truncated option-strip, ATM straddle proxy, 16-delta skew, surface features (smile_asymmetry, smile_width, tail_width, term_slope), time-to-expiry authority degradation; live path reads from `tradier_feed.py` quote stream; replay path bilateral with chain archive per `C-AI-005-3` option 2 or 3; **14E.6** Terminal distribution simulator per `B-AI-005-7` — `backend/vol_terminal_distribution.py` (or methods on engine) with N=10,000 scenarios + grid MLE over `df ∈ [3, 12]` + regime-conditional df; performance: cache scenario draws by `(time_bucket, regime_bucket, RV_decile)` to amortize Monte Carlo cost; **14E.7** LightGBM directional tilt per `B-AI-005-8` — **calendar-blocked** on Item 1 V0.1 (`governor.uncertainty_score` / `governor.event_class` / `governor.data_freshness_warning` outputs) + Item 6 V0.x (`ml_confidence` / `ml_calibration_ok` outputs); V0.1 advisory-only ship runs with `mu_tilt = 0` always; **14E.8** Strategy-conditional EV table per `B-AI-005-9` — implements per-strategy quality scores + threshold gates per Spec §4 lines 309–414; output schema includes 5 strategies (IC, IB, PCS, Long Straddle, DCS) × per-strategy quality fields; persistence to NEW `strategy_ev_table` Supabase table with versioned rows (`ev_table_version`, `computed_at`); anti-double-counting rule (Spec §1 line 187–190): `skew_z` for decision penalties; `smile_asymmetry_score` / `smile_width_score` / `tail_width_score` for meta-labeler features (separate output column); **14E.9** Reliability score state machine per `B-AI-005-10` — per-channel authority levels (`short_gamma_filter_authority`, `long_vol_enable_authority`, `strategy_ev_ranking_authority`) + degradation triggers + 30-session cooldown + 20-qualified-signals + 4-criteria recovery state machine; bilateral with Item 4 `item_promotion_records.channel TEXT NULL` extension; per-channel authority published to `vol:authority_level:{channel}` Redis keys + persisted to `vol_fair_value_snapshots`; new `vol_engine_reliability_log` table for transition audit trail; **14E.10** Spread filter per `B-AI-005-11` — per-leg admission-time filter for entry; advisory for exits; bilateral with chain archive per `C-AI-005-3`; bilateral with AI-SPEC-010 `B-AI-010-6` slippage formula for shared `features:vix_z` / `features:spread_z` namespace per `C-AI-010-3` shared producer; **14E.11** Replay calibration harness per `B-AI-005-12` — **calendar-blocked** on Item 4 V0.1 (`replay_eval_cases` table + Step 0 binary gates + walk-forward harness); V0.2 ship plugs Item 5's HAR-RV / EWMA / EV table into Item 4's harness as Baseline C / B / D respectively; Baseline E (full stack) and Baseline F (full stack minus Vol Engine) ride on the harness; **14E.12** Retraining cadence orchestration per `B-AI-005-13` — extension of `backend/model_retraining.py` (or new `backend/vol_retraining.py`); cron jobs in `backend/main.py` for daily HAR retrain (252-session rolling), weekly Student-t df + EWMA, monthly decision-score weight recalibration OR after 60 new qualified signals; bilateral with `B-AI-004-4` Polygon historical-bars + `B-AI-004-10` model artifact-log substrate; **14E.13** Output Redis namespace `vol:*` per `B-AI-005-16` — producer: engine module writes after each decision-time pass; bilateral with `B-AI-001-6` freshness substrate — `vol:engine_updated_at` is the producer for Item 5's freshness gate; `_safe_redis()` caller wiring required (or replacement freshness mechanism per `B-AI-001-6`); HIGH-IMPACT confirmation of carry-forward #4; **14E.14** LLM-facing compact summary per Spec §9 lines 675–687 — format: 9 fields (`0DTE implied move`, `HAR forecast realized move`, `iv_rv_ratio`, `skew_z`, `term structure`, `strategy_ev_table top-3`, `late-day penalty`, `engine confidence`, `authority_level per channel`, `degradation_flags`); bilateral with Item 1 Card schema per `B-AI-001-2`; **14E.15** D-016 disposition wiring per `C-AI-005-1` operator decision — default option 2: HAR-RV is NEW signal; D-016 unchanged; no `prediction_engine.py` changes; `polygon:spx:realized_vol_20d` continues feeding existing IV/RV filter (line 706); `vol:rv_forecast_remaining` is a NEW key feeding Item 5's `strategy_ev_table` only; option 1 or 3 require `prediction_engine.py` changes + D-016 amendment migration; **14E.16** Layer 1 / Layer 2 cutover policy implementation per `C-AI-005-2` operator decision — default option 1: Layer 1 stays put as backward-compat surface; no Layer 1 changes; Layer 2 ships parallel `vol:*` keys; `system-state.md` `vol_fair_value_engine.layer_1_legacy_realized_vol_writer_status = 'active_backward_compat'`; option 2 or 3 require migration of existing consumers; **14E.17** Calibration-eligible classifier for daily-RV history per `B-AI-005-17` — records `produced_at < fc64840 deploy timestamp (2026-04-20 18:43 -0400)` are marked `calibration_eligible = false`; records `produced_at >= fc64840` AND `produced_at < <Commit_4_deploy_date>` (per `C-AI-010-5`) are marked `calibration_eligible = true` provisionally; records `produced_at >= <Commit_4_deploy_date>` are marked `calibration_eligible = true` definitively; folds into AI-SPEC-002 `B-AI-002-6` legacy cutover policy framework; **14E.18** Tests covering HAR-RV regression (deterministic test with known synthetic data; QLIKE vs EWMA on holdout), EWMA fallback (transition logic), IV extraction (model-free strip on synthetic chain), EV table generation (per-strategy quality scores + threshold gates), reliability state-machine transitions (degradation triggers + recovery criteria), spread filter (per-leg + per-strategy budget), replay calibration baselines A–F (Baseline E beats F on synthetic data), retraining cadence (cron job entry points), freshness gate (`_safe_redis()` caller behavior + stale value handling)). **Sequencing: Item 5 V0.2 ships AFTER Phase 3B (Item 2 schema landings) + Phase 3C (Item 4 V0.1 scaffolding) + Phase 3D (Item 10 Layer 2 schema landings); Item 5 V0.2 ships BEFORE Items 6 / 9 V0.x (consumers of EV table); calibration-grade promotion to authority bilateral with `C-AI-004-4` chain-archive substrate operator decision.**

- [ ] **New section: §14H — OPRA Flow Alpha V0.3 implementation** (AI-SPEC-008 §11). Sub-items per `AI-SPEC-008.md` §12.2 (canonical numbering 14H.0 through 14H.17): (**14H.0** Schema bootstrap — 1 migration for `opra_flow_feature_bars` per `B-AI-008-3` with full DDL (21 NUMERIC columns for 20 production features + 1 derived `zero_dte_minus_1_3dte_pressure` per `B-AI-008-18`, `features_jsonb JSONB`, `data_quality_flags JSONB`, `lee_ready_inferred_share NUMERIC`, `unknown_aggressor_share NUMERIC`, `calibration_eligible BOOLEAN NOT NULL DEFAULT false`); 1 migration for `opra_flow_events` per `B-AI-008-3` with full DDL (event_type CHECK constraint with sweep/block/burst, expiry/strike/right enums, aggressor_sign / direction / confidence); recommended migration filename: `2026MMDD_add_opra_flow_substrate.sql`; **14H.1** `backend/flow_alpha_engine.py` (canonical name per `B-AI-008-1`) deterministic feature producer engine module — orchestrator class with methods `infer_aggressor_sign()`, `compute_customer_delta()`, `detect_sweep_clusters()`, `detect_block_trades()`, `compute_directional_pressure()`, `compute_atm_delta_pressure()`, `compute_short_delta_pressure()`, `compute_dealer_hedge_pressure_z()`, `compute_flow_persistence()`, `compute_zero_dte_pressure()`, `compute_call_put_imbalance()`, `compute_sweep_premium_share()`, `compute_quality_flags()`, `produce_governor_compact_summary()`; **14H.2** Lee-Ready aggressor inference per `B-AI-008-5` — `infer_aggressor_sign(trade, current_nbbo, prior_tick)` method with 30s prior-tick lookup + quote-stale guard; requires NBBO data class subscription per `C-AI-008-3` operator gate; unit tests cover all aggressor side cases (above/below mid, locked/crossed, NBBO_age > 1s, tick-rule fallback); **14H.3** Sweep cluster detection per `B-AI-008-6` — `detect_sweep_clusters(trades_buffer)` method with multi-exchange print disambiguation; bilateral with `C-AI-008-3` Databento subscription scope; **14H.4** Block trade detection per `B-AI-008-7` — `detect_block_trades(trade, time_bucket, symbol)` method with rolling 95th percentile distributions per time-bucket and per-symbol; refresh nightly from prior 20 trading days; **14H.5** All 20 production features per `B-AI-008-4` — feature method per spec section 1 (lines 22–76) with mathematical formulas; each feature gets unit test against synthetic OPRA tape data; **14H.6** Quality tracking features per `B-AI-008-8` — `lee_ready_inferred_share_5m` + `unknown_aggressor_share_5m` emission to `opra_flow_feature_bars` columns AND `opra:flow_alpha:latest` JSONB; threshold parametric (default 0.30) operator-tunable in `system-state.md` `opra_flow_alpha.unknown_aggressor_share_threshold`; **14H.7** Separate Railway worker process per `B-AI-008-9` — new Procfile entry `flow_alpha_worker: python -m backend.flow_alpha_engine` (or equivalent process manager wiring); ensures feature computation latency does NOT block trading cycle; **14H.8** Backpressure handling per `B-AI-008-10` — `BackpressureMonitor` class with state machine (normal / degraded / stale); state changes trigger Redis flag updates (`opra:flow_alpha:health` JSONB with `state`, `lag_seconds`, `unknown_aggressor_share`); 5-second staleness threshold per Spec §4 lines 257–258; **14H.9** Output Redis namespace `opra:flow_alpha:*` per `B-AI-008-2` — 4 keys: `:latest` (TTL = 15 minutes; full feature snapshot JSONB), `:bars:1m` (LTRIM 120 bars; rolling minute-bar archive), `:bars:5m` (LTRIM 80 bars; rolling 5-minute-bar archive), `:health` (TTL = 5 minutes; backpressure state); producer wiring at `flow_alpha_engine.py`; consumer wiring at Items 5/6/Governor sites for stale-flag consumption (or bilateral implementation per `B-AI-001-6` if `_safe_redis()` design replaced); HIGH-IMPACT confirmation of carry-forward #4 per `B-AI-008-16`; **14H.10** Storage namespace coexistence per `B-AI-008-17` — `databento:opra:*` namespace remains as Layer 0 raw-feed substrate (continues serving GEX engine via `databento_feed.py:349 lpush`); `opra:flow_alpha:*` namespace ships as Layer 2 alpha-feature output; cutover is additive (no migration of `databento:opra:trades` consumers); folds into D-023 enrichment item (v); **14H.11** Item 5 integration shim (8 features) per `B-AI-008-13` — `opra:flow_alpha:latest` Redis JSONB output OR `opra_flow_feature_bars` Supabase row read; **bilateral with `C-AI-008-1`** — Item 5 spec is silent on the 8-feature contract at the field-name level (V7); Item 5 acknowledges OPRA flow abstractly only (lines 390 + 549); cross-spec contract ratification via `C-AI-008-1` operator decision required before integration tests can be authored; default option 3 (DEFERRED BILATERAL CONFIRMATION) ships shim with `cross_spec_contract_ratified = false` flag; **14H.12** Item 6 integration shim (20 features + 1 quality flag) per `B-AI-008-14` — `opra:flow_alpha:latest` Redis JSONB output AND/OR `opra_flow_feature_bars` Supabase row read; **bilateral with `C-AI-008-1`** — Item 6 spec is fully silent on Item 8 as a producer surface (V8 + V10 zero matches); Item 6's 47 named feature fields per `B-AI-006-2`–`B-AI-006-6` come from 5 producer surfaces with Item 8 NOT named; calendar-blocked on Item 6 V0.2 ship per `B-AI-006-1`; **14H.13** Governor compact-summary emission per `B-AI-008-15` — `opra:flow_alpha:governor_summary` Redis JSONB output (TTL = 5 minutes); 5/6-field JSON dictionary (`flow_direction`, `flow_strength`, `vol_demand`, `sweeps`, `price_alignment`, `confidence` — count to be confirmed via Class A `A-AI-008-2`); bilateral with Item 1 Card schema per `B-AI-001-2`; **14H.14** Replay process per `B-AI-008-11` — **calendar-blocked** on Item 4 V0.1 ship + chain-archive substrate sufficiency per `C-AI-004-4`; bilateral with `B-AI-006-1` Item 6 V0.2 ship for label provider; V0.3 advisory-only ship runs without replay validation per `C-AI-008-4` default option 3; **14H.15** Production acceptance per `B-AI-008-12` — `Item 6 with OPRA features must beat Item 6 without OPRA features` joint test; **calendar-blocked** on Item 6 V0.2 ship + chain-archive maturation + 90+/30+ session accumulation per `C-AI-008-4`; production-acceptance flip to V0.4+ per `C-AI-008-4` default option 3; **14H.16** flow_agent narrowing per `C-AI-008-2` default option 1 — existing `backend_agents/flow_agent.py` (343 lines at HEAD; brief generator producing `ai:flow:brief` Redis key consumed by `synthesis_agent.py:93` + `main.py:1917`) narrows to LLM summarizer reading from `flow_alpha_engine` output (5/6-field Governor compact summary per `B-AI-008-15`); content shifts from Unusual Whales / put-call ratio brief to narrative summary over Item 8's compact summary; preserves backward-compat with synthesis_agent input contract; **14H.17** Tests covering Lee-Ready aggressor inference (positive/negative cases for above/below mid, NBBO_age > 1s, tick-rule fallback, locked/crossed market), sweep cluster detection (positive/negative cases for cluster reconstruction), block trade detection (95th percentile triggers), 20 feature formulas (synthetic OPRA tape data), backpressure state machine transitions, freshness gate (`_safe_redis()` caller behavior + stale value handling), Item 5 + Item 6 + Governor integration shims (round-trip data integrity)). **Sequencing: Phase 4D (Item 8 V0.3) ships AFTER Phase 4B (Item 6 V0.2); Phase 4D ships ALONGSIDE Phase 4B since Item 8's integration shims feed directly into Item 6 V0.2 outputs; Phase 4D calendar-blocked on `C-AI-008-3` operator pre-implementation Databento subscription scope verification.**

- [ ] **New section: §14F — Meta-Labeler V0.2 implementation** (AI-SPEC-006 §11). Sub-items per `AI-SPEC-006.md` §12.2 (canonical numbering 14F.0 through 14F.17): (**14F.0** Schema bootstrap — 1 migration for `meta_labeler_decisions` per `B-AI-006-11` with full DDL covering output fields + feature snapshot JSONB + `calibration_eligible BOOLEAN NOT NULL DEFAULT false` + `version_tag TEXT NOT NULL`; 1 ALTER on `strategy_attribution.decision_outcome` CHECK constraint adding `blocked_meta_labeler` + `reduced_meta_labeler` enum values per `B-AI-006-9`; 1 ALTER on `item_promotion_records` adding `scope JSONB NULL` for per-strategy × per-time-bucket authority granularity per Spec §9.4 — bilateral with `B-AI-005-10` `channel TEXT NULL` extension; **14F.1** `backend/meta_labeler_engine.py` (canonical name per `B-AI-006-1`) Bayesian three-part hybrid engine with method orchestrator class — `compute_p_prior()`, `compute_p_logistic()`, `compute_p_memory()`, `compute_p_take()`, `compute_p_eff()`, `compute_meta_size_cap()`, `compute_strategy_utility()`, `compute_calibrated_buckets()`, `compute_drift_metrics()`, `compute_authority_state()`, `produce_compact_summary_for_card()`; **14F.2** Strategy-specific utility label calculator per `B-AI-006-8` — methods `compute_U_IC()`, `compute_U_IB()`, `compute_U_PCS()`, plus optional `compute_U_LC()`, `compute_U_LP()` per `B-AI-006-12` resolution (2 long-side additions per DP §273 v1.9; D-023 default-and-alternative on the 3 not-yet-covered canonicals — `call_credit_spread`, `debit_put_spread`, `calendar_spread`); reads `MAE_R`, `max_loss_zone_peak`, `max_loss_zone_twap`, `short_strike_breach_flag`, `event_day_short_gamma_flag`, etc. from `closed_trade_path_metrics` (Item 10 V0.2 owner per `B-AI-010-2`) + `strategy_attribution` (Item 2); produces `y_take` binary + `quality_score` continuous per Spec §1 lines 146–150; **14F.3** Hierarchical Bayesian prior with smooth pooling per `B-AI-006-1.b` — 5-level hierarchy (`global` / `strategy` / `strategy×regime` / `strategy×regime×time_bucket` / `strategy×regime×time_bucket×event_bucket`); smooth-pooling formula `p_level = (wins_level + k * p_parent) / (n_level + k)` with `k=20` (operator-tunable per `C-AI-006-3` default option 1 via `system-state.md`); **14F.4** Regularized logistic scorecard per `B-AI-006-1` — sklearn `LogisticRegression(penalty='l2', C=0.5)` with max 30–50 features; uses `strategy_type` one-hot + `strategy × regime` + `strategy × vol_edge` + `strategy × governor_uncertainty` interactions per Spec §4.2; **14F.5** Memory analog evidence blender per Spec §13 line 750 — calendar-blocked on Item 7 Memory Retrieval V0.x per `B-AI-006-6`; case weights harmonized per `B-AI-006-13` resolution (default: 1.0 / 0.5 / 0.2 across both training and memory); **14F.6** Bayesian bucket calibration with bootstrap CIs per Spec §5 lines 397–435 — 5-bucket Beta posterior (`alpha`, `beta` operator-tunable per `C-AI-006-3`); credible interval as confidence band; **14F.7** Time-bucket-aware prior structure per Spec §8 — 5 time buckets (open / late_morning / midday / late / exit_only); separate priors with cross-bucket sparse pooling; per-bucket sample-size caps (`< 20: meta_size_cap = 0.25`; `< 10: advisory only`); **14F.8** Wilson-interval-based drift detection per `B-AI-006-15` partial — `Wilson_lower_95(observed_loss_rate, n) > max(1.5 * expected_loss_rate, expected_loss_rate + 0.10)`; rolling-30 + rolling-50 windows for Brier / ECE / cumulative top-bucket utility; PSI > 0.25 trigger for feature drift on `iv_rv_ratio` / `skew_z` / `governor_uncertainty` / strategy mix / time-of-day mix; **14F.9** Authority degradation per strategy × time_bucket per Spec §9.4 — degradation by scope first, escalating to global only if multiple buckets fail; Spec §9.4 line 622 RECOVERY semantics per `C-AI-006-1` operator decision (default option 2 — operator-mediated recovery only); **14F.10** Replay validation harness with walk-forward per Spec §10 — bilateral with Item 4 V0.1 `replay_eval_cases` substrate (calendar-blocker); promotion gates per Spec §10 lines 654–662 (Brier ≥5% beat baseline, ECE ≤ 0.12, top probability bucket has positive cumulative utility, bottom probability bucket underperforms top, max drawdown does not worsen, worst day does not worsen, trade retention ≥ 50%); **14F.11** Iran-day-style canonical test cases per Spec §12; **14F.12** `decision_outcome` writer wiring per `B-AI-006-9` — Item 6 writes `blocked_meta_labeler` / `reduced_meta_labeler` to `strategy_attribution.decision_outcome` per D-023 enrichment item (t); **14F.13** `calibration_eligible` filter wiring per `B-AI-006-10` — training-set queries filter on `calibration_eligible = true` across all 4 producer-substrate tables; **14F.14** Output Redis namespace `meta:*` per `B-AI-006-17` — producer: engine module writes after each decision-time pass; bilateral with `B-AI-001-6` freshness substrate — `meta:engine_updated_at` is the producer for Item 6's freshness gate; **14F.15** Layer 1 / Layer 2 cutover policy implementation per `C-AI-006-2` operator decision — default option 1 (Layer 1 stays put as legacy observability): no Layer 1 changes; Layer 2 ships parallel; `system-state.md` `meta_labeler.layer_1_phase = 'dormant_legacy_observability'`; option 2 or 3 require Layer 1 code retirement OR champion-challenger wiring; **14F.16** LLM-facing compact summary per Spec §0 line 17 — Item 6 outputs (`p_take`, `meta_size_cap`, `reason_codes`) flow into Item 1 Card via `B-AI-001-2` Card schema delta; bundled with Item 1 V0.1 Card schema landing; **14F.17** Tests covering Bayesian three-part hybrid (deterministic test with known synthetic feature vector; QLIKE-equivalent calibration metric on holdout), strategy utility label calculator (per-strategy unit tests), Bayesian prior smooth pooling (parent-shrinkage transition logic at n=5 / n=20 / n=80), logistic scorecard (L2 penalty + max-features cap), bucket calibration (Beta posterior credible interval), drift detection (Wilson interval + PSI), authority degradation per scope (strategy × time_bucket → global escalation), replay validation walk-forward (week-by-week sliding train/validate), Iran-day canonical test cases, freshness gate (`_safe_redis()` caller behavior + stale value handling)). **Sequencing: Item 6 V0.2 ships AFTER Phase 3A (Item 1) + Phase 3B (Item 2) + Phase 3C (Item 4) + Phase 3D (Item 10 V0.2 Layer 2) + Phase 3E (Item 5 V0.2) + Phase 4C (Item 7 Memory Retrieval V0.x); Items 9 and 13 V0.x ships AFTER Phase 4B (consume Item 6 outputs).**

### `system-state.md`
- [ ] **Operational state addition: `ai_risk_governor` field** (AI-SPEC-001 §11). Tracks `{ phase: 'not_started' | 'v0.1_advisory' | 'v0.2_paper_binding' | 'v0.2_small_live' | 'production' | 'demoted', authority_promoted_at: timestamp | null, last_demotion_reason: text | null }` (or equivalent). Tracks promotion-gate progression per Spec §12 + §17.
- [ ] **Operational state addition: `strategy_attribution` field** (AI-SPEC-002 §11). Tracks `{ phase: 'not_started' | 'v0.1_shipping' | 'v0.1_active' | 'v0.2_calibration_eligible_enabled', last_eod_run_at: timestamp | null, last_eod_run_status: 'success' | 'partial' | 'failed' | null, calibration_eligible_classifier_active: boolean, legacy_backfill_complete: boolean }`. Tracks Item 2 EOD engine health and the `calibration_eligible` flag's promotion (Spec §2 line 120 — initially false for all rows; operator-controlled enable once classifier validated).
- [ ] **Operational state addition: `closed_trade_path_metrics_substrate` field** (AI-SPEC-002 §11, conditional on `B-AI-002-2` resolution). Tracks `{ status: 'not_required' | 'pending' | 'shipping' | 'active', deferral_decision: 'v0.1_null_placeholders' | 'v0.1_substrate_built' | null }`. Only populated if operator decision on `B-AI-002-2` chooses deferral path (option B) instead of substrate-built path (option A).
- [ ] **Operational state addition: `replay_harness` field** (AI-SPEC-004 §11). Tracks `{ phase: 'not_started' | 'v0.1_scaffolding' | 'v0.1_active' | 'v0.1_advisory_only' | 'v0.1_calibration_grade' | 'demoted', last_run_id: uuid | null, last_run_status: 'success' | 'partial' | 'failed' | null, last_step0_validation_pass: bool | null, calibration_grade_capable: bool, chain_archive_status: 'absent' | 'forward_archival_in_progress' | 'paid_historical_acquired' | 'sufficient' }`. Tracks the `C-AI-004-4` architectural decision and Step 0 binary-gate outcomes.
- [ ] **Operational state addition: `item_promotion_records_status` field** (AI-SPEC-004 §11). Tracks per-item authority levels (Items 1 / 5 / 6) read from `item_promotion_records.is_current = true` rows; mirror state for dashboard / UI consumption. Default initial values: all items = `'advisory' or 'disabled'` per Spec §4 line 521–525.
- [ ] **Operational state addition: `counterfactual_engine` field** (AI-SPEC-010 §11). Tracks `{ layer_1_phase: 'shipped', layer_2_phase: 'not_started' | 'v0.2_scaffolding' | 'v0.2_active' | 'demoted', calibration_eligible_classifier_active: bool, chain_archive_capable: bool, last_eod_run_at: timestamp | null, last_eod_run_status: 'success' | 'partial' | 'failed' | null, layer_1_legacy_lockdown_anchor: timestamp | null, single_strategy_contamination_risk: 'low_observability_only' | 'high_consumer_active' }`. Tracks Layer 1 / Layer 2 progression, chain-archive substrate sufficiency for slippage formula, and the `<Commit_4_deploy_date>` cutover anchor lock (per `C-AI-010-5`). Bilateral with `replay_harness.chain_archive_status` (same substrate dependency).
- [ ] **Operational state addition: `vol_fair_value_engine` field** (AI-SPEC-005 §11). Tracks `{ phase: 'not_started' | 'v0.2_scaffolding' | 'v0.2_active' | 'demoted', calibration_grade_capable: bool, layer_1_active: bool, har_rv_layer_2_active: bool, har_rv_training_history_sufficient: bool, freshness_gate_active: bool, last_nightly_retrain_at: timestamp | null, last_nightly_retrain_status: 'success' | 'partial' | 'failed' | null, current_engine_version: text | null, current_reliability_state: 'calibrating' | 'healthy' | 'degraded' | 'halted', d016_disposition: 'option_2_unchanged' | 'option_1_amended' | 'option_3_two_phase' | null }`. Tracks Item 5 V0.2 progression, HAR-RV training data substrate sufficiency (`B-AI-005-2` 5-min bar archive + `B-AI-005-15` Pre-V0.2 substrate minimums + `B-AI-005-3` Layer 1 cutover), freshness gate substrate sufficiency (`B-AI-005-16` bilateral with AI-SPEC-001 `B-AI-001-6`), reliability state-machine state, and the operator-locked D-016 disposition decision per `C-AI-005-1`. Bilateral with `replay_harness.chain_archive_status` AND `counterfactual_engine.chain_archive_capable` (3-spec shared substrate dependency).
- [ ] **Operational state addition: `spx_5min_bar_archive_substrate` field** (AI-SPEC-005 §11, conditional on `B-AI-005-2` resolution). Tracks `{ status: 'absent' | 'forward_archival_in_progress' | 'paid_polygon_tier_acquired' | 'sufficient', start_date: timestamp | null, total_session_days: integer | null, calibration_eligible_threshold_met: bool }`. Only populated if operator decision on `B-AI-005-2` chooses substrate-built path; tracks calendar progression toward HAR-RV training-history sufficiency.
- [ ] **Operational state addition: `meta_labeler` field** (AI-SPEC-006 §11). Tracks `{ phase: 'not_started' | 'v0.2_scaffolding' | 'v0.2_advisory_only' | 'v0.2_skip_reduce_active' | 'production' | 'demoted', calibration_grade_capable: bool, layer_1_phase: 'dormant_legacy_observability' | 'active_legacy_observability' | 'retired' | 'champion_challenger' (per `C-AI-006-2` operator decision), layer_1_pkl_present: bool (runtime tracking — `backend/models/meta_label_v1.pkl` existence — verified 2026-04-27 absent at HEAD), layer_2_phase: 'not_started' | 'v0.2_scaffolding' | 'v0.2_active' | 'demoted', authority_state: 'calibrating' | 'normal' | 'reduced' | 'advisory' | 'halted', authority_recovery_disposition: 'option_2_operator_mediated' | 'option_1_automatic' | 'option_3_hybrid' | null (per `C-AI-006-1` operator decision), prior_parameters: JSONB { alpha, beta, k, w_prior_band_a, w_prior_band_b, w_prior_band_c, w_prior_band_d } (operator-tunable per `C-AI-006-3` default option 1 — default values match Spec §2 hardcoded), labeled_candidates_count: integer (counts qualified training rows; gates Spec §10 200-row promotion threshold), last_retrain_at: timestamp | null, last_retrain_status: 'success' | 'partial' | 'failed' | null, current_engine_version: text | null, drift_state: 'healthy' | 'concerning' | 'breached', last_drift_metric: JSONB { brier_30d, ece_30d, top_bucket_utility_30d, wilson_lower_loss_rate, psi_iv_rv_ratio, psi_skew_z, psi_governor_uncertainty, psi_strategy_mix, psi_time_of_day_mix } }`. Tracks Item 6 V0.2 progression, authority-recovery operator decision, Bayesian prior parameter governance, training-set sufficiency for promotion gates, drift-detection state. Bilateral with `vol_fair_value_engine.calibration_grade_capable` AND `replay_harness.chain_archive_status` AND `counterfactual_engine.layer_2_chain_archive_capable` (4-spec shared substrate dependency through transitive chain-archive cascade).
- [ ] **Operational state addition: `opra_flow_alpha` field** (AI-SPEC-008 §11). Tracks `{ phase: 'not_started' | 'v0.3_scaffolding' | 'v0.3_advisory_only' | 'v0.3_active' | 'production' | 'demoted', calibration_grade_capable: bool (default false per `C-AI-008-4` default option 3 advisory-only acceptance), layer_0_active: bool (`databento_feed.py` raw OPRA stream consumer status — observability-only per Layer 0/Layer 2 coexistence variant), layer_2_phase: 'not_started' | 'v0.3_scaffolding' | 'v0.3_active' | 'demoted', flow_agent_role: 'brief_generator_legacy' | 'narrowed_summarizer' (per `C-AI-008-2` default option 1 — `flow_agent.py` narrows to LLM summarizer over Item 8's compact summary), databento_subscription_tier_verified: bool (per `C-AI-008-3` default option 4 pre-implementation operator gate; V0.3 implementation cannot begin until this verification completes), databento_subscription_decision: 'option_4_default_pre_implementation_gate' | 'option_1_within_d008_budget' | 'option_2_d008_budget_revision' | 'option_3_spec_amendment' | null (per `C-AI-008-3`), cross_spec_contract_ratified_with_item_5: bool (default false per `C-AI-008-1` default option 3 DEFERRED BILATERAL CONFIRMATION; bilateral confirmation deferred to next P1.3.5 amendment cycle), cross_spec_contract_ratified_with_item_6: bool (default false per `C-AI-008-1` default option 3 DEFERRED BILATERAL CONFIRMATION; bilateral confirmation deferred to next P1.3.6 amendment cycle), unknown_aggressor_share_threshold: numeric (default 0.30; operator-tunable per `B-AI-008-8` quality flag emission), backpressure_state: 'normal' | 'degraded' | 'stale' (per `B-AI-008-10` `BackpressureMonitor` class), last_emit_at: timestamp | null, last_lag_seconds: numeric | null (5-second staleness threshold per Spec §4 lines 257–258), feature_count_emitted: integer (validates 20 production features per `A-AI-008-2` corrected count), separate_railway_worker_active: bool (per `B-AI-008-9` Procfile entry `flow_alpha_worker`), opra_flow_feature_bars_table_present: bool, opra_flow_events_table_present: bool, opra_flow_alpha_health_redis_key_present: bool }`. Tracks Item 8 V0.3 progression, Layer 0/Layer 2 coexistence variant status, flow_agent narrowing transition, Databento subscription scope operator gate, cross-spec contract ratification status with Items 5 + 6 (DEFERRED BILATERAL CONFIRMATION default), backpressure state machine, freshness substrate health, and storage architecture readiness. Bilateral with `meta_labeler.calibration_grade_capable` AND `vol_fair_value_engine.calibration_grade_capable` AND `replay_harness.chain_archive_status` AND `counterfactual_engine.layer_2_chain_archive_capable` (5-spec shared substrate dependency through transitive chain-archive cascade per `C-AI-008-4`).
- [ ] **Operational state addition: `memory_retrieval_substrate` field** (AI-SPEC-006 §11, conditional on future P1.3.7 audit per `B-AI-006-6`). Tracks `{ status: 'absent' | 'pgvector_enabled' | 'partial' | 'sufficient', pgvector_enabled: bool, trade_decision_cases_table_present: bool, embedding_pipeline_active: bool, last_index_rebuilt_at: timestamp | null, top20_query_pipeline_active: bool, total_indexed_cases: integer | null }`. Tracks Item 7 Memory Retrieval substrate progression — calendar-blocker for Item 6 V0.2 per `B-AI-006-6`. Bilateral with AI-SPEC-001 `B-AI-001-1` `pgvector` enablement prerequisite.

### `constitution.md`
- [ ] **Pointer note only — no rule change** (AI-SPEC-001 §11 + AI-SPEC-004 §11). If D-023 is ratified to include the AI authority boundary + `decision_outcome` enum coordination + `item_promotion_records` startup-read contract + decision-card-counting wording, T-Rule 4 needs a one-line note that D-023 is the explicit AI-authority-boundary record. T-Rule 5 / T-Rule 6 / T-Rule 9 already cover D-005 / D-010 / D-011 / D-013 non-overridability; the Governor design + Replay Harness design respect all three. T-Rule 10 (Silent Failures Are Forbidden) already aligns with Spec §2 Step 0 explicit "no downstream calibration runs" on Step 0 failure.
- [ ] **No constitution change required by AI-SPEC-002** (AI-SPEC-002 §11). Item 2 is observational labeling, advisory-authority across all V0.x phases; respects T-Rule 4 (locked decisions), T-Rule 5 (D-005 stop-loss not overridden — Item 2 reads `exit_reason` rather than altering it), T-Rule 6 (D-013 paper-binding gates not gated by Item 2), T-Rule 9 (paper phase gate logic unaffected — Item 2 produces labels for the gate but does not enforce it), T-Rule 10 (silent failures forbidden — Item 2 EOD engine logs all attribution failures to dashboard + telemetry per Spec §6.6).
- [ ] **No constitution change required by AI-SPEC-004** (AI-SPEC-004 §11). Item 4 is research-only offline replay infrastructure with structural research-vs-production isolation (DB-level CHECK constraints `environment = 'research'`); respects T-Rule 1 (Foundation Isolation — operator owns chain-archive substrate decision), T-Rule 4 (locked decisions — D-023 enrichment encodes Item 4's authority boundary), T-Rule 5 / T-Rule 6 (D-005 / D-013 unaffected by Item 4 — Item 4 produces calibration-grade evidence, does not override execution rules), T-Rule 9 (paper phase 12-GLC compliance unaffected — Item 4 operates as independent per-item gate per `C-AI-004-1` default option 1), T-Rule 10 (silent failures forbidden — Spec §2 Step 0 binary gates explicitly fail loud and block downstream calibration runs).
- [ ] **No constitution change required by AI-SPEC-010** (AI-SPEC-010 §11). Item 10 is observability + advisory-authority counterfactual attribution; respects T-Rule 1 (Foundation Isolation — operator owns Layer 1 / Layer 2 cutover decision per `C-AI-010-5`), T-Rule 2 (Governance Documents Are Authoritative — D-023 enrichment encodes Layer 1 / Layer 2 authority boundary + cutover policy), T-Rule 4 (Locked Decisions Are Final — D-023 + cutover anchor operator-locked once Commit 4 of IC/IB sequence ships), T-Rule 5 (Capital Preservation Absolute — Layer 2 NEVER gates trade execution; advisory authority only with `gates_execution = false` flag at function-signature layer), T-Rule 6 / T-Rule 9 (D-005 / D-013 unaffected — Item 10 reads `exit_reason` for outcome typing but does not alter it; paper-phase 12-GLC compliance unaffected — Item 10 produces calibration labels for Items 5/6/7/9 training, does not gate the system go-live), T-Rule 10 (Silent Failures Are Forbidden — Spec §6.6 implicit + AI-SPEC-002 §6.6 explicit telemetry on EOD attribution failures; Layer 2 failure modes log structured errors with `simulation_status` enum + audit-logged `diagnostic_opt_in` accessor calls per Spec §6 lines 414–419).
- [ ] **No constitution change required by AI-SPEC-005** (AI-SPEC-005 §11). Item 5 is **a deterministic pricing/forecasting module, not an AI module** per locked spec architectural commitment (Spec §1 line 22 first paragraph) — outputs measurements (HAR-RV `rv_forecast_remaining`, IV ATM, EV table top-3) consumed by Items 1 / 6 / 9; respects T-Rule 1 (Foundation Isolation — operator owns chain-archive substrate decision per `C-AI-005-3` bilateral with `C-AI-004-4` AND multi-year RV history substrate decision per `B-AI-005-2`), T-Rule 2 (Governance Documents Are Authoritative — D-023 enrichment encodes Item 5's authority boundary over `vol:*` namespace + 4 new tables), T-Rule 4 (**Locked Decisions Are Final — D-016 sigma-blending formula `sigma = max(realized, 0.70 × implied)` cannot be modified without operator approval + new D-XXX**; default position per `C-AI-005-1`: option 2 — HAR-RV runs as new signal feeding EV table only; D-016 stays unchanged; no D-XXX required), T-Rule 5 (Capital Preservation Absolute — Item 5 outputs are advisory measurements; reliability state-machine `halted` state explicitly freezes downstream consumption rather than allowing degraded data to gate execution), T-Rule 6 / T-Rule 9 (D-005 / D-013 unaffected — Item 5 is upstream of execution decisions; paper-phase 12-GLC compliance unaffected — Item 5 produces V0.2 outputs that flow into Item 4 replay validation per `C-AI-005-3`), T-Rule 10 (Silent Failures Are Forbidden — Spec §7 reliability state-machine + freshness gate substrate `B-AI-005-16` log structured errors when input keys are stale; `calibration_eligible` flag per `B-AI-005-15` Pre-V0.2 substrate minimums + `B-AI-005-17` pre-`fc64840` cutover classifier enforces structural data-quality gating at function-signature layer for downstream consumers; Spec §7 Fallback Level 3 explicitly fails loud and switches to "logging only, no production authority" rather than silently degrading).
- [ ] **Pointer note only — no rule change** (AI-SPEC-008 §11). If D-023 is ratified to include items (u)/(v)/(w) — Item 8 authority boundary + flow_agent / Layer 0 / Layer 2 cutover policy + cross-spec contract ratification process governance — T-Rule 4 needs a one-line note that D-023 is the explicit AI-authority-boundary record (now extended to include cross-spec producer→consumer contract ratification process). Item 8 respects T-Rule 1 (Foundation Isolation — operator owns Databento subscription scope decision per `C-AI-008-3` AND chain-archive substrate decision per `C-AI-008-4` bilateral with `C-AI-004-4`), T-Rule 2 (Governance Documents Are Authoritative — D-023 enrichment encodes Item 8's authority boundary over `opra:flow_alpha:*` namespace + `opra_flow_feature_bars` + `opra_flow_events` tables ONLY; **architectural principle 1 enforcement** — Item 8 produces measurements consumed by Items 5/6/Governor; Item 8 does NOT have authority to override Item 1 Governor veto, Item 6 Meta-Labeler skip, or Item 5 Vol Fair-Value Engine outputs; producer-only authority preserves capital preservation invariant), T-Rule 4 (Locked Decisions Are Final — D-023 + Item 8 cross-spec contract ratification process governance per `C-AI-008-1` operator-locked once item (w) ratified; bilateral amendment to AI-SPEC-005 §3/§4 + AI-SPEC-006 §3 deferred to next P1.3.5 + P1.3.6 amendment cycle per default option 3 DEFERRED BILATERAL CONFIRMATION), T-Rule 5 (Capital Preservation Absolute — Item 8 outputs are advisory measurements; backpressure state machine per `B-AI-008-10` `degraded` and `stale` states explicitly freeze downstream consumption rather than allowing degraded data to gate execution; Item 8 V0.3 ships in advisory-only mode with `opra_flow_alpha.calibration_grade_capable = false` per `C-AI-008-4` default option 3), T-Rule 6 / T-Rule 9 (D-005 / D-013 unaffected — Item 8 is upstream of execution decisions; paper-phase 12-GLC compliance unaffected — Item 8 produces V0.3 outputs that flow into Item 6 V0.2 replay validation per `B-AI-008-11` calendar-blocked on Item 4 V0.1), T-Rule 10 (Silent Failures Are Forbidden — Spec §4 backpressure handling + freshness gate substrate `B-AI-008-16` log structured errors when Databento feed lags; `calibration_eligible` flag per `B-AI-008-3` recommended `opra_flow_feature_bars.calibration_eligible BOOLEAN NOT NULL DEFAULT false` enforces structural data-quality gating at function-signature layer for downstream consumers Items 5 + 6 + Governor; Spec §4 lines 257–258 explicitly fail loud and downgrade authority when `lag > 5 seconds` rather than silently degrading; Lee-Ready aggressor inference per `B-AI-008-5` explicitly emits `aggressor_sign = 0` for unknown cases (NBBO_age > 1s, locked/crossed market) rather than guessing — explicit-unknown handling).
- [ ] **Pointer note only — no rule change** (AI-SPEC-006 §11). If D-023 is ratified to include items (o)/(p)/(q)/(r)/(s)/(t) — Item 6 authority boundary + Authority Recovery operator-mediated disposition + Layer 1/Layer 2 cutover + strategy taxonomy reconciliation + Bayesian prior parameter governance + `decision_outcome` enum coordination — T-Rule 4 needs a one-line note that D-023 is the explicit AI-authority-boundary record (now bilateral with AI-SPEC-005 §5 reliability state machine via `C-AI-006-1` resolution). Item 6 respects T-Rule 1 (Foundation Isolation — operator owns Layer 1/Layer 2 cutover decision per `C-AI-006-2` AND Bayesian prior parameter governance decision per `C-AI-006-3` AND LightGBM Direction Model spec ownership decision per `C-AI-006-4` AND most-consequential Authority Recovery decision per `C-AI-006-1`), T-Rule 2 (Governance Documents Are Authoritative — D-023 enrichment encodes Item 6's authority boundary over `meta:*` namespace + `meta_labeler_decisions` table; **architectural principle 1 enforcement** — Item 6 has authority to BLOCK trades and REDUCE size but NO authority to FORCE, UPSIZE, or override Item 1 Governor veto; per `C-AI-006-1` default option 2, authority RECOVERY requires explicit operator action), T-Rule 4 (Locked Decisions Are Final — D-023 + Authority Recovery operator-mediated disposition operator-locked once `C-AI-006-1` ratified; bilateral amendment to AI-SPEC-005 §5 reliability state machine recovery wording mandatory), T-Rule 5 (Capital Preservation Absolute — Item 6 size cap composes with Items 1/Rules/Constitutional caps via `min()` only per Spec §6 line 446 + §0 line 18; reductive-only constraint preserves capital preservation invariant), T-Rule 6 / T-Rule 9 (D-005 / D-013 unaffected — Item 6 produces meta-labeler probabilities + size caps that flow into Item 1 Card per `B-AI-001-2`; paper-phase 12-GLC compliance unaffected — Item 6 produces V0.2 outputs that flow into Item 4 replay validation per `B-AI-006-7`), T-Rule 10 (Silent Failures Are Forbidden — Spec §9 authority degradation + freshness gate substrate `B-AI-006-17` `meta:engine_updated_at` producer + `_safe_redis()` caller wiring at consumer sites log structured errors when meta-labeler outputs are stale; `calibration_eligible` flag per `B-AI-006-10` filter wiring + `B-AI-006-11` `meta_labeler_decisions.calibration_eligible BOOLEAN NOT NULL DEFAULT false` enforces structural data-quality gating at function-signature layer for downstream consumers Items 9 + 13).

---

## Update Log

| Date | Audit | Action |
|------|-------|--------|
| 2026-04-26 | P1.3.1b | Initial creation. Populated from AI-SPEC-001 audit (PR #60, merged at `f456922`). 3 open Class C escalations (C-AI-001-1, C-AI-001-2, C-AI-001-3); 8 open Class B corrections (B-AI-001-1 through B-AI-001-8); 2 Class A items (logged for Gate 1 batch-clear); 5 cross-spec themes identified; 5 governance update line items aggregated. |
| 2026-04-26 | P1.3.1b backfill | Added Section 0 (Pre-Audit Findings) covering Phase 0 PRs #54/#55, P1.1 PR #56, P1.2 PR #57, P1.3.0 PR #58, P1.3.0b PR #59. 13 pre-audit findings catalogued (4 Phase 0, 7 P1.1, 4 P1.2, 1 P1.3.0 [duplicate of PRE-P0-4], 2 P1.3.0b informational). §0.6 cross-reference table added linking pre-audit IDs to §5 cross-spec themes and §1–§3 per-audit entries where applicable. Sections 1–6 unchanged. |
| 2026-04-26 | P1.3.2 | Added AI-SPEC-002 audit findings to register: 3 Class C entries (`C-AI-002-1` decision_outcome enum coordination with AI-SPEC-001 D-023; `C-AI-002-2` `trade_counterfactual_cases` table-vs-column architectural divergence with Item 10; `C-AI-002-3` legacy `attribution_*` BOOLEAN columns on `trading_positions` violating spec's "execution ledger only" architectural commitment); 8 Class B entries (`B-AI-002-1` EOD attribution engine file absent; `B-AI-002-2` 3 new tables + `closed_trade_path_metrics` substrate missing; `B-AI-002-3` FK dependency cascade to Items 1/6/7/10; `B-AI-002-4` `outcome_mode` enum vs `exit_reason` taxonomy mismatch; `B-AI-002-5` `strategy_performance_matrix.py` integration silence; `B-AI-002-6` legacy `trading_positions` cutover policy missing; `B-AI-002-7` table-vs-column escalated to Class C as `C-AI-002-2`; `B-AI-002-8` `candidate_id` FK target undefined); 1 Class A item (FK reference `trading_positions(position_id)` should be `trading_positions(id)`). §5 cross-spec themes updated: strategy-class taxonomy now confirmed in 2 audits; `_safe_redis()` ruled out for AI-SPEC-002; `calibration_eligible` flag producer correctly identified as Item 2 (not Item 10); 3 NEW themes added (FK dependency cascade, `closed_trade_path_metrics` substrate gap, decision_outcome enum coordination, legacy `attribution_*` columns). §6 governance updates: D-023 wording extended for enum coordination; new MASTER_PLAN Phase 3B; new TASK_REGISTER §14B with 9 sub-items; 2 new system-state.md fields; constitution unchanged. AI-SPEC-002 redline at `trading-docs/08-planning/ai-architecture-audits/AI-SPEC-002.md`. |
| 2026-04-26 | P1.3.3 | Added AI-SPEC-004 audit findings to register: 4 Class C entries (`C-AI-004-1` `paper_phase_criteria` vs `item_promotion_records` authority overlap; `C-AI-004-2` decision-card definition for Spec §12 200-card threshold across `replay_eval_cases` vs `shadow_predictions`; `C-AI-004-3` D-023 startup-read contract enrichment for "default to advisory or disabled, NEVER production"; `C-AI-004-4` archived option-chain data substrate as HARD V0.1 prerequisite); 13 Class B entries (`B-AI-004-1` 4 new tables absent; `B-AI-004-2` 8 `replay_*.py` files absent + `backtest_gex_zg.py` deprecation; `B-AI-004-3` chain-archive infrastructure absent; `B-AI-004-4` Polygon historical 1-min fetch capability missing; `B-AI-004-5` Commit-N production hash anchoring missing; `B-AI-004-6` news/event `published_at` field missing; `B-AI-004-7` FK cascade on Items 1+2 unshipped tables; `B-AI-004-8` `trade_counterfactual_cases` bilateral with `C-AI-002-2`; `B-AI-004-9` decision-card JSON sub-schema undefined; `B-AI-004-10` LightGBM artifact-log substrate missing; `B-AI-004-11` walk-forward implementation absent (bundled into B-AI-004-2); `B-AI-004-12` Step 0 validation tests absent (bundled); `B-AI-004-13` model/version artifact substrate missing (bundled into B-AI-004-10)); 0 Class A items (Item 4 is pure forward-design, no `backend/<file>.py:NN` cites to verify). §5 cross-spec themes updated: strategy-class taxonomy now confirmed in 3 audits (Item 4 silently honors canonical names per Spec §9 line 916); `_safe_redis()` ruled out for AI-SPEC-004; `calibration_eligible` flag now has 2 producer surfaces (Item 2 + Item 4); FK dependency cascade confirmed in 2 audits; `closed_trade_path_metrics` substrate gap confirmed in 2 audits; decision_outcome enum coordination confirmed in 2 audits; 4 NEW themes added (`paper_phase_criteria` vs `item_promotion_records` authority overlap; decision card definition / counting ambiguity for Spec §12 200-card threshold; archived option-chain data substrate as V0.1 hard prerequisite; news/event `published_at` substrate for point-in-time gating; LightGBM artifact-log substrate for model-state-source). §6 governance updates: D-023 wording extended with `item_promotion_records` startup-read + decision-card definition; D-013 non-amendment confirmed as default; possible new D-XXX for chain-archive substrate (default deferred); new MASTER_PLAN Phase 3C; new TASK_REGISTER §14C with 11 sub-items; 2 new system-state.md fields (`replay_harness`, `item_promotion_records_status`); constitution unchanged (pointer note only if D-023 ratified). Risk rating: **CRITICAL** (driven by chain-archive hard prerequisite + FK cascade on Items 1, 2, 10). Recommended status: `spec_has_semantic_drift_from_locked_intent`. AI-SPEC-004 redline at `trading-docs/08-planning/ai-architecture-audits/AI-SPEC-004.md`. |
| 2026-04-26 | P1.3.4 | Added AI-SPEC-010 audit findings to register: 5 Class C entries (`C-AI-010-1` Spec §0 "do not rewrite" vs Spec §3 Layer 1 surgical fix internal contradiction; `C-AI-010-2` cluster/tier classification discrepancy V0.1 vs V0.2-V0.4 graduated; `C-AI-010-3` z-score producer authority for slippage formula `vix_z` / `spread_z`; `C-AI-010-4` Item 3 vs Item 10 ownership of synthetic counterfactual cases storage; `C-AI-010-5` `<Commit_4_deploy_date>` cutover anchor lock for legacy-rows lockdown migration); 11 Class B entries (`B-AI-010-1` Layer 2 file location undefined → canonical `backend/counterfactual_engine_v2.py`; `B-AI-010-2` 4 new tables + 3 new columns absent → scheduled buildout; `B-AI-010-3` Layer 1 + Layer 2 default-iron_condor fallback fix bilateral with `C-AI-010-1`; `B-AI-010-4` `useActivationStatus.ts` `shippedDate`/`shippedCommit` fields missing — Spec §13 Gap 5 partially resolved; `B-AI-010-5` 4 API accessor functions absent → scheduled buildout; `B-AI-010-6` slippage formula implementation absent; `B-AI-010-7` halt-cycle row-creation wiring missing; `B-AI-010-8` versioned width table absent; `B-AI-010-9` `feedback:counterfactual:layer2:enabled` Redis flag absent; `B-AI-010-10` `counterfactual_reason ↔ decision_outcome` enum mapping function absent; `B-AI-010-11` migration header observability contract vs Layer 2 calibration-grade promotion governance change); 2 Class A items (line count 349 → 406; test count 9 → 11 — both due to `4970095` + `fc64840` shipping after spec lock 2026-04-25). **`C-AI-002-2` MOVED TO §4 Resolved Findings** — Item 10 spec §4 lines 198–225 authoritatively ships `trade_counterfactual_cases` as a NEW table; `B-AI-002-7` resolves transitively; `B-AI-004-8` resolves; PRE-P11-4 carry-forward closes with Layer 1 / Layer 2 split. §5 cross-spec themes updated: strategy-class taxonomy now confirmed in 4 audits; `_safe_redis()` ruled out for AI-SPEC-010; `calibration_eligible` flag now has 3 producer surfaces (Items 2 + 4 + 10); FK dependency cascade confirmed in 3 audits with Item 10 identified as canonical FK provider; `closed_trade_path_metrics` substrate gap — owner identified as Item 10 V0.2 (theme will close on V0.2 ship); decision_outcome enum coordination confirmed in 3 audits with `counterfactual_reason ↔ decision_outcome` mapping required; archived option-chain data substrate confirmed in 2 audits (Item 4 V0.1 + Item 10 V0.2 share dependency); 4 NEW themes added (`counterfactual_pnl` shape RESOLVED by AI-SPEC-010; Layer 1 / Layer 2 architectural pattern as reusable template; single-strategy contamination risk; z-score producer authority). §6 governance updates: D-023 wording extended with Layer 1 / Layer 2 cutover policy + z-score producer authority + `counterfactual_reason ↔ decision_outcome` mapping table + legacy-rows lockdown anchor; new MASTER_PLAN Phase 3D; new TASK_REGISTER §14D with 15 sub-items; 1 new system-state.md field (`counterfactual_engine`); constitution unchanged (pointer note only if D-023 ratified). Risk rating: **HIGH** (driven by the 4 Class C escalations, especially `C-AI-010-1` internal contradiction + `C-AI-010-5` cutover anchor lock; mitigated somewhat by Layer 1 already shipped at `2400e98`). Recommended status: `spec_has_semantic_drift_from_locked_intent`. AI-SPEC-010 redline at `trading-docs/08-planning/ai-architecture-audits/AI-SPEC-010.md`. **CLUSTER A AUDIT COMPLETE** (Items 1, 2, 4, 10 all audited per consolidated plan §4.A). Operator checkpoint pending — review accumulated findings (15 open Class C + 40 open Class B + 5 Class A across all four audits) before deciding to proceed to Cluster B (P1.3.5+ Items 5, 6, 7). |
| 2026-04-27 | P1.3.5 | Added AI-SPEC-005 audit findings to register: 3 Class C entries (`C-AI-005-1` D-016 governance interaction — spec is **completely silent on D-016 sigma-blending formula** despite producing the natural successor to its `realized` term; T-Rule 4 protected; default option 2 = HAR-RV is NEW signal feeding EV table only; D-016 stays unchanged; no D-XXX amendment; `C-AI-005-2` Layer 1 cutover policy unspecified — default option 1 = Layer 1 stays put as backward-compat surface; Layer 2 ships parallel `vol:*` keys; `C-AI-005-3` V0.x advisory-only acceptance bilateral with `C-AI-004-4` chain-archive substrate; default option 3 = scaffolding-only ship until chain-archive matures); 17 Class B entries (`B-AI-005-1` HAR-RV regression model file absent → canonical `backend/vol_fair_value_engine.py`; `B-AI-005-2` 252-session 5-min SPX bar + daily-RV history substrate missing — bilateral with `B-AI-004-4` Polygon historical-bars; `B-AI-005-3` Layer 1 12A `polygon:spx:realized_vol_20d` writer output shape incompatible with HAR-RV `RV_D` 252-session input — cutover folds into `C-AI-005-2`; `B-AI-005-4` 7 lag-structure features (`RV_D` / `RV_W` / `RV_M` / `RV_so_far_scaled` / `overnight_gap` / `vix_change_from_prior_close` / `event_day_flag` / `time_bucket`) absent + `polygon:spx:overnight_gap` consumer-only orphan; `B-AI-005-5` output fields + `vol_fair_value_snapshots` migration DDL absent → scheduled buildout; `B-AI-005-6` IV extraction layer (7 sub-modules: chain cleaning, forward price, model-free implied variance, ATM straddle proxy, 16-delta skew, surface features, term structure) absent — bilateral with `C-AI-004-4` chain archive; `B-AI-005-7` terminal distribution simulator (10,000 scenarios + grid MLE over `df ∈ [3, 12]` + regime-conditional df) absent; `B-AI-005-8` LightGBM directional tilt **calendar-blocked** on Items 1 + 6 schema-landing milestones — confirms FK cascade theme in 4 audits; `B-AI-005-9` strategy-conditional EV table generator absent + strategy-class taxonomy alignment with Item 2 `outcome_mode` + Item 6 strategy-class definitions — extends `decision_outcome enum coordination` theme; `B-AI-005-10` reliability score state machine + per-channel authority (`short_gamma_filter_authority` / `long_vol_enable_authority` / `strategy_ev_ranking_authority`) — bilateral with Item 4 `item_promotion_records.channel TEXT NULL` schema extension; `B-AI-005-11` per-leg spread filter absent — bilateral with `C-AI-004-4` chain archive + `B-AI-010-6` slippage formula via `C-AI-010-3` shared `features:*` namespace; `B-AI-005-12` replay calibration harness (Baselines A–F) **calendar-blocked** on Item 4 V0.1 + chain archive + Items 2 + 10 calibration_eligible substrate — escalates to `C-AI-005-3`; `B-AI-005-13` retraining cadence orchestration (daily HAR + weekly Student-t df + weekly EWMA + monthly decision-score weights) absent — bilateral with `B-AI-004-4` + `B-AI-004-10`; `B-AI-005-14` `vol_fair_value_snapshots` schema gap **bundled with `B-AI-005-5`** — register row preserved for §3 verification cross-reference traceability; `B-AI-005-15` Pre-V0.2 data-substrate minimums (30–60 / 90 / 60 / 100 / 100 / 30 sessions) absent — confirms `calibration_eligible` flag theme in 4 audits; `B-AI-005-16` output Redis namespace `vol:*` undefined + `_safe_redis()` dead code + `gex:updated_at` no-producer freshness substrate non-functional **HIGH-IMPACT confirmation** of carry-forward #4 — bilateral with `B-AI-001-6`; `B-AI-005-17` Pre-Commit-4 calibration_eligible classifier with anchor `fc64840 (2026-04-20 18:43 -0400)` — bilateral with `B-AI-002-6` legacy cutover + `C-AI-010-5` operator-locked Commit-4 anchor); 0 Class A items (Item 5's spec is forward-design — no `backend/<file>.py:NN` line cites or test counts to verify; closest existing surface `polygon_feed.py:_append_spx_daily_return_if_due` is an INPUT to a future HAR-RV model, not an existing implementation). **No findings moved to §4 Resolved Findings.** §5 cross-spec themes updated: strategy-class taxonomy now confirmed in 5 audits; `_safe_redis()` dead code now confirmed in 2 audits (Item 5 HIGH-IMPACT); `calibration_eligible` flag now has 4 producer surfaces (Items 2 + 4 + 5 + 10); FK dependency cascade confirmed in 4 audits with Item 5 added as bilateral consumer-and-producer; decision_outcome enum coordination extended to STRATEGY-TAXONOMY alignment via `B-AI-005-9`; archived option-chain data substrate **confirmed trilaterally** in 3 audits (Items 4 V0.1 + Items 5 V0.2 + Item 10 V0.2 share same dependency — `C-AI-004-4` operator decision cascades to all three); Layer 1 / Layer 2 architectural pattern **confirmed as reusable architectural template** (now confirmed in 2 audits: Items 5 + 10); z-score producer authority refined — existing `polygon:vix:z_score` production key satisfies `vix_z` reference, `spread_z` requires NEW producer; 3 NEW themes added (D-016 governance interaction sigma blending vs HAR-RV — major new theme; multi-year SPX 5-minute bar history substrate — HAR-RV training prerequisite; `realized_vol_20d` Layer 1 cutover anchor B1-2 fix `fc64840` — verified 2026-04-27 via `git show fc64840 --format="%ci %s"` returning `2026-04-20 18:43:14 -0400 fix(EOD): move daily return append from 19:00 UTC to 21:00 UTC to capture full SPX session`). §6 governance updates: D-023 wording extended with items (k)/(l)/(m)/(n) — Item 5 authority boundary; Item 5 Layer 1 / Layer 2 cutover policy; canonical strategy-taxonomy enumeration; chain-archive substrate cascade across Items 4/5/10; new MASTER_PLAN Phase 3E; new TASK_REGISTER §14E with 18 sub-items; 2 new system-state.md fields (`vol_fair_value_engine`, `spx_5min_bar_archive_substrate`); constitution unchanged (Item 5 is deterministic pricing/forecasting module per spec architectural commitment, not AI module — explicit T-Rule alignment note added). Risk rating: **HIGH** (driven by the D-016 governance interaction `C-AI-005-1` + chain-archive bilateral `C-AI-005-3` + multi-year RV history substrate `B-AI-005-2` + freshness gate substrate `B-AI-005-16` HIGH-IMPACT — same flavor as Item 4 V0.1 risk profile; mitigated by deterministic-not-AI architectural distinction limiting D-016 amendment pressure to operator-explicit decision only). Recommended status: `spec_has_semantic_drift_from_locked_intent`. AI-SPEC-005 redline at `trading-docs/08-planning/ai-architecture-audits/AI-SPEC-005.md`. **CLUSTER B AUDIT 1 of 4.** Cluster A audit complete (Items 1, 2, 4, 10); Cluster B remaining (Items 6, 7, 9 per consolidated plan §4.B P1.3.6+). Cumulative findings across audits: 18 open Class C + 57 open Class B + 5 Class A; 1 finding resolved (`C-AI-002-2`). |
| 2026-04-27 | P1.3.6 | Added AI-SPEC-006 audit findings to register: 4 Class C entries (`C-AI-006-1` Authority Recovery automatic-vs-operator-mediated — **CRITICAL Class C escalation per Guard 1; FIRST authority-boundary-violation Class C in Cluster B; bilateral with AI-SPEC-005 §5 reliability state machine** which has SAME automatic-recovery pattern; default option 2 = operator-mediated recovery only; D-023 enrichment item (p); Spec §9.4 line 622 wording amendment + bilateral AI-SPEC-005 §5 amendment mandatory; `C-AI-006-2` Spec §13 V0.2 Build Scope **silent on `bf41175` Layer 1 cutover policy** — verified 2026-04-27 via `grep -nE "bf41175\|Loop 2\|meta-label scaffold\|meta_label_v1" ITEM_6_META_LABELER_LOCKED.md` returning 0 matches; default option 1 = Layer 1 stays put as legacy observability per Items 5 + 10 precedent; `C-AI-006-3` Bayesian prior parameter governance (operator-tunable vs code-bound) — Spec §2 hardcodes prior parameters with zero references to `system-state.md` / `approved-decisions.md` / operator-tunable language; default option 1 = operator-tunable via `system-state.md` `meta_labeler.prior_parameters` field; `C-AI-006-4` LightGBM Direction Model spec ownership — Spec §3.4 reads 5 fields from a "LightGBM Direction Model" producer that has NO spec in the AI architecture catalog; default option 2 = new spec AI-SPEC-014 added to catalog with new D-XXX (D-024 candidate)); 17 Class B entries (`B-AI-006-1` engine module file absent → canonical `backend/meta_labeler_engine.py` Bayesian three-part hybrid; `B-AI-006-2` Item 5 EV Table 18 fields entirely absent **calendar-blocked on Item 5 V0.2** per Guard 4 — Item 6 V0.2 ship date depends on Item 5 V0.2 build-out completion; `B-AI-006-3` Rules Engine 5 fields (`rules_regime`, `rules_confidence`, `strategy_selected_by_rules`, `strategy_allowed_by_map`, `distance_from_regime_boundary`) — `backend/strategy_selector.py` extension required; `B-AI-006-4` LightGBM Direction Model 5 fields (`p_bull`, `p_bear`, `direction_confidence`, `direction_edge`, `strategy_direction_alignment`) absent at HEAD — bilateral with `C-AI-006-4` operator decision; `B-AI-006-5` AI Risk Governor 9 structured fields **calendar-blocked on Item 1 V0.1**; `B-AI-006-6` Memory Retrieval 10 Top-20 Analogs fields **deepest calendar-blocker** — Item 7 itself un-audited; bilateral with AI-SPEC-001 `B-AI-001-1` `pgvector` enablement prerequisite; fallback if Item 7 V0.x slips: `w_memory = 0` advisory-only mode; `B-AI-006-7` replay validation harness with walk-forward **calendar-blocked on Item 4 V0.1** `replay_eval_cases` substrate; `B-AI-006-8` strategy-specific utility label calculator absent — methods `compute_U_IC()`, `compute_U_IB()`, `compute_U_PCS()` per Spec §1 lines 30–141 + canonical 8 strategies per `B-AI-006-12` resolution; `B-AI-006-9` `decision_outcome` writer wiring absent — Item 6 emits `blocked_meta_labeler` + `reduced_meta_labeler` to `strategy_attribution.decision_outcome`; bilateral with AI-SPEC-002 `C-AI-002-1` enum CHECK constraint extension; `B-AI-006-10` `calibration_eligible` filter wiring absent — training-set queries filter on `calibration_eligible = true` across all 4 producer-substrate tables; confirms `calibration_eligible` flag theme in 5 audits; `B-AI-006-11` `meta_labeler_decisions` table + schema bootstrap absent → scheduled buildout; FIFTH producer surface for `calibration_eligible` flag; `B-AI-006-12` strategy taxonomy inconsistency — Spec §1 enumerates 5 strategies (`iron_condor`, `iron_butterfly`, `put_credit_spread`, `long_straddle`, `debit_call_spread`) — strict subset of Items 2 + 5 canonical 8 with non-canonical membership; default option 1 = Item 6 V0.2 ships utility-label calculators for canonical 8; bilateral with `C-AI-005-1` strategy-taxonomy theme; `B-AI-006-13` case weight discrepancies — Spec §4 line 247 (1.0 / 0.5 / 0.2 for actual / counterfactual / synthetic) vs Spec §3.6 line 339 (1.0 / 0.5 / 0.2 in similar-case weighted average) vs AI-SPEC-010 Spec §4 line 222 (`case_weight ∈ {1.0, 0.5, 0.2}`); default option 1 = harmonized 1.0 / 0.5 / 0.2 across both training and memory; `B-AI-006-14` un-executed candidate slippage imputation policy missing — Spec §3.1 lines 234–253 reads `slippage_to_ev_ratio` + `estimated_slippage` from candidates that may NOT have executed; default option 1 = imputation `estimated_slippage = quantile(50, observed_slippage_for_strategy×regime×time_bucket)`; bilateral with AI-SPEC-010 `B-AI-010-6` slippage formula; `B-AI-006-15` Wilson-interval-based drift detection absent — Spec §9 lines 553–584 specifies trigger conditions but math implementation requires Wilson-interval + PSI feature drift detection; partial scaffold from `bf41175` exists but tests are stub; `B-AI-006-16` Bayesian bucket calibration with bootstrap CIs absent — Spec §5 lines 397–435 specifies 5-bucket Beta posterior (`alpha` / `beta` operator-tunable per `C-AI-006-3`); credible interval as confidence band; `B-AI-006-17` output Redis namespace `meta:*` undefined + `_safe_redis()` caller wiring at consumer sites — `meta:engine_updated_at` is producer for Item 6's freshness gate; bilateral with `B-AI-001-6` + `B-AI-005-16` (3rd HIGH-IMPACT confirmation of `_safe_redis()` dead code theme); **without freshness gate, stale meta-labeler outputs could silently authorize trades after engine has degraded — CRITICAL runtime correctness gap given Item 6's authority over admissibility per architectural principle 1**); 0 Class A items (Item 6's spec is pure forward-design with Layer 1 `bf41175` silence; no `backend/<file>.py:NN` line cites to verify mechanical drift; closest "would-be Class A" candidate Spec §13 V0.2 Build Scope silence on `bf41175` correctly classified as `C-AI-006-2` architectural-intent governance escalation, not Class A mechanical drift). **No findings moved to §4 Resolved Findings.** §5 cross-spec themes updated: strategy-class taxonomy now confirmed in **6 audits** with Item 6 P1.3.6 adding the strongest internal-consistency case (Item 6 is THE keystone admissibility decision-maker per architectural principle 1; its strategy taxonomy must match Items 2 + 5 exactly for per-strategy utility-label calculator alignment); `_safe_redis()` dead code now confirmed in **3 audits** with Item 6 confirming theme **escalates from "open governance debt" to "load-bearing safety substrate"** (Item 6 has admissibility authority; freshness gate now safety-critical, not just observability); `calibration_eligible` flag now has **5 producer surfaces** (Items 2 + 4 + 5 + 6 + 10) — Item 6 is also the **deepest cross-spec cascade consumer** reading from ALL FOUR upstream producer surfaces; FK dependency cascade confirmed in **5 audits** with Item 6 confirmed as deepest cross-spec cascade consumer (calendar-blocker depth: 5 upstream specs = Items 1, 2, 4, 5, 7 plus transitive Item 10 V0.2 Layer 2 — making Item 6 V0.2 the LAST Cluster B alpha-generation phase to ship); decision_outcome enum coordination confirmed in **5 audits** with Item 6 adding writer commitment for `blocked_meta_labeler` + `reduced_meta_labeler` per `B-AI-006-9`; archived option-chain data substrate **confirmed quadrilaterally** in 4 audits (Items 4 V0.1 + Items 5 V0.2 + Item 10 V0.2 + Item 6 V0.2 transitively share same dependency — `C-AI-004-4` operator decision cascades to all four); LightGBM artifact-log substrate now confirmed in **2 audits** with Item 6 confirming bilateral consumer + producer role (Item 6 logistic scorecard artifact serialization + Item 6's LightGBM Direction Model dependency calendar-blocked on `C-AI-006-4`); Layer 1 / Layer 2 architectural pattern **confirmed as architectural template** (now confirmed in 3 audits: Items 5 + 10 + 6) with Item 6 P1.3.6 expanding the pattern with the `dormant pass-through` variant (Layer 1 can be dormant pkl-absent scaffolds-present AND still trigger same cutover policy framework); `realized_vol_20d` Layer 1 cutover anchor `fc64840` confirmed in 2 audits (Item 5 + Item 6 transitive consumer); 4 NEW themes added (Bayesian prior parameter governance — operator-tunable vs code-bound; LightGBM Direction Model spec ownership — catalog completeness; **Authority Recovery automatic-vs-operator-mediated — architectural principle 1 boundary** — CRITICAL bilateral with AI-SPEC-005 §5 — first authority-boundary-violation Class C in Cluster B — MOST CONSEQUENTIAL OPERATOR DECISION in P1.3.6; Memory Retrieval substrate dependency — `pgvector` + Top-20 analog query pipeline — deepest calendar-blocker on un-audited Item 7). §6 governance updates: D-023 wording extended with items (o) Item 6 authority boundary; (p) Authority Recovery operator-mediated disposition (CRITICAL bilateral with AI-SPEC-005 §5 amendment mandatory); (q) Layer 1/Layer 2 cutover policy for Item 6 (extends items g + l); (r) strategy taxonomy reconciliation for Item 6 (extends item m); (s) Bayesian prior parameter governance via `system-state.md`; (t) `decision_outcome` enum coordination — Item 6 emits `blocked_meta_labeler` (extends item b); possible new D-XXX (D-024 candidate) for LightGBM Direction Model spec ownership; new MASTER_PLAN Phase 4B (Meta-Labeler V0.2) + Phase 4C (Memory Retrieval V0.x conditional on future P1.3.7); new TASK_REGISTER §14F with 18 sub-items (canonical numbering 14F.0 through 14F.17); 2 new system-state.md fields (`meta_labeler` with most-comprehensive operational tracking yet — phase + calibration_grade_capable + Layer 1 dormant_pass_through tracking + Layer 2 phase + authority_state + authority_recovery_disposition + prior_parameters operator-tunable JSONB + labeled_candidates_count for promotion-gate counting + drift_state + drift_metrics; `memory_retrieval_substrate` for Item 7 substrate progression); constitution unchanged (pointer note only if D-023 ratified — Item 6 respects all T-Rules with **architectural principle 1 enforcement** explicit: Item 6 has authority to BLOCK trades and REDUCE size but NO authority to FORCE, UPSIZE, or override Item 1 Governor veto; reductive-only constraint via `min()` composition preserves capital preservation invariant). Risk rating: **CRITICAL** (driven by `C-AI-006-1` Authority Recovery escalation — first authority-boundary-violation Class C in Cluster B; bilateral with AI-SPEC-005 §5 amendment mandatory; calendar-blocker depth of 5 upstream specs makes Item 6 V0.2 the LAST Cluster B alpha-generation phase to ship; Item 6's admissibility authority elevates `_safe_redis()` dead code theme to safety-critical; mitigated by Layer 1 dormant pass-through state limiting immediate runtime risk to zero — Layer 1 inference at `execution_engine.py:328-399` is dormant pass-through with `meta_label_v1.pkl` absent at HEAD). Recommended status: `spec_has_semantic_drift_from_locked_intent`. AI-SPEC-006 redline at `trading-docs/08-planning/ai-architecture-audits/AI-SPEC-006.md`. **CLUSTER B AUDIT 2 of 4.** Cluster B remaining (Items 7, 9 per consolidated plan §4.B P1.3.7+). Cumulative findings across audits: 22 open Class C + 74 open Class B + 5 Class A; 1 finding resolved (`C-AI-002-2`). |
| 2026-04-28 | P1.3.7 | Added AI-SPEC-008 audit findings to register: 4 Class C entries (`C-AI-008-1` cross-spec producer→consumer contract gap — Item 8 §5 unilaterally claims 8-feature contract with Item 5 + 20-feature contract with Item 6; verified 2026-04-28 — Item 5 spec contains 2 abstract OPRA references only at lines 390 + 549 (no field-name enumeration), Item 6 spec is fully silent on Item 8 as 6th producer surface; mirrors `C-AI-002-1` writer/consumer-coordination pattern; default option 3 = DEFERRED BILATERAL CONFIRMATION (preserves audit-trail clarity; bilateral confirmation deferred to next P1.3.5 + P1.3.6 amendment cycle); D-023 enrichment item (w) = cross-spec contract ratification process governance; `C-AI-008-2` flow_agent migration cutover policy contradiction between two locked sources — locked spec §8 lines 444–462 explicitly states coexistence with role-narrowing; locked AI_BUILD_ROADMAP.md §3.9 line 372 explicitly states successor-replacement; mirrors `C-AI-005-2` Layer 1/2 cutover and `C-AI-006-2` `dormant_legacy_observability` patterns; default option 1 = coexistence with role-narrowing per locked spec §8 explicit text; folds into D-023 enrichment item (v) flow_agent / Layer 0 / Layer 2 cutover policy; `C-AI-008-3` Databento subscription scope — sub-second NBBO + sweep cluster reconstruction + block trade detection assumptions distinct from D-008 budget governance; **default option 4 = pre-implementation operator gate** (V0.3 implementation cannot begin until subscription scope verification completes); folds into D-023 enrichment item (u) Item 8 authority boundary; `C-AI-008-4` V0.x advisory-only acceptance bilateral with chain-archive — Item 8's Spec §6 lines 322–407 production-acceptance gate requires Item 6 V0.2 ship + chain-archive substrate sufficiency; bilateral with `C-AI-004-4` + `B-AI-006-1` + `C-AI-005-3` patterns; **default option 3 = explicit V0.3 advisory-only acceptance** logged in `system-state.md` `opra_flow_alpha.calibration_grade_capable = false`); 18 Class B entries (`B-AI-008-1` engine module file absent → canonical `backend/flow_alpha_engine.py` deterministic feature producer + separate Railway worker process per Spec §4 line 247; `B-AI-008-2` `opra:flow_alpha:*` Redis namespace 4 keys absent — `:latest` 15-min TTL, `:bars:1m` LTRIM 120 bars, `:bars:5m` LTRIM 80 bars, `:health` 5-min TTL; `B-AI-008-3` `opra_flow_feature_bars` + `opra_flow_events` Supabase tables absent → scheduled buildout with 21 NUMERIC columns + `calibration_eligible BOOLEAN NOT NULL DEFAULT false` (POSSIBLE 6th producer surface for `calibration_eligible` flag — scheduled for substrate hygiene only; Item 8 is NOT a 6th producer surface at spec level — theme stays at 5 surfaces); `B-AI-008-4` all 20 production features absent at HEAD; `B-AI-008-5` Lee-Ready aggressor inference + NBBO age tracking + `customer_delta = aggressor_sign * option_delta * size * 100` absent — bilateral with `C-AI-008-3` Databento subscription scope; `B-AI-008-6` sweep cluster definition + multi-exchange print disambiguation absent — bilateral with `C-AI-008-3`; `B-AI-008-7` block trade detection with rolling 95th-percentile distributions absent; `B-AI-008-8` quality tracking features (`lee_ready_inferred_share_5m` + `unknown_aggressor_share_5m`) absent → V0.3 ship adds threshold parametric (default 0.30) operator-tunable in `system-state.md`; `B-AI-008-9` separate Railway worker process per Spec §4 line 247 absent → V0.3 adds Procfile entry `flow_alpha_worker`; `B-AI-008-10` backpressure handling state machine (normal / degraded / stale) + 5-second staleness flag absent → V0.3 adds `BackpressureMonitor` class on `flow_alpha_engine.py`; `B-AI-008-11` walk-forward replay validation harness **calendar-blocked on Item 4 V0.1 + chain-archive substrate sufficiency per `C-AI-004-4` + Item 6 V0.2 ship per `B-AI-006-1`**; `B-AI-008-12` production acceptance gate (90+ clean replay sessions; 30+ paper-shadow sessions; Item 6 + OPRA beats Item 6 alone joint test) **calendar-blocked**; `B-AI-008-13` Item 5 integration shim (8 features) — bilateral with `C-AI-008-1` cross-spec contract ratification deferred-confirmation default; `B-AI-008-14` Item 6 integration shim (20 features + 1 quality flag) — bilateral with `C-AI-008-1` + calendar-blocked on Item 6 V0.2 per `B-AI-006-1`; `B-AI-008-15` Governor compact-summary emission absent — bilateral with Item 1 Card schema per `B-AI-001-2` (5/6-field count check pending Class A `A-AI-008-2`); `B-AI-008-16` `opra:flow_alpha:health` freshness gate substrate non-functional — `_safe_redis()` is dead code at HEAD (V4 — only def + docstring example caller in `prediction_engine.py:100` + `:120`); consumer wiring at Items 5/6/Governor sites absent; **carry-forward #4 — 4th audit confirmation HIGH-IMPACT; theme escalation: Item 8's freshness substrate is safety-critical for Cluster B alpha-generation tier**; `B-AI-008-17` storage namespace divergence — Spec uses `opra:flow_alpha:*`; HEAD uses `databento:opra:*` for Layer 0 raw-feed substrate; default coexistence resolution → `databento:opra:*` namespace remains as Layer 0 raw-feed substrate (continues serving GEX engine via `databento_feed.py:349 lpush`); `opra:flow_alpha:*` namespace ships as Layer 2 alpha-feature output; folds into D-023 enrichment item (v) flow_agent / Layer 0 / Layer 2 cutover policy per `C-AI-008-2`; `B-AI-008-18` cross-DTE derived feature `zero_dte_minus_1_3dte_pressure` + `flow_persistence_15m` formula — both features absent at HEAD; V0.3 ship adds `zero_dte_minus_1_3dte_pressure` to `opra_flow_feature_bars` as 21st column with explicit `is_derived = true` flag; `flow_persistence_15m` is implemented as feature #16 per spec); 2 Class A items (`A-AI-008-1` Spec §0 line 5 + §11 lines 530–533 declare tier as **V0.2** but Spec §12 lines 552–561 V0.3 Build Scope explicitly names V0.3; cross-internal-consistency drift — recommended correction: harmonize tier as **V0.3** per §12 most-operationally-binding label; `A-AI-008-2` Spec §5 lines 305–313 framing says `Compact Summary Only` (suggesting 1 dictionary) but the example dictionary shown contains **6 fields** (`flow_direction`, `flow_strength`, `vol_demand`, `sweeps`, `price_alignment`, `confidence`) not 5 — recommended correction: harmonize §5 framing to `6-field JSON dictionary` matching the example verbatim). **No findings moved to §4 Resolved Findings.** §5 cross-spec themes updated: `_safe_redis()` dead code now confirmed in **4 audits** with Item 8 P1.3.7 confirming **theme is now safety-critical for the entire Cluster B alpha-generation tier** — Item 8's 5-second backpressure handling per Spec §4 lines 257–258 makes freshness substrate even more time-sensitive than Items 1/5/6 (60-second TTL Redis keys); without `_safe_redis()` caller wiring at Items 5/6/Governor consumer sites for `opra:flow_alpha:*` keys, stale OPRA features could silently inform Items 5/6/Governor decisions after Databento feed lag; Layer 1 / Layer 2 architectural pattern **confirmed in 4 audits with 3 variants** — Items 10 (Layer 1 retrofit) + 5 (Layer 1 retrofit) + 6 (dormant Layer 1 + Layer 2 fresh ship) + 8 (**Layer 0 + Layer 2 coexistence variant** — `databento_feed.py` Layer 0 raw OPRA stream consumer + new `flow_alpha_engine.py` Layer 2 alpha-feature producer; NO LAYER 1 observability scaffold exists between them; Item 8 jumps directly Layer 0 → Layer 2; coexistence default per `C-AI-008-2` resolution + `B-AI-008-17`; cutover is therefore additive — no migration of `databento:opra:trades` consumers); 2 NEW themes added (cross-spec producer→consumer contract ratification process — sibling pattern to Layer 0/1/2 architectural template; Databento OPRA subscription scope — procurement decision distinct from D-008 budget governance — pre-implementation operator gate per `C-AI-008-3` default option 4); §6 governance updates: D-023 wording extended with items (u) Item 8 authority boundary; (v) flow_agent / Layer 0 / Layer 2 cutover policy for Item 8 (introduces Layer 0 + Layer 2 coexistence variant + flow_agent narrowing per `C-AI-008-2` default option 1); (w) cross-spec producer→consumer contract ratification process governance (per `C-AI-008-1` default option 3 DEFERRED BILATERAL CONFIRMATION); possible new D-XXX (D-025 candidate) for Databento subscription budget revision conditional on `C-AI-008-3` operator decision (default option 4 = no D-XXX needed if subscription tier sufficient); new MASTER_PLAN Phase 4D (OPRA Flow Alpha V0.3) sequenced AFTER Phase 4B (Item 6 V0.2) and ALONGSIDE Phase 4B since Item 8's integration shims feed directly into Item 6 V0.2 outputs; new TASK_REGISTER §14H with 18 sub-items (canonical numbering 14H.0 through 14H.17); 1 new system-state.md field (`opra_flow_alpha` with comprehensive operational tracking — phase + calibration_grade_capable + Layer 0 + Layer 2 phases + flow_agent_role + Databento subscription verification gate + cross-spec contract ratification status with Items 5 + 6 (DEFERRED BILATERAL CONFIRMATION default) + backpressure_state + freshness substrate health + storage architecture readiness); constitution unchanged (pointer note only if D-023 ratified — Item 8 respects all T-Rules with **architectural principle 1 enforcement** explicit: Item 8 produces measurements consumed by Items 5/6/Governor; Item 8 does NOT have authority to override Item 1 Governor veto, Item 6 Meta-Labeler skip, or Item 5 Vol Fair-Value Engine outputs; producer-only authority preserves capital preservation invariant; Lee-Ready aggressor inference per `B-AI-008-5` explicitly emits `aggressor_sign = 0` for unknown cases — explicit-unknown handling). Risk rating: **HIGH** (driven by `C-AI-008-1` cross-spec contract gap + `C-AI-008-2` flow_agent migration policy contradiction + `C-AI-008-3` Databento subscription pre-implementation operator gate + freshness gate substrate `B-AI-008-16` 4th HIGH-IMPACT confirmation; mitigated by `C-AI-008-4` V0.3 advisory-only acceptance default option 3 deferring production-binding to V0.4+ per chain-archive maturation cascade). Recommended status: `spec_has_semantic_drift_from_locked_intent`. AI-SPEC-008 redline at `trading-docs/08-planning/ai-architecture-audits/AI-SPEC-008.md`. **CLUSTER B AUDIT 3 of 4.** Cluster B remaining (Items 7, 9 per consolidated plan §4.B P1.3.8+). Cumulative findings across audits: 26 open Class C + 92 open Class B + 7 Class A; 1 finding resolved (`C-AI-002-2`). |
